### Verification-gap review result

### `nameTheHolder`'s re-read-failure fallback is unverified

- **Changed surface:** `nameTheHolder`'s `catch` block at `src/lib/server/nomination.ts:415-419`, which wraps the entire post-rollback naming re-read (`gateway.connect()`, `begin`, `loadEventsViaClient`, `rollback`, the fold and lookup) and is supposed to turn any failure of that re-read into `{ kind: 'unrecorded' }` rather than letting the exception escape `placeNomination`.
- **Impacted consumer or site:** `placeNomination` at `src/lib/server/nomination.ts:443` (its own `catch` block calls `nameTheHolder` at line 506), and transitively the `nominate` action at `src/routes/nominate/+page.server.ts:126-136`, which assumes `placeNomination` either resolves `accepted`/`rejected` or throws only a genuine bug — not a re-read hiccup.
- **Existing test evidence:** `Regression gap.` `tests/server/nomination.test.ts` has two `unrecorded` tests: "falls back to unrecorded when the naming re-read finds nothing" (line ~549) and the general naming tests (lines 503, 537, 570-596). Every one of them sets `eventsAfterRollback` to a log that the re-read **successfully reads**; the `unrecorded` outcome in that test comes from `nominationForPlayer`/`nominationForTeam` returning `null` inside the `try`, not from the `catch`. I searched the whole file for anything that makes the second `gateway.connect()` or the second `client.query('begin'/'rollback')` throw specifically on the re-read; none exists. The shared `fakeGateway`'s single `throwOn`/`throwPg` regex is matched against every statement regardless of which "connection" issues it, and `loadEventsViaClient`'s `select * from auction_events order by seq asc` (`src/lib/server/event-log.ts:124`) is the exact same statement used by the *initial* gate read inside `loadNominationState` (`src/lib/server/nomination.ts:115`) — so any `throwOn` configured to hit that statement would also break the initial `load` step before the code ever reaches the claim insert/23505 branch, making the `catch` at line 415 unreachable through this harness as currently built.
- **Missing verification:** an assertion that when the naming re-read itself throws (not "finds nothing"), `placeNomination` still resolves to a `rejected` outcome with `{ kind: 'unrecorded' }` rather than rejecting/throwing.
- **Demonstration:** if the `catch` block at line 415 were removed, weakened (e.g. only catching a specific error class), or changed to rethrow, no existing test would fail — the tests that assert `unrecorded` never cause the re-read itself to throw, they only cause the lookup inside the successful re-read to find nothing.
- **Consequence:** a bug in the "courtesy" re-read (e.g. a transient connection failure while naming the winner) would surface as an unhandled exception (a 500 in the route) instead of the documented graceful `unrecorded` refusal, and this would ship without any test catching it.
- **Suggested test shape:** extend the fake `ConnectionGateway`'s `connect()` to support returning a *different* client/behavior on the second call (e.g. a call counter), then have that second connection's `begin` or the re-read `select` throw a plain error, and assert `placeNomination` still resolves `{ kind: 'rejected', reason: { refusal: { kind: 'unrecorded' }, ... } }`.