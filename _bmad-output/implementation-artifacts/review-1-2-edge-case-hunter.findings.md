[{"location":"src/lib/core/money.ts:86","trigger_condition":"parseMoney(\"-00\") or similar multi-zero negative strings (\"-000\", etc.)","guard_snippet":"if (String(parsed) !== (text === '-0' ? '0' : text)) throw new TypeError(...);","potential_consequence":"Malformed leading-zero negative-zero strings silently parse to 0 instead of being rejected as the round-trip comment promises."},
{"location":"src/lib/core/money.ts:117-121","trigger_condition":"multiplyMoney(amount, factor) called with a negative factor, e.g. -1","guard_snippet":"if (factor < 0) throw new TypeError('money may only be scaled by a non-negative count');","potential_consequence":"A negative count silently produces a sign-flipped Money value where the doc comment implies only a non-negative count (holes)."},
{"location":"scripts/check-core-purity.js:203-214","trigger_condition":"Math['random']() reached via element access instead of dot access","guard_snippet":"else if (ts.isElementAccessExpression(node) && ts.isIdentifier(node.expression) && node.argumentExpression && ts.isStringLiteralLike(node.argumentExpression)) { /* same FORBIDDEN_MEMBERS check */ }","potential_consequence":"Math.random is reachable from the pure core via bracket notation without tripping the purity gate that blocks Math.random()."},
{"location":"scripts/check-core-purity.js:203-214","trigger_condition":"Math is aliased to another identifier before .random is accessed, e.g. `const m = Math; m.random();`","guard_snippet":"resolve node.expression's symbol via ts.TypeChecker instead of matching literal identifier text 'Math'","potential_consequence":"An aliased reference to Math.random silently passes the purity gate that a direct Math.random() call fails, since the check is purely name-based."},
{"location":"scripts/check-core-purity.js:101-111","trigger_condition":"an entry under src/lib/core is a symlink to a file or a directory","guard_snippet":"if (entry.isSymbolicLink()) { const real = statSync(full); if (real.isDirectory()) walk(full); else if (full.endsWith('.ts')) sources.push({ path: relative(root, full).split(sep).join('/'), source: readFileSync(full, 'utf8') }); }","potential_consequence":"A symlinked file or directory in the core is invisible to both entry.isDirectory() and entry.isFile(), so its contents are never checked by the purity gate."},
{"location":"scripts/bmad-cost-report.py:122-124","trigger_condition":"an assistant record has no timestamp field or one parse_ts cannot parse (ts is None)","guard_snippet":"if ts is None or ts < cutoff: continue","potential_consequence":"Records with missing or unparseable timestamps are always counted, silently including cost outside the requested --hours/--since window."},
{"location":"scripts/bmad-cost-report.py:68-70","trigger_condition":"no directory under ~/.claude/projects ends with the project root's slugified name","guard_snippet":"if not hits: print('warning: no matching project dir found; aggregating ALL projects', file=sys.stderr)","potential_consequence":"Cost report silently aggregates every unrelated project's token spend on the machine with no indication the fallback occurred."},
{"location":"scripts/bmad-cost-report.py:126-128","trigger_condition":"message.model does not contain 'opus', 'sonnet', or 'haiku' (e.g. a new/renamed model family)","guard_snippet":"if not t: agg[(scope, 'unknown')]['turns'] += 1; continue  # instead of silently dropping","potential_consequence":"Turns from any unrecognized model tier are dropped from the totals with no warning, undercounting actual spend."},
{"location":"scripts/bmad-cost-report.py:93","trigger_condition":"--since is passed a string dt.datetime.fromisoformat cannot parse","guard_snippet":"try: cutoff = dt.datetime.fromisoformat(args.since) except ValueError: sys.exit(f'invalid --since value: {args.since}')","potential_consequence":"Script exits with an unhandled Python traceback instead of a usable error message."},
{"location":"scripts/run-review-layers.sh:49-51","trigger_condition":"--auto re-run within 6h of a prior run, when *.findings.md output files already exist in the artifacts dir","guard_snippet":"find \"$ARTIFACTS\" -maxdepth 1 -type f -name '*.md' -newermt '-6 hours' ! -name '*.findings.md' ! -name 'deferred-work.md' ! -name 'sprint-status*' ! -name 'spec-*' | sort","potential_consequence":"Previously-generated .findings.md outputs get re-submitted to claude -p as if they were new review prompts, wasting cost and producing garbage output."}]
