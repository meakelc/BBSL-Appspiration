Good, line numbers confirmed. Now producing the final review result.

### CLI entry point of the purity gate has no test that exercises it

- **Changed surface:** the CLI wiring in `scripts/check-core-purity.js:271-283` — the module-identity guard (`import.meta.url.toLowerCase() === pathToFileURL(invoked).href.toLowerCase()`), the `process.stderr.write(...)` of formatted violations, and the `process.exitCode = 1` that together make the purity gate actually fail a build when the core is impure.
- **Impacted consumer or site:** `npm run build` (`package.json:12`: `"build": "node scripts/check-pins.js && node scripts/check-core-purity.js && vite build"`) and `npm run check:purity` (`package.json:17`). Per the CI workflow's own header comment (`.github/workflows/ci.yml:1-6`), `npm run build` is never invoked in CI — CI runs `npm test`, `npm run check`, and a separate Deno step, but not `build`. That makes this CLI block the only place this gate runs before a broken core reaches a Netlify deploy.
- **Existing test evidence:** `tests/purity.test.ts:7` imports only `checkCorePurity` and `readCoreSources` from the script and exercises them as library functions throughout, including the "the core as committed" block (`tests/purity.test.ts:118-131`), which calls `checkCorePurity(readCoreSources())` directly rather than invoking the script. No test in `tests/purity.test.ts` spawns the script or otherwise runs the code after line 270. The sibling gate script has exactly this coverage: `tests/pins.test.ts:361-397` contains a dedicated `describe('the CLI entry point', ...)` block that `spawnSync`s a probe replicating `check-pins.js`'s exit logic and asserts the exit code becomes `1` with the violation text on stderr. No equivalent block exists for `check-core-purity.js`.
- **Missing verification:** an assertion that running the script directly (e.g. via `spawnSync`, matching the pattern already used for `check-pins.js`) against a core containing a violation actually sets a non-zero exit code and writes the violation to stderr, and that it leaves the exit code unset for a clean core.
- **Demonstration:** if the case-insensitive module-identity comparison at line 275 regresses (e.g. a change to how `process.argv[1]` or `import.meta.url` is resolved, or the guard condition is otherwise never true on the actual runner), or if `process.exitCode = 1` on line 281 is dropped or replaced with a non-failing side effect, `node scripts/check-core-purity.js` would silently do nothing on a purity-violating core. Every existing test in `tests/purity.test.ts` would continue to pass unchanged, since all of them call `checkCorePurity`/`readCoreSources` directly and never go through line 271 onward.
- **Consequence:** the actual mechanism intended to block a deploy of a core that violates AD-1/AD-2 could break silently — `npm run build` would exit 0 on a violating core — with no test catching it, mirroring exactly the failure mode `tests/pins.test.ts:361-397` was written to guard against for the neighboring gate script.
- **Suggested test shape:** a `describe('the CLI entry point', ...)` block in `tests/purity.test.ts` mirroring `tests/pins.test.ts:361-397` — spawn the script (or a probe importing its exports) against a fixture core with a known violation and assert a non-zero exit with the violation text on stderr, plus a clean-fixture case asserting the exit code stays 0.