## Findings

Agent: `bmad-reviewer-acceptance` (opus). Story 2.3 is Tier A in `BMAD-EFFORT-TRIAGE.md:126` and the diff touches `src/lib/core/`; either alone selects the opus auditor.

- **The AC3 integration test does not exercise the `DELETE` grant it is specified to be the only exercise of.** Constraint: spec Code Map — "The only place `20260825000000_open_nominations.sql`'s DELETE grant is exercised." Evidence: the new block connects with the bare superuser URL and never assumes the granted role, unlike every other grant proof in that file (`set local role service_role` at `tests/integration/auction-events.test.ts:117,132,147,200`). `LOCAL_DB_URL` is the `postgres` role, which bypasses both the grants and the `force row level security` on `public.open_nominations`. The test would pass identically if the migration's `grant ... delete ... to service_role` were removed. The delete-against-real-Postgres and second-run-no-op halves of the bullet are met; the grant half is not.

- **`releaseNomination` reads the close payload with an unchecked cast, so the two release paths disagree on a malformed close.** Constraint: Boundaries & Constraints "Always" and the I/O matrix row "Malformed payload … Skipped, state unchanged / No throw". The fold got a defensive reader; the write side did not. On the same `AuctionClosed` the reducer skips, the deleter either throws a `TypeError` inside 3.4's appending transaction (null payload) or issues `where fantrax_player_id = null`. No test covers a malformed close on `releaseNomination`. The spec did say "mirroring `claimNomination`", and `claimNomination` casts the same way, so this is arguably compliant with the Code Map letter and non-compliant with the payload-contract intent.

- **`deferred-work.md:216` is claimed closed by this story but is neither updated nor actually resolved at runtime.** Constraint: Design Notes — "Tested-but-unregistered … closes `deferred-work.md:216` with a proven path." Evidence: the diff's only `deferred-work.md` change is two new entries appended after line 225; the entry at `:215-217` is untouched. Because `releaseNomination` is deliberately unregistered, no shipped code path deletes a claim row: after a close the fold frees the Slot while the row survives, so the next nomination by that Team hits `open_nominations_team_id_key` and is classified into a `slot_in_use` refusal. The deployment hazard persists until Story 3.4 registers.

- **`src/lib/core/rules/nomination.ts:82` now states something the diff falsifies.** It reads "There is deliberately no 'already won in this auction' refusal. No close event exists yet — `AuctionClosed` is Story 2.3's — so that state is unreachable", while the diff adds `AUCTION_CLOSED_EVENT` and a fold case reading it. The conclusion still holds, but its stated premise is now false. Raised rather than fixed because the spec's own Code Map and Verification block touching that file.

## Checked and conforming

AC1's matrix is fully covered in `tests/core/nomination.test.ts` (released on close, winner-irrelevant via `it.each`, never-bid, close-before-open, another-Player-closes, replay convergence, re-nomination both directions, malformed payload, hostile keys). AC2 holds: no `select` against `open_nominations` in `src/` (only the insert at `src/lib/server/nomination.ts:349` and the new delete at `:392`), `league-clock.ts` untouched, and the only release trigger is `AUCTION_CLOSED_EVENT`. The Never list holds: no producer, no production call site for `releaseNomination`, no timer/expiry/withdrawal, no `core/instant.ts`, no Bid Board, no `planning-artifacts/` edits. `omitKey` via `Object.fromEntries` and the `hasOwn` lookup satisfy the Code Map's prototype-safety instruction, and `fold`'s BigInt `seq` sort backs the reversed-array convergence assertion.
