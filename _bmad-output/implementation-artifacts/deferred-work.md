# Deferred Work

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-the-append-only-log-and-the-transactional-write-path.md`
  summary: Decide whether `pg_advisory_xact_lock` in the transactional shell needs a `lock_timeout`/`statement_timeout`, and if so what value.
  evidence: bmad-build review (2026-08-21), Blind Hunter layer. `src/lib/shell/write.ts` takes the AD-6 global write lock with no timeout configured, so a stuck or slow transaction blocks every other writer indefinitely with no documented recourse. The architecture spine's own AD-6 reasoning ("at 31 users global serialization costs nothing") argues against a timeout — the concurrency AC explicitly wants a second writer to *wait* for the first, not fail — so this is a product/ops decision (what value, if any) rather than a code defect, and is left to whichever story first operates the write path under real load.

- source_spec: none
  summary: Provision the two Supabase projects (wipeable dev, never-hand-touched prod) and wire their connection details as server-only environment variables.
  evidence: Story 1.1 AC 3 requires both projects to exist before a migration can be applied dev-first. Creating Supabase projects requires the Commissioner's own account and credentials, which the build agent cannot hold. Story 1.1 delivers the migrations directory, the env-var contract and the dev-first workflow; the projects themselves are provisioned by hand.
  assigned: 2026-09-05 to Story 9.1 (dev) and Story 9.8 (prod), Epic 9.

- source_spec: none
  summary: Create the Netlify site, run a production deploy reachable over HTTPS, configure deploy previews and branch deploys against dev Supabase with the production branch against prod, and record the credit cost of the configuration.
  evidence: Story 1.1 AC 5 requires a live deploy. Netlify site creation, branch-deploy configuration and deploy authorization all require the Commissioner's own account. Story 1.1 delivers netlify.toml, the pinned adapter configuration and the branch-to-environment mapping in committed form; the account-side setup and the first real deploy are done by hand.
  partial: 2026-08-20 — the site exists and is deployed. `https://bbslapp.netlify.app` answers HTTP 200 serving the built app, and deploy-preview, header-rule and redirect-rule checks run on pull requests. Still outstanding: confirming the branch-to-environment mapping is actually configured account-side against two Supabase projects that do not yet demonstrably exist, and recording the credit cost. Also note the site is now publicly reachable with no authentication in front of it.
  assigned: 2026-09-05 to Story 9.1 (Epic 9) — the outstanding half only; the site itself has been live since 2026-08-20.
  resolved: 2026-09-05 by Story 9.1. The branch-to-environment mapping is now confirmed account-side AND was found broken: `build_settings.allowed_branches` was `["main"]`, so netlify.toml's committed `branch-deploy -> dev Supabase` mapping had never been exercisable — a branch deploy could not be produced at all. Set to `["main","pilot"]` (specifically, not all branches: Netlify credits are 300/month and exhaustion pauses the site, AD-19). Verified per context via `netlify env:list --context`: production carries APP_ENV=production and SUPABASE_ENVIRONMENT=prod and NO Supabase credentials; deploy-preview and branch-deploy each carry all eight Story 9.1 values with SUPABASE_ENVIRONMENT=dev — matching netlify.toml exactly. Credit cost recorded: 100 deploys over the project's life total 3,259 build seconds (54.3 minutes) against a 300 minute/month ceiling, averaging ~27s per build; a pilot branch deploy measured 25s and is billed like any other. The `curl -I` half of this entry is NOT discharged — see the SSR-headers entry at the end of this file.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Add a CI workflow that runs the test suite and svelte-check on every push and pull request.
  evidence: Story 1.1's build path is `check-pins && vite build`; nothing invokes `vitest` or `svelte-check`. A reviewer demonstrated that all 113 tests can be red — or the drift gate removed from the build script entirely — while a Netlify deploy still succeeds. The four suites guarding the AR-2 tree, token parity, the pin contract and the Commissioner distinction can rot indefinitely without a runner.
  resolved: 2026-08-20 in `.github/workflows/ci.yml` (PR #2, merged as 86b58fd). Runs `npm ci`, `npm test` and `npm run check` on every push to `main` and every pull request; actions pinned to commit SHAs rather than tags. Verified green on main at 38373fc — 183 tests pass, svelte-check reports 0 errors. Note this covers the GitHub Actions builder only; the separate `npm ci` entry below concerns the Netlify build command and is untouched by this.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Add `supabase/config.toml` and document the migration and Edge Function deploy commands.
  evidence: `supabase/migrations/` and `supabase/functions/tick/` are established as canonical locations, but without a config file `supabase start`, `supabase db push` and `supabase functions serve` cannot run. The dev-first, migrations-only rule the .gitkeep states has no local workflow behind it. Belongs with Story 1.5, which first writes schema.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Write a README covering setup, environment configuration, the two-project Supabase topology, and how to run the tests.
  evidence: The repository ships a Discord OAuth app, a webhook, an out-of-band Commissioner recovery secret and a cron-invoked Edge Function with no setup path documented. Much of the current reasoning lives in .gitkeep comments that will be deleted the moment those directories receive real files.
  assigned: 2026-09-05 to Story 9.6 (Epic 9).
  resolved: 2026-09-05 by Story 9.6. `README.md` is the setup runbook: the two-project Supabase topology, the environment contract, the Discord application and webhook, the migration workflow, seeding, the Fantrax import, the Edge Function deploy and how to run the tests. Written FROM the Story 9.1-9.4 provisioning run rather than from memory, so every command in it has been executed against a real project. Three corrections to the environment contract came out of writing it and are applied to `.env.example` in the same change: SUPABASE_JWT_SECRET, DISCORD_REDIRECT_URI and DISCORD_GUILD_ID are read by no code at all and were removed, and APP_ORIGIN, TICK_INVOCATION_SECRET and DISCORD_WEBHOOK_URL are Edge Function secrets rather than Netlify variables — the README carries a table naming which of the three consumers reads each variable, because putting one in the wrong place fails silently. NOT discharged: the AC also asks that the runbook be followed end to end from a clean checkout by someone other than its author. Story 9.8 is that test, and is the honest place for it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Establish CSP, frame-ancestors, Referrer-Policy and a noindex posture for the private league.
  evidence: netlify.toml declares no headers block and svelte.config.js sets no kit.csp. An app carrying a Discord OAuth redirect and a secret-based Commissioner sign-in should establish its security headers before the first auth surface lands in Story 1.3.
  escalated: 2026-08-20 — no longer theoretical. The site is live and publicly reachable at `https://bbslapp.netlify.app`, and `curl -I` confirms no Content-Security-Policy, X-Frame-Options, Referrer-Policy or X-Robots-Tag is served. The noindex half now matters immediately rather than at Story 1.3: a private league's app is currently indexable.
  resolved: 2026-08-21 by Story 1.3. `netlify.toml` now carries a `[[headers]]` block scoped `for = "/*"` serving `X-Robots-Tag: noindex, nofollow, noarchive, nosnippet`, `Referrer-Policy: strict-origin-when-cross-origin`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Cross-Origin-Opener-Policy: same-origin`, `Permissions-Policy` and a Content-Security-Policy with `default-src 'self'`, `frame-ancestors 'none'`, `object-src 'none'`, `base-uri 'self'` and `form-action 'self' https://discord.com`. `tests/headers.test.ts` parses the block and asserts each header, including that the CSP names no wildcard host and no third-party host beyond `https://discord.com`. Two knowing limitations, both stated in the file: (1) `script-src` and `style-src` carry `'unsafe-inline'` because SvelteKit emits an inline bootstrap script in every SSR'd document and the nonce-based alternative (`kit.csp`) would emit a second, conflicting policy enforced as the intersection; (2) `connect-src` is `'self'` only — this story ships no Realtime client and neither Supabase project exists, so the Realtime origin is added as a literal host (`https://<ref>.supabase.co`, `wss://<ref>.supabase.co`, never `*.supabase.co`) when the provisioning entry above is discharged. The committed configuration is verified; `curl -I` against a real deploy is the outstanding manual check.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Record the design rationale, rate limiting, rotation policy and audit logging for COMMISSIONER_RECOVERY_SECRET.
  evidence: The out-of-band Commissioner sign-in is currently documented only as a comment in .env.example. Its rationale — Discord carries both authentication and notification, so an outage must not lock out the referee — is sound and belongs in a design record with its operational controls. Belongs with Story 1.3.
  resolved: 2026-08-21 by Story 1.3. The design record is the module header of `src/lib/server/commissioner-recovery.ts`, which states the rationale (AD-27), why the session is application-signed rather than a Supabase session (every other first-party path reintroduces the address field FR-4 forbids), and each operational control: constant-time comparison via `timingSafeEqual` over SHA-256 digests so neither length nor first-wrong-byte is observable; per-source sliding-window throttling (5 attempts / 15 minutes) applied *before* the comparison, so a throttled source cannot use the response as an oracle; a signed absolute expiry of 12 hours carried inside the signature, so altering it is forgery rather than extension; and rotation by changing the Netlify environment variable, which invalidates every outstanding cookie immediately because the signing key is `HMAC(secret, 'bbsl.break-glass.v1')`. `tests/commissioner-recovery.test.ts` drives all of it. Two gaps are recorded rather than closed: the attempt ledger is in-memory and therefore per-function-instance, so the effective ceiling is the stated one times the instance count — the durable form is a counter in the database and belongs with Story 1.5, the first story with a write path; and audit logging of attempts needs the append-only log, which is also Story 1.5.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Add a component-test harness (jsdom or browser project) and an +error.svelte page.
  evidence: vite.config.ts sets `environment: 'node'` and includes only `tests/**`, so no .svelte file can be rendered in a test and no colocated test under src/ is collected. There is also no +error.svelte, so an error renders on the browser default white background, outside the dark-only contract. The Playwright greyscale check that Story 1.7 inherits has no harness waiting for it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Enforce the pure-core import boundary by walking src/lib/core/** and asserting explicit .ts extensions on relative specifiers.
  evidence: structure.test.ts checks a hardcoded three-file list and its stated ".ts imports only" rule is not actually asserted — `from './money'` and `from './money.js'` both pass, and both fail to load under Deno. src/lib/core/rules/ and projection/ are never scanned. Story 1.2's acceptance criteria own this purity check in CI; building it here would pre-empt that story.
  resolved: 2026-08-20 by Story 1.2. `scripts/check-core-purity.js` walks `src/lib/core/**` recursively and parses each file with the TypeScript compiler, so comments and strings are not scanned; `tests/purity.test.ts` drives it, and the gate runs in `npm run build` and `npm test`. The superseded block in structure.test.ts is deleted. `deno check` runs as a CI step, covering the half a static walk cannot.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Replace the CSS-generated Commissioner label with a Svelte component that emits it into the DOM with aria-describedby.
  evidence: The ::before label cannot be translated or selected and vanishes if the stylesheet fails to load, leaving an unmarked dashed button. Story 1.1 patches it to be presentational so it is not announced twice; the durable fix is a component, and it belongs with Story 1.7, which builds the first real Commissioner surface.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Handle Windows High Contrast / forced-colors mode so the Commissioner differentiators survive it.
  evidence: Under forced-colors the author-declared fill and ground are overridden by the OS palette, collapsing two of the four differentiators and leaving the referee control harder to distinguish from a player control — the exact confusion the four-property design exists to prevent.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Replace structure.test.ts's `.gitkeep`-presence assertion with a non-empty-directory assertion.
  evidence: The test requires a .gitkeep in nine directories, but each marker should be deleted the moment a real file lands there — so the test is built to fail on a correct change. Story 1.5 will be the first to trip it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Verify tsconfig.json actually type-checks scripts/ and tests/, and add an explicit include if it does not.
  evidence: tsconfig inherits `include` from the generated .svelte-kit/tsconfig.json, which covers src/** and not sibling top-level directories — so allowJs/checkJs may never check scripts/check-pins.js, and npm run check may give no coverage of the test suite that imports it. Confirm with `npx tsc --showConfig`.
  verified: 2026-08-20 at 38373fc — the suspicion is confirmed and the remedy is still outstanding. `npx tsc --showConfig` resolves `include` to `.svelte-kit/ambient.d.ts`, `.svelte-kit/env.d.ts`, `.svelte-kit/non-ambient.d.ts`, `.svelte-kit/types/**/$types.d.ts`, `vite.config.{js,ts}`, `src/**/*.{js,ts,svelte}`, `test/**/*.{js,ts,svelte}` and `tests/**/*.{js,ts,svelte}`. So `tests/` IS covered, `scripts/` is NOT, and `checkJs` is true — `scripts/check-pins.js` is never type-checked despite being the build's first gate. Only the verification half of this entry is discharged; the explicit `include` has not been added.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Reconcile whether the design intends `#1D2922` as the corrected Manager control fill in DESIGN.md itself.
  evidence: Story 1.1 corrected `--control-fill` from DESIGN.md:229's `#223028` to `#1D2922` because the original measured 2.77:1 against the control boundary, below WCAG 1.4.11's 3:1. DESIGN.md is status:final and its line 229 still carries the failing value, so the design document and the implementation now disagree until the correction is folded back at source.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Commit the planning artifacts, or vendor DESIGN.md's token frontmatter into the repository.
  evidence: tests/tokens.test.ts reads DESIGN.md out of _bmad-output/ at module load. Story 1.1 patches the path to resolve by glob, but the token suite still depends on a planning directory being committed alongside the source and never moved. Vendoring the frontmatter would make the repository self-contained.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Use `npm ci` rather than `npm install` in the Netlify build command once the lockfile is committed.
  evidence: Direct dependencies are pinned exactly, but transitive versions resolve fresh on every build unless the lockfile is both committed and installed from. `npm ci` is the flag that makes the pin contract hold below the top level.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-deployable-skeleton-on-the-pinned-stack.md`
  summary: Make `check-pins.js`'s CLI entry-point detection case-insensitive on Windows drive letters.
  evidence: bmad-code-review (2026-08-20) on `f6f7be4`, Edge Case Hunter layer. The guard `import.meta.url === pathToFileURL(invokedPath).href` compares two URL strings whose drive-letter casing can legitimately differ on Windows (`C:` vs `c:`) depending on how the process was invoked. When they disagree the CLI branch never runs, so `node scripts/check-pins.js` silently does nothing — no drift check, no error, no exit code — on a local Windows dev machine, this repository's actual environment. CI is unaffected (`ubuntu-latest`), and `npm run build`'s own invocation was verified to still trip the check correctly in the current tree, so this is a latent gap rather than an observed regression. Fix is a case-insensitive comparison of the two URL strings before deciding whether to run.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-the-pure-core-boundary-and-integer-money.md`
  summary: Make tests/pins.test.ts's CLI test spawn scripts/check-pins.js itself instead of a probe that re-implements its exit logic.
  evidence: Surfaced incidentally while closing the same gap for the new purity gate. `tests/pins.test.ts:361-386` builds an inline probe that imports `checkPins` and then re-writes the CLI's own `for (const error ...) process.stderr.write(...)` and `process.exitCode = result.ok ? 0 : 1` lines, so it asserts that the probe works, not that `check-pins.js`'s entry point does. The real guard — `import.meta.url === pathToFileURL(invokedPath).href`, still case-sensitive there and the subject of an open deferred entry about Windows drive-letter casing — is never executed by any test. `tests/purity.test.ts` now spawns its script directly against a temp fixture via an optional root argument; the same shape would work here if `check-pins.js` accepted its inputs from a path.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sign-in-with-discord.md`
  summary: Give the Commissioner break-glass path a durable attempt record and a throttle that holds across function instances.
  evidence: bmad-build review (2026-08-21), Blind Hunter and Edge Case Hunter layers. `src/lib/server/commissioner-recovery.ts`'s `AttemptLedger` is an in-memory Map, so the 5-attempts-per-15-minutes ceiling is per Netlify function instance — the effective ceiling is that number times the instance count — and every record of an attempt, accepted or refused, is lost on cold start. A brute-forced or successfully used break-glass secret currently leaves no durable trace anywhere. Both halves need a write path: Story 1.5 delivers the append-only log and the transactional shell, and the durable form is a counter plus an event there. The module's own docstring already states the intent and defers the wiring.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sign-in-with-discord.md`
  summary: Admit the Supabase Realtime origin to the CSP's connect-src once the two Supabase projects exist to name.
  evidence: `netlify.toml` ships `connect-src 'self'` and `tests/headers.test.ts` pins it with `expect(CSP_DIRECTIVES.get('connect-src')).toEqual(["'self'"])`. That was correct while Story 1.3 shipped no Realtime client. **Story 4.1 (2026-09-01) has now landed one, so this entry is BLOCKING rather than anticipatory**: a deployed browser's `auction_watermark` socket is refused by this directive today. 4.1 kept the policy and the assertion unchanged on purpose — neither project is provisioned, so there is still no ref to name, and the freshness contract degrades honestly to Reconnecting while its same-origin liveness poll (which needs no widening) keeps the board refreshing. It is closed by adding the two projects' literal hosts: `https://<ref>.supabase.co` and `wss://<ref>.supabase.co`, never `*.supabase.co`. The pinning test must be updated in the same change, which is deliberate — the widening cannot happen silently. Until then, production meets the epic's 5-second board floor on the poll interval rather than on the push.
  assigned: 2026-09-05 to Story 9.3 (Epic 9), unblocked by Story 9.1 giving it a ref to name.
  resolved: 2026-09-05 by Story 9.3, and NOT the way this entry anticipated. The plan here was to add both projects' literal hosts to `netlify.toml`; that would have been worse, because one TOML file serves every context, so production would admit dev's host and dev would admit production's, permanently. `connect-src` is now DERIVED from PUBLIC_SUPABASE_URL in `src/lib/server/security-headers.ts`, so each deployment admits exactly its own project and Story 9.8 adds prod without touching the policy. A host that is not a plain hostname degrades to `'self'` rather than being interpolated, so `*.supabase.co` cannot reach the policy by any input — asserted over arbitrary input in `tests/headers.test.ts`, not over one policy string. netlify.toml keeps `connect-src 'self'`, now for a sharper reason: that block reaches static assets only and no static asset opens a WebSocket. Verified on the live pilot deploy: `connect-src 'self' https://ymtermqgujdemgxnbgku.supabase.co wss://ymtermqgujdemgxnbgku.supabase.co`, no wildcard.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sign-in-with-discord.md`
  summary: Add a CSP violation reporting endpoint so policy failures are observable rather than discovered by a broken page.
  evidence: bmad-build review (2026-08-21), Blind Hunter layer. The policy in `netlify.toml` carries no `report-to`/`report-uri` directive, so a violation — or a future misconfiguration introduced when connect-src is widened per the entry above — is invisible in production. The only verification the story has of the live response is a one-off manual `curl -I`; a reporting endpoint would make that continuous. Low urgency while the app has no Realtime client and one page, but it grows with every origin added.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-teams-managers-and-the-commissioner-role.md`
  summary: Constrain `managers.is_commissioner` cardinality and its relationship to `team_id` — decide and enforce whether zero/one/many Commissioners are valid, and whether `is_commissioner = true` with `team_id` still null is a valid state.
  evidence: bmad-build review (2026-08-21), Blind Hunter and Acceptance Auditor layers. `20260821010000_teams.sql` adds `is_commissioner boolean not null default false` with no uniqueness or check constraint, and `commissioner-guard.ts`'s refusal logic and its docblock both write as if "the Commissioner" is a singular identity, but nothing in the schema prevents zero, one, or many `managers` rows from carrying the flag simultaneously, or prevents a Commissioner row with no Team. Low urgency now — the Commissioner writes these columns directly via SQL, a trusted single-operator path — but worth a partial-unique-index / check-constraint hardening pass once the product settles the intended cardinality.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-one-destination-list-gated-by-phase-and-role.md`
  summary: Confirm whether `serviceRoleClient()` (`src/lib/server/supabase.ts`) is reused as a singleton, or fix it to be one, now that `resolveLeaguePhase`'s default parameter constructs one on every request via `hooks.server.ts`.
  evidence: bmad-build review (2026-08-22), Blind Hunter layer, on Story 1.6's first implementation pass. `export function serviceRoleClient(): SupabaseClient { return createClient(...) }` builds a fresh client on every call; Story 1.4's `managerRegistry(client = serviceRoleClient())` already called this only per-registry-lookup (once per session resolution), but Story 1.6's `resolveLeaguePhase(client: SupabaseClient = serviceRoleClient())` now calls it on every single request via `hooks.server.ts`. Not a Story 1.6 defect — the pattern and the function predate it — but the new call frequency makes any per-call construction cost (or connection-pooling assumption) worth verifying now rather than later.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-one-destination-list-gated-by-phase-and-role.md`
  summary: Log/alert when `resolveLeaguePhaseOrDefault` swallows a phase-read failure and falls back to Setup, so a persistent (not just transient) outage is observable rather than silently indistinguishable from a genuinely empty log.
  evidence: bmad-build review (2026-08-22), Blind Hunter layer, review-loop-iteration 2 on Story 1.6. The fail-closed wrapper added at iteration 1/2 catches every error identically with no logging, metric, or trace — correct behavior for the request (never 500 on a DB hiccup), but a real, ongoing outage would look identical to "no events yet" with nothing to page anyone. No logging/observability infrastructure exists yet anywhere in this repo, so this is naturally deferred rather than built ad hoc for one call site.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-one-destination-list-gated-by-phase-and-role.md`
  summary: Minor polish items on the header menu / destinations list, noted but not blocking Story 1.6 — (1) the `<details>` menu has no Escape-key or click-outside close, only close-on-navigate; (2) neither component sets explicit `aria-expanded`/`aria-current` beyond native `<details>` semantics; (3) `resolveDestinations`'s Manager-then-Commissioner-only ordering is enforced only by `tests/destinations.test.ts`, not structurally.
  evidence: bmad-build review (2026-08-22), Blind Hunter layer, review-loop-iteration 2 on Story 1.6. None of the three has a design mock to build against (`EXPERIENCE.md` flags the sheet/menu interaction `[DEFERRED]`), and none is a correctness or security gap — worth a look whenever this surface gets a real interaction design, e.g. alongside Story 4.2's persistent-strip sheet, which reuses the same `DestinationsList` component.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-one-destination-list-gated-by-phase-and-role.md`
  summary: Two minor header-menu UX notes, not blocking — (1) `HeaderMenu` now renders on `/commissioner-recovery` too, adding site-wide chrome to a page whose design intent (AD-27) is minimal, unadvertised footprint; the menu itself reveals nothing about break-glass, so this is a design-consistency note, not a leak. (2) A signed-out visitor on `/signin` now sees a second, redundant "Sign-in" link in the header pointing back at the same page they're already on.
  evidence: bmad-build review (2026-08-22), Blind Hunter layer, review-loop-iteration 3 on Story 1.6. Both are direct consequences of `+layout.svelte` rendering the header menu on every route, which is correct per AD-30 ("one destination list... rendered on every page"); resolving either would mean special-casing specific routes against that rule, a design call worth making with real interaction design rather than ad hoc in this story.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-one-destination-list-gated-by-phase-and-role.md`
  summary: Build the actual "Manager registration" admin page/route that Story 1.6's destination list points at.
  evidence: bmad-build planning (2026-08-22). Epics.md names "Manager registration" as a live Setup-phase destination (Story 1.6's AC), but no story in the epic owns building the page itself — Story 1.4 built only the `managers.team_id`/`is_commissioner` schema and its guard, and its own spec explicitly excluded any admin UI ("no new page or route is in this story's scope"). Story 1.6 wires the destination as a catalog entry with no route behind it yet, the same treatment as Import/Eligibility/the auction-open gate, whose routes are owned by 1.7-1.11 respectively — but unlike those, nothing downstream currently claims this one. User confirmed (2026-08-22) treating it as a stub link deferred to a future story rather than building it inline or dropping it from the list.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-import-thirty-team-roster-files.md`
  summary: Persist a file-altitude refusal (`refused_file`) for a Team with nothing currently staged, so it survives a refresh rather than living only in the upload batch's transient result.
  evidence: bmad-build review (2026-08-24), Blind Hunter and Acceptance Auditor layers on Story 1.7 review-loop-iteration 1. `import_team_sources`'s `status` check constraint includes `refused_file`, but no code path writes it: an unmatched-file or ambiguous-file refusal has no `team_id` to key by, and an already-claimed-in-batch refusal must not overwrite an earlier file's status from the same drop. Low urgency — the Commissioner sees the refusal immediately in the same upload's result, and retrying is cheap (re-check the file name, re-drop it); the outstanding-Teams list still correctly shows the Team as needing a file either way.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-import-thirty-team-roster-files.md`
  summary: Add a database-level `unique(team_id, fantrax_player_id)` constraint on `import_staged_rosters` and a non-negativity check on `cap_hit`, as defense-in-depth alongside the app-level checks `parseRosterCsv` now performs.
  evidence: bmad-build review (2026-08-24), Blind Hunter layer on Story 1.7 review-loop-iteration 1. The app-level checks added this round (duplicate Fantrax Player ID, negative Cap Hit) make both scenarios unreachable through the shipped code path, so a DB constraint is redundant today; worth adding if a second write path to this table is ever introduced.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-import-thirty-team-roster-files.md`
  summary: Add a per-Team advisory lock (or `select ... for update`) around `stageRosterFile`'s staging transaction, an actor/audit column on `import_team_sources`, a batch file-size/count guard, and re-checking `requireLiveDestination` mid-batch rather than only once before the loop.
  evidence: bmad-build review (2026-08-24), Blind Hunter and Edge Case Hunter layers on Story 1.7 review-loop-iteration 1. All four are real but low-probability-or-impact for this app's single-Commissioner, one-league-at-a-time usage: two concurrent uploads for the same Team resolve via `on conflict (team_id) do update` (last write wins, no corruption, just no detection); staging writes intentionally skip `auction_events` (Design Notes) so audit logging arrives with promotion in 1.9, not here; upload payloads are Commissioner-only and small (CSV rosters); a phase transition mid-batch would require the Commissioner to open the auction (Story 1.11, a deliberate one-time action) while their own import is still running.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-import-the-free-agent-pool.md`
  summary: The pool/roster cross-source conflict check is not safe against two concurrent /import requests.
  evidence: findPoolConflicts and findRosterConflicts are plain SELECTs in independently committed per-file transactions with no lock, so two simultaneous uploads can each pass their check before the other commits, landing a Player in both the pool and a roster. Same class as 1.7's already-deferred per-Team concurrency gap; staging deliberately skips the global write lock (AD-28).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-import-the-free-agent-pool.md`
  summary: Nothing detects the same Fantrax player ID appearing on two different Teams' staged rosters.
  evidence: import_staged_rosters carries no uniqueness on fantrax_player_id across Teams, and 1.7 built no cross-Team check. Surfaced by 1.8's review, not caused by it — 1.8 only added pool-vs-roster conflict detection. Belongs with 1.9's promotion, where all thirty-one sources are validated as one set.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-per-team-preview-and-atomic-promotion.md`
  summary: AR-33 — obtain a real Fantrax export and confirm the roster and Free Agent pool column mappings against it. Outstanding HUMAN action, before setup day.
  evidence: `ROSTER_COLUMNS` (`src/lib/adapters/fantrax/roster-file.ts`) and `POOL_COLUMNS` (`src/lib/adapters/fantrax/pool-file.ts`) are documented placeholders carrying `TODO-confirm`, taken from `addendum.md`'s described shape rather than from a real export. Stories 1.7 and 1.8 each deferred the confirmation to 1.9; 1.9 does not confirm it either, because doing so requires a real Fantrax export that only the Commissioner's own Fantrax account can produce — the build agent cannot hold those credentials. The two column maps are deliberately independent (no shared base was extracted) so each is a one-file edit when the export lands. Nothing else in the codebase names a Fantrax CSV column (AD-24), so the blast radius of getting this wrong is exactly those two objects — but until it is done, every one of the thirty-one files will refuse at content altitude on setup day if the real headers differ. The eligibility flag is settled as absent from the export and needs no confirmation.
  assigned: 2026-09-05 to Story 9.5 (Epic 9), scheduled first in the epic as the only unbounded-risk item.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-per-team-preview-and-atomic-promotion.md`
  summary: `loadEventsViaClient` reads the whole `auction_events` log into memory on every promotion, unbounded.
  evidence: `src/lib/server/event-log.ts`'s `loadEventsViaClient` issues `select * from auction_events order by seq asc` with no limit, and `pg`'s `query()` buffers the entire result rather than streaming it. Harmless as shipped — promotion runs only in Setup, where the log is empty or nearly so — but the same full-log fold pattern is used by `resolveLeaguePhase` and will be reached by later phases with a large log. Pre-existing in kind (1.5 established the fold-the-whole-log pattern), surfaced by 1.9's review rather than caused by it. A bounded or snapshot-based phase read is the fix when a story first folds the log outside Setup.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-per-team-preview-and-atomic-promotion.md`
  summary: Promotion does not re-derive the pool/roster Player-overlap conflict inside its transaction, and no database constraint spans the two live tables.
  evidence: `refusePromotion` covers phase, outstanding sources and slot ceilings, but not the pool/roster conflict 1.8 checks at staging time in both directions. The staged set is conflict-free by induction (every stage re-checks), so this is not reachable today; `team_rosters` and `free_agent_players` each carry their own `fantrax_player_id unique` with nothing cross-referencing them, so the disjointness is enforced by application code alone. Same shape as the cross-request TOCTOU 1.8 already logged. A cross-table exclusion — or re-deriving the conflict inside the promotion transaction — would make it structural.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-per-team-preview-and-atomic-promotion.md`
  summary: The "promotion is refused once the auction has opened" gate has no end-to-end test, only a unit test of the decision function.
  evidence: `phaseReducer` (`src/lib/core/projection/phase.ts`) has no `AuctionOpened` case until Story 1.11, so no log content can fold to anything but Setup and `promoteImport` cannot be driven to a `phase` rejection. The gate itself is real and is covered at `refusePromotion({ phase: 'Auction', ... })`, and a promotion-level test asserts the log is read inside the transaction before any staging. 1.11 should add the end-to-end case when it teaches the reducer `AuctionOpened`.
  resolved: 2026-08-25 by `spec-1-11-open-the-auction.md`. `phaseReducer` (`src/lib/core/projection/phase.ts`) now carries the `AuctionOpened` case, so a log folding to Auction is constructible. `tests/server/import-promotion.test.ts` drives `promoteImport` end to end through a real `AuctionOpened` log entry and asserts the `phase` refusal kind, the folded phase name, the core's own sentence via `promotionRefusalDetail`, and that no event was appended and no live roster deleted before the rollback.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-set-minor-league-eligibility-by-hand.md`
  summary: The "eligibility is refused once the auction has opened" gate has no end-to-end test through `setEligibility`, only a unit test of the exported `refuseEligibilityChange` gate.
  evidence: bmad-build review (2026-08-25), Acceptance Auditor layer. `phaseReducer` (`src/lib/core/projection/phase.ts`) still has no `AuctionOpened` case, so no log content a fake gateway can return folds to anything but Setup, and `setEligibility` cannot be driven to a `phase` rejection through its own transaction. The gate itself is real and is covered at `refuseEligibilityChange({ phase: 'Auction', ... })`, and a transaction-level test proves the phase is folded from the log before any pool read. Identical in kind to the gap 1.9 logged for `promoteImport`; 1.11 should close both when it teaches the reducer `AuctionOpened`.
  resolved: 2026-08-25 by `spec-1-11-open-the-auction.md`. Same root cause and same round as the 1.9 entry above. `tests/server/eligibility.test.ts` now drives `setEligibility` end to end through a real `AuctionOpened` log entry: one new case asserts the refusal end to end, and two existing pure-gate cases were strengthened to assert the phase is named with the FR-35 override sentence and that the phase gate fires before an unknown-id complaint.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-set-minor-league-eligibility-by-hand.md`
  summary: `/minor-league-eligibility` renders the entire Free Agent pool as one list with no pagination, search or filter, and its select-all checkbox has no indeterminate state.
  evidence: bmad-build review (2026-08-25), Blind Hunter layer. A real Fantrax pool is hundreds of Players, and the Commissioner typically wants to flag a handful — scanning one unbroken list to find them is the wrong shape, and a partially-ticked select-all currently renders as plain unchecked rather than the conventional mixed state. Neither is a correctness or accessibility-floor failure (every row states its own state in words and the touch targets hold), and neither has a design mock to build against — `EXPERIENCE.md` specifies this surface in prose only, which epic-1-context.md names as the epic's highest design-drift risk. Worth revisiting with a real export in hand (AR-33) so the list's true size is known rather than guessed.
  assigned: 2026-09-05 to Story 9.7 (Epic 9) — revisit with the real pool in hand, as this entry itself recommends.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-11-open-the-auction.md`
  summary: AD-22 must be amended to name `AuctionOpened` as the League Clock's origin event, distinct from the two reset events.
  evidence: bmad-build planning (2026-08-25). Epics.md:740 requires the League Clock to start at 48 hours when the auction opens, but AD-22 (`ARCHITECTURE-SPINE.md:211-216`) states the clock is reset by exactly two event types — Nomination and accepted Bid — and that "new event types default to *not* resetting it; extending the set requires changing this AD." Story 1.11 resolves the contradiction by folding the clock's origin from `AuctionOpened` while leaving the reset set at two, which is consistent with AD-22's intent (an origin is not a reset, so a `BidVoided` compensation can never unwind past the open) but is not yet written down in the AD. The spine is owned by the `bmad-*` architecture skills and is not hand-edited here; the amendment needs a `bmad-create-architecture` or `bmad-correct-course` run.
  resolved: 2026-08-25 by `bmad-correct-course` — see `_bmad-output/planning-artifacts/sprint-change-proposal-2026-08-25.md` (scope: Minor, Direct Adjustment, approved in batch mode). AD-22 (`ARCHITECTURE-SPINE.md`) carries a new third bullet, "An origin is not a reset", naming `AuctionOpened` as the League Clock's origin, fixing the expiry at `LEAGUE_CLOCK` after the **later** of the origin and the latest surviving reset, and confirming the two-event reset set and its default-not-resetting rule are unchanged. The same reconciliation was propagated to the three documents that restate the reset set: `epics.md` AR-22, PRD FR-22 (`prd.md`) and SPEC CAP-9 — each now distinguishes the start from a reset and states that no void recomputes the Clock back past the origin, which settles the previously unaddressed edge of voiding the only bid of a just-opened auction. `src/lib/core/projection/league-clock.ts` now cites the AD rather than arguing for the distinction. No §10 example outcome changes, so AD-25's same-commit rule was not triggered; no story, epic, schema or test changed and `sprint-status.yaml` needed no edit.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-11-open-the-auction.md`
  summary: No Manager is notified when the auction opens; the `AuctionOpened` event is appended with no delivery intent.
  evidence: bmad-build planning (2026-08-25). Epics.md:746 requires every Manager to be notified at open, but AD-17's transactional outbox does not exist — `enqueue` is a documented unimplemented seam (`src/lib/shell/write.ts:22,84`) and no outbox table appears in any migration. Notification transport is Discord only (AD-18) and is owned by Epic 5.1. Deferred by user decision (2026-08-25) rather than building a one-off dispatcher this story would be the only caller of. The event carries `seq`, actor and timestamp, so the notice is reconstructible from the log when the outbox lands.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-11-open-the-auction.md`
  summary: The designed empty Bid Board screen for a just-opened auction is not built.
  evidence: bmad-build planning (2026-08-25), SCOPE STANDARD multi-goal check. Epics.md:743-745 requires an arriving Manager to meet a designed empty-board screen that explains the state and points at Nominate. `/board` is an Auction-phase destination (`src/lib/server/destinations.ts:76`) with no route behind it, and the Bid Board itself is built in Epic 2. The gate and the empty board are two independently shippable deliverables — each reviewable and mergeable without breaking the other — so the user chose [S] Split (2026-08-25) and the empty state goes with the board that owns it. Nominate, which the screen must point at, is also Epic 2.1.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-11-open-the-auction.md`
  summary: `leagueClockReducer` folds the League Clock's origin but has no production consumer — the reducer and `LEAGUE_CLOCK` are joined only in a test.
  evidence: bmad-build review (2026-08-25), Blind Hunter and Acceptance Auditor layers. `src/lib/core/projection/league-clock.ts` is imported only by `tests/core/auction-open.test.ts`; no shell code adds `LEAGUE_CLOCK` to the folded origin to produce an absolute expiry, so AC4's "48 hours from it" is proven in a test rather than by a running clock. This is the same deliberate shape as the Nomination Slot decision — nothing READS the League Clock until the Auction Phase exists, and AD-5 says projections are built by the story that first reads them. Story 2.1's nomination command and Epic 3's sweep are the first readers; the fold and its idempotency are already correct and tested, so the remaining work is wiring, not derivation. Flagged rather than built because a consumer written now would have no caller to prove it right.
  resolved: 2026-09-01 by Story 3.7. `src/lib/server/phase-end.ts`'s `evaluateLeagueClock` is the consumer: it folds `leagueClockReducer` over one `loadEventsViaClient` read inside a `runTransactionalWrite`, asks `leagueClockExpiry` for the absolute instant, and hands it to the pure `decidePhaseEnd` (`src/lib/core/rules/phase-end.ts`) alongside the database's transaction-start clock. `src/lib/server/sweep.ts` calls it once per tick pass, after the closes and before the drain, and reports what it did on the heartbeat — so `LEAGUE_CLOCK` reaches a running clock rather than a test. What a Manager can see: the phase folds to Contract Assignment, every still-unbid nomination is terminated and its Nomination Slot freed, the ninth `PLACE_BID_GATES` gate refuses every Bid league-wide, `refuseNomination`'s phase gate becomes reachable for the first time, and `src/routes/+layout.svelte` states the transition in words on every page. The fold also gained the `BidVoided` case AD-22 warned about — resets are now kept by `seq` and the expiry recomputes past a voided one without ever passing the origin — proven by §10 examples 13 and 27 (`tests/examples/example-13-league-clock.test.ts`, `tests/examples/example-27-a-voided-bid-shortens-the-clock.test.ts`) and by `tests/core/league-clock.test.ts`, `tests/core/phase-end.test.ts` and `tests/server/sweep.test.ts`.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-11-open-the-auction.md`
  summary: The `/auction-open` surface's rendering and gating branches are verified only by matching source text, never by rendering the component.
  evidence: bmad-build review (2026-08-25), Verification Gap layer, with a worked demonstration: inverting `blocked = !report.ready || !confirmed` to `&&` would enable the submit for an unconfirmed-but-ready state and every existing assertion still passes, because they are `readFileSync` + `toContain` on the `.svelte` source and no assertion reads the derivation's operator. `vite.config.ts` pins `environment: 'node'` and no component harness exists anywhere in `tests/`, so this is repo-wide precedent (`tests/signin-surface.test.ts`, `tests/structure.test.ts`) rather than a gap this story introduced. Data integrity is unaffected — `openAuction` re-derives every gate under the lock and refuses independently of what the page rendered — so the exposure is surface correctness on the one screen for an irreversible action. The cheap partial fix, short of a harness, is extracting `blocked` and the availability-sentence choice into a pure helper the way `destinations-view.ts` already splits view logic out of `DestinationsList.svelte`.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-11-open-the-auction.md`
  summary: Rendering the auction-open report reads the entire `auction_events` log and folds it three times, on every page load.
  evidence: bmad-build review (2026-08-25), Blind Hunter layer. `readAuctionOpenReport` calls `loadEventsViaClient` (one unbounded `select * from auction_events order by seq asc`) and folds phase, promotion and eligibility over the result to render three small facts. Harmless as shipped — the gate runs only in Setup, where the log holds at most a handful of import events — but it is the same unbounded full-log read 1.9 already logged against `loadEventsViaClient`, now on a render path rather than a write path. The fix is shared with that entry: a bounded or snapshot-based read when a story first folds the log outside Setup.

## Deferred from: code review of spec-2-1-nominate-a-free-agent (2026-08-25)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-nominate-a-free-agent.md`
  summary: AC6's surface guarantees are verified by matching `.svelte` source text, never by rendering the component.
  evidence: bmad-code-review (2026-08-25), Verification Gap layer, with a worked demonstration: changing `src/routes/nominate/+page.svelte:75` from `!pool.slotAvailable || chosen === null || !confirmed` to drop the `!confirmed` term enables the submit for an unconfirmed selection, and every assertion in `tests/routes/nominate.test.ts` still passes — they are `readFileSync` + `toContain` on the source and none reads the derivation's operators. `vite.config.ts` pins `environment: 'node'` and no component harness exists, so this is the identical gap already logged for `/auction-open` under story 1.11, not one this story introduced. Data integrity is unaffected: the server action independently refuses an unconfirmed submit (`+page.server.ts:90`), so no bad write can ship. The remedy is the one already proposed for the sibling entry — extract `blocked` into a pure `(slotAvailable, chosen, confirmed) => boolean` helper a unit test can call directly, as `destinations-view.ts` already does for `DestinationsList.svelte`.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-nominate-a-free-agent.md`
  summary: `leagueClockExpiry` and `leagueClockReducer` still have no production consumer — AC4 is satisfied by unit test only.
  evidence: bmad-code-review (2026-08-25), Acceptance Auditor layer. A grep over `src/` finds `leagueClockExpiry`, `leagueClockReducer` and `INITIAL_LEAGUE_CLOCK` referenced only inside `src/lib/core/projection/league-clock.ts` and from tests; nothing in `src/lib/server` or `src/routes` folds the clock or reads the expiry. This story added the reset case the League Clock was missing, so the fold is now complete and correct — but the reset changes no observable product behaviour yet, which means the Intent's stated problem ("the phase would expire 48 hours after the open no matter what the league did") is fixed in the log and not yet in anything a Manager can see. This directly continues the same entry logged under story 1.11 and remains consistent with AD-5: the first genuine reader is the Epic 3 close sweep. Flagged rather than built because a consumer written now would still have no caller to prove it right.
  resolved: 2026-09-01 by Story 3.7. `src/lib/server/phase-end.ts`'s `evaluateLeagueClock` is the consumer: it folds `leagueClockReducer` over one `loadEventsViaClient` read inside a `runTransactionalWrite`, asks `leagueClockExpiry` for the absolute instant, and hands it to the pure `decidePhaseEnd` (`src/lib/core/rules/phase-end.ts`) alongside the database's transaction-start clock. `src/lib/server/sweep.ts` calls it once per tick pass, after the closes and before the drain, and reports what it did on the heartbeat — so `LEAGUE_CLOCK` reaches a running clock rather than a test. What a Manager can see: the phase folds to Contract Assignment, every still-unbid nomination is terminated and its Nomination Slot freed, the ninth `PLACE_BID_GATES` gate refuses every Bid league-wide, `refuseNomination`'s phase gate becomes reachable for the first time, and `src/routes/+layout.svelte` states the transition in words on every page. The fold also gained the `BidVoided` case AD-22 warned about — resets are now kept by `seq` and the expiry recomputes past a voided one without ever passing the origin — proven by §10 examples 13 and 27 (`tests/examples/example-13-league-clock.test.ts`, `tests/examples/example-27-a-voided-bid-shortens-the-clock.test.ts`) and by `tests/core/league-clock.test.ts`, `tests/core/phase-end.test.ts` and `tests/server/sweep.test.ts`.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-nominate-a-free-agent.md`
  summary: The "Cannot afford" matrix row and AC1's zero-cap-space clause are satisfied by construction rather than demonstration.
  evidence: bmad-code-review (2026-08-25), Acceptance Auditor layer. The only test claiming the row — `tests/core/nomination.test.ts:353`, `'nominates normally for a Team with no cap space — nothing here is arithmetic'` — asserts `Object.keys(readyState())` equals `['phase','nominations','poolPlayer','contractHolderTeamName']`. That is a structural proxy for "no Money reaches the nomination gate" and a defensible one, since a gate that cannot see cap space cannot refuse on it; but no test in `tests/server/nomination.test.ts` places a nomination for a Team stated to have zero cap space, so the behavioural claim in AC1 is inferred from the type's shape rather than exercised. Cheap to close once Epic 2's bid stories build a state fixture carrying Team money.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-nomination-refusals-and-concurrency.md`
  summary: `hermes.md` §7 and §3 carry two stale claims about local Supabase — `supabase/config.toml` is recorded as absent, and Supabase as "unverified — assume not provisioned".
  evidence: bmad-build step-03 verification (2026-08-25). `supabase/config.toml` exists on disk and `npx supabase start` applied all seven migrations cleanly against local Postgres 15.8.1.085, including this story's `20260825000000_open_nominations.sql`. `hermes.md` §7 lists "No `supabase/config.toml`, so `supabase start`, `db push` and `functions serve` cannot run locally. Belongs with Story 1.5" and §3 states the blocker on Story 1.5. Both are now false for the local dev loop. `hermes.md` is unmanaged and hand-editable, so this is a documentation correction rather than a skill-owned artifact change; the hosted dev/prod projects remain genuinely unverified and that half of the note still stands.
  resolved: 2026-09-05 by the §3 rewrite in sprint-change-proposal-2026-09-05.md, which restates the infrastructure section against confirmed fact.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-nomination-refusals-and-concurrency.md`
  summary: The integration suite silently self-skips its entire contents when no local Postgres is reachable, so a story whose central AC lives there can pass CI having never executed it.
  evidence: bmad-build step-03 verification (2026-08-25). `tests/integration/auction-events.test.ts` is guarded by `describe.skipIf(!reachable)`; with Docker absent, `npm test` reported 1168 passed / 13 skipped and exit 0 while AC4's concurrency proof, the real-23505 classification and the migration itself had never run. Once local Postgres was provisioned the same file surfaced a genuine order-dependence defect (the PK race test's committed claim row broke the Slot test's setup), fixed here by an `afterEach`. CI (`.github/workflows/ci.yml`) runs `npm ci`/`npm test`/`npm run check` with no Postgres service, so every integration assertion in the repo is currently unexecuted in CI. Options are a Postgres service container in the workflow or a non-zero-exit guard that fails when the suite skips; assigned to whichever story first depends on an integration-only guarantee.
  unblocked: 2026-09-05 — Story 9.1 creates the first real database this suite can reach. Not a pilot blocker; recommended immediately after, per sprint-change-proposal-2026-09-05.md.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-nomination-refusals-and-concurrency.md`
  summary: `open_nominations` has no deletion path in any shipped code, so once a claim row is written it is permanent until Story 2.3 lands — a Team gets one nomination ever, and a Player is nominatable once ever.
  evidence: bmad-code-review (2026-08-26), Blind Hunter layer. The migration grants `service_role` DELETE precisely so 2.3's `AuctionClosed` and Epic 7.3's override release can clear a claim, but neither exists yet: `src/lib/core/rules/nomination.ts:80-84` states "No close event exists yet — `AuctionClosed` is Story 2.3's", and a grep over `src/` finds no `delete from open_nominations` outside the integration suite's own `afterEach` cleanup. This is by design and explicitly scoped out by the spec's **Never** list ("No release/delete case for a claim row"), so it is not a defect in this story — but it is a live deployment hazard: if this migration reaches the hosted dev or prod project before Story 2.3 ships, the auction exhausts itself permanently after each Team's first nomination and only a manual DELETE can recover it. Assigned to Story 2.3, which must land the release path before any deploy carrying this migration.
  reassigned: 2026-08-26 (bmad-code-review of Story 2.3) — NOT closed by Story 2.3, despite that spec's Design Notes claiming it would be. Story 2.3 ships `releaseNomination` tested but DELIBERATELY UNREGISTERED — its own **Never** list forbids a production call site, because the delete must run inside the transaction appending the close, which is Story 3.4's transaction and not `placeNomination`'s. So no shipped code path deletes a claim row and the hazard stands exactly as described above. What Story 2.3 did change is the shape of the failure: the Slot now frees by the fold, so after a close the log says the Slot is free while the claim row survives, and the Team's next nomination draws a `slot_in_use` refusal off `open_nominations_team_id_key` rather than an `already_nominated` one. Reassigned to Story 3.4, which closes it by registering the deleter on the close transaction's `projections`.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-nomination-refusals-and-concurrency.md`
  summary: `nameTheHolder`'s outer `catch` swallows every failure with no diagnostic trail.
  evidence: bmad-code-review (2026-08-26), Blind Hunter + Edge Case Hunter layers, `src/lib/server/nomination.ts:415-419`. Returning `unrecorded` on any failure is the correct product behaviour — the refusal is already decided and a courtesy re-read must never become a 500 — and that path is now covered by a test added during this review. What is missing is observability: a genuine defect in the re-read (connection exhaustion, a reducer bug) is indistinguishable from a legitimately un-nameable holder. The repo has exactly one logging call today (`src/lib/shell/db.ts:46`) and no logging convention, so adding one here would be inventing the convention inside a refusal path. Deferred to whichever story first establishes server-side logging; the same applies to the absence of any timeout bounding the re-read's `connect()`.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-nomination-refusals-and-concurrency.md`
  summary: The integration suite's skip probe and the code under test read the connection string from different places, so `npm test` fails — rather than skipping — whenever local Postgres is up but `SUPABASE_DB_URL` is unset.
  evidence: Observed on `main` at 62a0a11 (2026-08-26). `tests/integration/auction-events.test.ts:26-27` builds `LOCAL_DB_URL` as `process.env['SUPABASE_DB_URL'] ?? 'postgresql://postgres:***@127.0.0.1:54322/postgres'`, and the literal `***` is accepted by a local `supabase start` Postgres because trust auth never checks the password. So `isLocalPostgresReachable()` returns true and `describe.skipIf(!reachable)` runs the whole suite — including the block at line 601, which goes through `writeGateway()` into `src/lib/shell/db.ts`, and that module reads `SUPABASE_DB_URL` directly and throws `SUPABASE_DB_URL is not set. The transactional write path is unavailable.` Result on a developer machine with Docker running and the variable unexported: `1 failed | 1182 passed`, exit non-zero, in exactly the state the guard exists to skip cleanly. Exporting the variable turns the same tree green at 1183/1183. This is the inverse failure of the entry above (which records the suite skipping when it should run) and is not fixed by that entry's remedies: a Postgres service container in CI leaves the local case untouched, and a fail-on-skip guard would not fire here because the suite does not skip. The fix is to make the probe read the same source the code under test does — skip unless `SUPABASE_DB_URL` is actually set, instead of probing a guessed default whose password is never validated — which also removes a fixture that would silently connect to whatever happens to be listening on 54322. Note the environment-dependence is real but narrow: CI exports the variable (`.github/workflows/ci.yml`) and a machine with no local Postgres at all skips correctly, so only the Docker-up/env-unset combination is red.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-nomination-slot-lifecycle-and-dead-nomination-visibility.md`
  summary: Dead-nomination visibility — FR-10's "unbid for more than 24 hours" flag, its derivation, and the nominating Team's own statement — is split out of Story 2.3, which now covers only the Nomination Slot lifecycle fold.
  evidence: bmad-build step-02 SCOPE STANDARD check (2026-08-26). The drafted spec measured 4,520 tokens against the 1600 ceiling, and the two halves pass the multi-goal test independently: the fold (`AUCTION_CLOSED_EVENT`, the release case in `src/lib/core/projection/nominations.ts:170-185`, `releaseNomination`) and the flag (a new `src/lib/core/instant.ts` extracted from `src/lib/core/projection/league-clock.ts:142-270`, a new `unbid.ts`, and the word-and-shape treatment on `src/routes/nominate/+page.svelte:93-106`) could each merge as its own PR without breaking the other. The flag reads a nomination's `occurredAt`, which exists today (`src/lib/core/projection/nominations.ts:57`) and is unaffected by whether the release case is built. The fold was taken first because it unblocks Epic 3 and closes the claim-deletion hazard logged at `deferred-work.md:216`. Carries with it: AC2 and AC3 of `epics.md:838-846` (epics.md's own numbering, unrelated to the local AC1/AC2/AC3 scheme the Story 2.3 spec and its tests use), the `core/instant.ts` extraction, and the deferred public-board half below.
  status: open
  escalated: The "word and a shape" treatment this story owes is in DIRECT conflict with `DESIGN.md`, which is `status: final`. `epics.md:840` requires the unbid flag to carry "a **word and a shape**, never colour alone" (deriving from FR-10, `prd.md:263`), but `DESIGN.md:194` specifies the State chip as "Plain `text-secondary` label, **no chip**, for ambient states (**Open**, **Awaiting bid**)" — a plain label is a word with no shape, and Awaiting Opening Bid is named there as exactly such an ambient state. `DESIGN.md:191` further reserves `attention` for "Outbid and nothing else in the entire system", closing off the obvious chip token, while `EXPERIENCE.md:153` independently requires the state to show "hours unbid" and `:159` insists the surfacing "should sting a little". Per the hierarchy in `hermes.md` §2 the AC wins (PRD/epics outrank the UX documents, and the spine outranks both), but the resolution is a real design decision and must not be taken silently by the implementer: it changes the one component `DESIGN.md` says has no chip. Note the mockup at `mockups/Board.dc.html:101-104` already shows a THIRD answer — an outlined chip reading "Your nomination" in `#5E7568`/`#93A69A`, which is neither `DESIGN.md`'s no-chip ambient label nor an `attention` treatment. Route this to `bmad-correct-course` to amend `DESIGN.md` at source, or resolve it at the checkpoint of the visibility story before any markup is written. Detected during bmad-build step-02 investigation of Story 2.3 (2026-08-26); not reached by that story because the visibility half was split out.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-nomination-slot-lifecycle-and-dead-nomination-visibility.md`
  summary: The PUBLIC half of FR-10 — the Bid Board showing the nominating Team and the hours unbid to the whole league — belongs to Story 4.3, not to Story 2.3.
  evidence: bmad-build step-02 investigation (2026-08-26). `epics.md:846` requires "the public board shows the nominating Team and the hours unbid", but no Bid Board exists: a walk of `src/routes/` finds no surface listing open Auctions, and `epics.md:1457` assigns "View the Bid Board" to Story 4.3 with its own card anatomy at `epics.md:1468`. Building a throwaway board in 2.3 is work 4.3 deletes. `EXPERIENCE.md:153` and the mockup at `mockups/Board.dc.html:101-104` already specify the board-card treatment ("No opening bid" in `text-tertiary`, an outlined chip, "31h unbid"), so 4.3 has the design in hand and needs only the derivation the split-out visibility story will provide.
  status: open

## Deferred from: code review of spec-2-3-nomination-slot-lifecycle-and-dead-nomination-visibility (2026-08-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-nomination-slot-lifecycle-and-dead-nomination-visibility.md`
  summary: The escalated `DESIGN.md` / `epics.md` conflict over the unbid flag's "word and a shape" is filed as prose with no enforceable follow-up, so the split-out visibility story can still silently pick one of three conflicting treatments.
  evidence: bmad-code-review (2026-08-26), Blind Hunter layer, confirmed against source. The `escalated:` field on the visibility entry above states the conflict correctly and names the resolution path ("Route this to `bmad-correct-course` to amend `DESIGN.md` at source, or resolve it at the checkpoint of the visibility story before any markup is written"), but nothing in the Story 2.3 diff links a task, amends `DESIGN.md`, or adds an acceptance criterion that would block the follow-on story from choosing unilaterally. The three live answers remain: `DESIGN.md:194`'s no-chip ambient label, the `attention` token `DESIGN.md:191` reserves for Outbid alone, and the outlined "Your nomination" chip already drawn at `mockups/Board.dc.html:101-104`. Deferred rather than patched because resolving it is a design decision over a `status: final` document, which is `bmad-correct-course`'s job and not a code review's — but it must be closed before the visibility story writes markup, not during it.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-nomination-slot-lifecycle-and-dead-nomination-visibility.md`
  summary: `src/lib/core/rules/nomination.ts:81-84` states a premise Story 2.3 falsified — "No close event exists yet — `AuctionClosed` is Story 2.3's" — while its conclusion, that the "already won" refusal is unreachable, still holds.
  evidence: bmad-code-review (2026-08-26), Acceptance Auditor layer, confirmed against source. Story 2.3 added `export const AUCTION_CLOSED_EVENT = 'AuctionClosed'` (`src/lib/core/projection/nominations.ts:73`) and the fold case reading it (`:252-268`), so a close event now demonstrably exists as a type the core understands. The refusal remains genuinely unreachable because nothing appends one — Story 2.3's **Never** list forbids a producer and Epic 3 owns closing — so the comment's conclusion is still correct and only its stated reason is dated. Not fixed in Story 2.3 because that spec's Code Map ("Nothing here touches `core/rules/nomination.ts`") and Verification ("`git diff` touches nothing under `src/lib/core/rules/`") both forbid the file, and amending a Tier A story's boundary for a comment is not a worthwhile trade.
  deferred_reason: Story 3.4 must revisit that exact sentence anyway when a close becomes reachable, and loosening a Tier A story boundary for a comment is a poor trade.
  resolved: 2026-08-29 by Story 3.4. The sentence is rewritten at `src/lib/core/rules/nomination.ts` and its conclusion is replaced rather than merely re-dated: a close is now reachable — `src/lib/server/close.ts`'s `closeAuction` appends the first `AuctionClosed` this codebase has ever produced — so "already won in this auction" is no longer an unreachable state. It is refused as `under_contract`, which needed no new `NominationRefusal` case and no new wording: `src/lib/server/nomination.ts` resolves `contractHolderTeamName` from the `team_rosters` join OR the `AuctionContracts` fold's own `teamName`, and the gate reads one field with no notion of which source answered. The roster wins a tie. Proven in `tests/core/nomination.test.ts` (the fold composing into the refusal, with no database) and `tests/server/nomination.test.ts` (a won Player with no roster row refused at the submit and greyed out on the pool page with the identical sentence).
  status: resolved

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-the-auction-page.md`
  summary: Story 2.4's bid control — the 46px input pre-filled with the minimum legal Bid, its adjacent submit button, the two-part confirm, and the per-cause disabled wording ("already leading" / "no money" / "no roster slot") — is deferred to Story 2.5, which builds the rules it renders.
  evidence: bmad-build step-01 (2026-08-26), scope split agreed at the routing checkpoint. `epics.md:873-883` states both the pre-fill and the disabled wording as Story 2.4 acceptance criteria, but every figure they display is owned elsewhere: the minimum legal Bid is `current high + $500,000` from Story 2.5 (`epics.md:914`), and the three disabled causes are 2.5's self-bid refusal (`epics.md:923-925`), 2.6's money gate and 2.7's capacity gate. Investigation confirmed none of it exists — there is no `src/lib/core/rules/bidding`, no `BidPlaced` event and no Auction entity in `src/lib/core/types.ts` (its own header comment at `:7-13` names them as Epic 2 work still to come), and no Bid-related code anywhere in `src/` beyond CSS token names and one static caption at `src/routes/+page.svelte:46`. `epic-2-context.md:57` states the ordering directly: "2.4's bid control depends on 2.5 for its minimum-legal-Bid pre-fill and on 2.6/2.7's gate outcomes for its disabled reasons — build the rules before the surface, or the page invents figures." Building it in 2.4 would have meant a rendered number with no rule behind it, which is the precise failure the refusal design exists to prevent.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-the-auction-page.md`
  summary: The Auction page's metadata line ships as `NBA · POS` only; the existing-salary and contract-length halves of `epics.md:864`'s `NBA · POS · $0.0M · Nyr` are deferred until a story imports those columns for Free Agents.
  evidence: bmad-build step-02 (2026-08-26), intent gap resolved at the planning checkpoint. `supabase/migrations/20260824020000_live_reference_tables.sql:94-125` defines `free_agent_players` with exactly `fantrax_player_id`, `player_name`, `positions`, `nba_team` and `minor_league_eligible` — there is no salary and no contract column on it. `cap_hit` and `contract_years_remaining` exist only on `team_rosters` (`:55`, `:59`), which holds rostered players, not the Free Agent pool. The mirroring type `ParsedPoolRow` (`src/lib/core/types.ts:154-159`) carries the same four fields and no more. Rendering `$0.0M · Nyr` would therefore require either a fabricated figure or a migration and import path that no story in Epic 2 owns. Deferred rather than escalated because the two omitted fields are informational, and `open action before setup day` in `hermes.md:§9` already flags that the real Fantrax export's salary columns are unconfirmed against the adapter mapping.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-the-auction-page.md`
  summary: The Auction page shows the nomination's own timestamp twice (relative and absolute); `epics.md:863,866`'s "time remaining" and "closes 2:14 AM Wed" close-time rendering is deferred to the story that creates a close timestamp.
  evidence: bmad-build step-02 (2026-08-26), intent gap resolved at the planning checkpoint. An Auction Clock is set only by an accepted Bid — "exactly 24 hours from that Bid's timestamp" (`epics.md:929`) — which is Story 2.5, and the absolute close timestamp it persists (`epics.md:931`) has no column, no event and no producer today. Nothing in the fold carries an expiry: `OpenNomination` (`src/lib/core/projection/nominations.ts:76-87`) holds `occurredAt` and nothing else time-shaped. The AC's structural requirement — that time appear twice, relative and absolute in the viewer's timezone, with the absolute never dropped to save space — is honoured in 2.4 against the nomination instant, so the pattern and its pure helper (`src/lib/core/instant.ts`) exist for 2.5 to point at a close instead. Deliberately not solved by rendering a placeholder countdown: the **Never** list forbids countdown pressure, and a clock counting to nothing is worse than no clock.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-the-auction-page.md`
  summary: Current price, Leading Bidder and the chronological bid history render as an explicit empty state in Story 2.4; the populated versions arrive with Story 2.5's `BidPlaced` event.
  evidence: bmad-build step-02 (2026-08-26), intent gap resolved at the planning checkpoint. `epics.md:863,868-871` require current price, Leading Bidder, and every Bid listed chronologically with Team, acting Manager, amount and timestamp, visible to all with no anonymity. No `BidPlaced` event exists to fold — `src/lib/core/types.ts:7-13` states outright that domain event variants including `PlaceBid` are Epic 2's still to add, and `nominationsReducer` (`src/lib/core/projection/nominations.ts:248-280`) handles exactly two event types, neither of them a bid. The page therefore renders the honest facts — a stated no-bids price and a history region carrying an explicit "no bids" sentence rather than an empty box — which is also the correct permanent rendering for a freshly nominated Player once 2.5 lands. The no-anonymity rule is satisfied vacuously here and must be re-asserted as a live claim by the story that first renders a real bid row.
  status: open

## Deferred from: code review of spec-2-4-the-auction-page (2026-08-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-the-auction-page.md`
  summary: The sprint tracking key for Story 2.4 still reads `2-4-the-auction-page-and-its-bid-control` after the bid control was split out to Story 2.5.
  evidence: bmad-code-review (2026-08-26), Blind Hunter layer, confirmed against source. `_bmad-output/implementation-artifacts/sprint-status.yaml:56` carries the un-renamed key, while the story's Intent and four `deferred-work.md` entries all place the bid control in Story 2.5. Anyone reading `sprint-status.yaml` alone would believe 2.4 still covers it.
  deferred_reason: The key is generated from `epics.md` by sprint planning, not hand-owned by this story; renaming it here would desynchronise it from its generator.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-the-auction-page.md`
  summary: The Auction page's relative phrase never refreshes — `nowIso` is read once at render time, so "moments ago" silently ages on a tab left open.
  evidence: bmad-code-review (2026-08-26), Blind Hunter layer, confirmed against source. `src/routes/auction/[fantraxPlayerId]/+page.svelte:46` derives `nowIso` from a single `new Date().toISOString()` with no interval, no `invalidate` and no revalidation. The staleness is bounded in practice because the absolute stamp renders beside it and never goes stale.
  deferred_reason: No AC requires the phrase to advance, and the **Never** list forbids countdown pressure — a self-advancing clock is closer to that than to the honest snapshot this story asks for. Revisit when Story 2.5's Auction Clock makes elapsed time load-bearing.
  status: open

## Deferred from: code review of spec-2-5-place-a-bid (2026-08-27)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-place-a-bid.md`
  summary: The `::text` cast that keeps a malformed historical `managerId` from 500ing the Auction page has no test able to observe its failure mode, because the only harness that could express a Postgres cast error is the integration suite that self-skips.
  evidence: bmad-build step-04 (2026-08-27), Verification Gap layer, confirmed against source. `src/lib/server/auction-page.ts` resolves bidder names with `select m.id::text ... where m.id::text = any($1::text[])`, and the comment above it states the deliberate contract: a non-uuid id must produce a missing NAME, not a failed query. That guarantee is reachable — `readPayload` in `src/lib/core/projection/auctions.ts` validates `managerId` only as a non-empty string, so a malformed `BidPlaced` payload folds successfully and flows into the query. But the fake gateway in `tests/server/auction-page.test.ts` answers the statement by regex-matching its text and filtering in plain JavaScript, which cannot distinguish `::text = any($1::text[])` from `= any($1::uuid[])`; every test `managerId` (`m-1`, `m-gone`, ...) is already a non-uuid string and none of them discriminates. Reverting the cast would therefore break the page for a corrupt log row while the whole suite stayed green.
  deferred_reason: Closing it requires a real Postgres connection, and the one integration suite is blocked by the pre-existing probe/env connection-string mismatch already logged above — the `SUPABASE_DB_URL is not set` case that is this repository's single standing test failure. Writing an integration case now would add a second permanently-skipped test rather than a passing one. Revisit with that harness.
  status: open

## Deferred from: spec-2-6-the-money-gate-and-the-refusal-panel (2026-08-27)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-the-money-gate-and-the-refusal-panel.md`
  summary: A bid on a Minor League Eligible Player is judged by the ordinary Maximum Bid formula, so an eligible bid a Free Minor League Slot could absorb is refused on money it should not be bounded by. Story 2.8 owns the unbounded branch that closes it.
  evidence: bmad-build step-03 (2026-08-27), a scope boundary the epic set rather than a defect found. `epic-2-context.md` assigns the split directly -- "2.6 owns the money gate and refusal panel, 2.7 the capacity gate and the always-both-gates report, 2.8 the eligible-player exposure feeding `committedBids` back into 2.6" -- and the epic AC for 2.6 qualifies its formula as "`Maximum Bid` = `Available Cap Space` - `Roster Reserve` **for a non-eligible Player**". FR-35 and PRD §10 examples 18-22 and 25 are the eligible branch, and every one of them is listed against Story 2.8. The half that IS implementable today is done and is not deferred: `teamMoneyStateFor` (`src/lib/core/rules/bidding.ts`) already excludes an eligible leading Bid from Committed Bids per FR-14, and `CapGateOutcome.minorsExposure` is a named term carried at zero so the breakdown sums -- so 2.8 supplies the set it sums over and the unbounded branch without rewriting this arithmetic. The gap is unreachable in production before 2.8 lands, because both stories are inside Epic 2 and the Auction does not open to Managers until the epic is complete.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-the-money-gate-and-the-refusal-panel.md`
  summary: Committed Bids counts only the LEADER of a `minimum_bid` contention, not every Contender, so a non-eligible lottery a Team is contending in without leading commits nothing against its cap.
  evidence: bmad-build step-03 (2026-08-27), a seam this story could not close. FR-14 requires "$1,000,000 for every open Minimum-Bid Contention on a Player who is not Minor League Eligible in which it is a Contender, because any Contender may win". `teamMoneyStateFor` implements the figure -- a `minimum_bid` auction contributes `MINIMUM_BID` rather than the leading amount, asserted in `tests/core/bidding.test.ts` -- but it can only see the leader, because `Auction` (`src/lib/core/projection/auctions.ts`) carries `leadingBid` and a chronological `bids` list and no Contender list. There is no Contender list to fold: Story 3.2 "Enter and join a Minimum-Bid Contention" creates one, and this story's Never list forbids producing a Minimum-Bid Contention at all -- `minimum_bid` is reachable today only as a state literal. Story 3.2 must extend the one expression in `teamMoneyStateFor` when it lands.
  status: open

## Deferred from: spec-2-7-the-slots-gate (2026-08-27)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-7-the-slots-gate.md`
  summary: The `slots` gate counts Projected Active/Bench Additions as `leading.length + 1` unconditionally, so a Team at Roster Count 12 is refused on capacity even when it bids on a Minor League Eligible Player a Free Minor League Slot would absorb. Story 2.8 owns the eligible branch that closes it.
  evidence: bmad-build step-03 (2026-08-27), the same scope boundary Story 2.6 already logged above, reaching the capacity gate from the other side. FR-37 states that a full Team may still bid on a Minor League Eligible Player because that win adds nothing to Active/Bench, and PRD §10 example 25 is the executable form of it -- `N+1 <= M` gives Projected Active/Bench Additions 0 and capacity `12 + 0 = 12 <= 12`, permitted, until the fourth eligible lead produces Overflow Count 1 and `13 > 12`. Neither figure exists to branch on: `TeamMoneyState` (`src/lib/core/rules/bidding.ts`) carries `capSpace`, `rosterCount` and the non-eligible `leading` list and nothing else, so there are no Free Minor League Slots and no current-Player eligibility flag in the gate's reach. `epic-2-context.md` assigns the split -- "2.7 the capacity gate and the always-both-gates report, 2.8 the eligible-player exposure" -- and §10 examples 18-22 and 25 are all listed against 2.8. `projectedAdditionsFor` is the single expression both gates read, so 2.8 extends one function rather than two. The gap is unreachable in production before 2.8 lands: both stories sit inside Epic 2 and the Auction does not open to Managers until the epic is complete.
  status: open

## Deferred from: spec-2-8-minors-exposure (2026-08-27)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-8-minors-exposure.md`
  summary: PRD §10 examples 19 and 20 cannot reach their stated verdicts until Epic 3 builds Minimum-Bid Contention, because both bid exactly $1,000,000 as an OPENING Bid and Story 2.5's `opening` gate refuses that amount by name. The exposure arithmetic they exist to prove is fully implemented and asserted; only the verdicts are blocked.
  evidence: bmad-build step-03 (2026-08-27), a cross-story seam rather than a defect. Example 19 bids $1,000,000 on a second eligible Player with nothing leading it, and example 20 re-bids the same amount on that Player after the $30,000,000 Auction closes — both openings. PRD §3 makes an Opening Bid of exactly `MINIMUM_BID` the thing that opens a Minimum-Bid Contention, so `evaluateOpening` (`src/lib/core/rules/bidding.ts`) returns `at_the_minimum` and refuses, and `minimumLegalBid` reports $1,500,000 as the smallest legal opening. Story 2.8's Never list forbids producing a Minimum-Bid Contention, and the Contender list, seed table, fixed clock and draw are Stories 3.2/3.3. So example 19's "refused on cap ALONE" is really `['opening', 'cap']` at the literal amount, and example 20's "permitted" is refused on `opening`. Neither test papers over it: `tests/examples/example-19-overflow-refuses-the-cheap-bid.test.ts` asserts N=2, M=1, Overflow 1 and Minors Exposure $30,000,000 at the literal $1,000,000, pins `failedGates` there as `['opening', 'cap']`, and proves the sole-ground claim at $1,500,000 — where every figure the example states is identical, because the exposure is the OTHER Auction's. `tests/examples/example-20-a-resolved-win-stops-being-exposure.test.ts` asserts `cap` and `slots` both pass exactly as the example says and that `opening` is the only refusal left.
  deferred_reason: Closing it requires Minimum-Bid Contention to exist, which Epic 3 owns and this story's Never list forbids. Story 3.2 or 3.3 should re-run both examples at the literal $1,000,000 and assert the accepted/sole-ground verdicts the PRD states.
  resolved: 2026-08-28 by Story 3.2, and confirmed while building Story 3.3. `evaluateOpening` returns `at_the_minimum` and PASSES it (`src/lib/core/rules/bidding.ts`), because that amount now opens a Minimum-Bid Contention for real — so both examples reach their stated verdicts at the literal $1,000,000, and both tests assert them there rather than at a substitute amount. `tests/examples/example-19-overflow-refuses-the-cheap-bid.test.ts` asserts `failedGates(evaluate(STATE, bidOf(1_000_000), NOW))` is `['cap']`, which is the example's "refused on cap ALONE"; `tests/examples/example-20-a-resolved-win-stops-being-exposure.test.ts` asserts `failedGates` is `[]` and `gates.opening.opening` is `at_the_minimum`, which is the example's "permitted". The sole-ground claim at $1,500,000 is kept beside each as the second reading, because every figure the examples state is identical there — the exposure is the OTHER Auction's. Nothing about the exposure arithmetic changed; only the verdicts were ever blocked.
  status: resolved

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-expiry-is-authoritative.md`
  summary: The Auction page's reactive client logic is verified by matching raw source text, never by executing a rendered component.
  evidence: `vite.config.ts` runs the suite under `environment: 'node'` and no `.svelte` file can be rendered, so `tests/routes/auction-page.test.ts` asserts against the file's own text. Story 3.1 added the page's first genuinely time-varying behaviour — the one-second tick, the re-anchor on a fresh server instant, and the control disabling itself as the clock runs out — so a reactivity bug that preserved the asserted source tokens would ship green. Closing it needs a jsdom or browser environment with fake timers, which is infrastructure work no story owns yet.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-expiry-is-authoritative.md`
  summary: A malformed `closesAt` in the insert-only log makes that Auction refuse every future Bid permanently, and no story yet names the recovery path.
  evidence: `hasExpired` reads an unreadable close instant as expired — the deliberate fail-closed direction, matching `readPayload`'s own fallback — and AD-4 forbids correcting a `BidPlaced` payload in place. Story 7.2 (Void a Bid and restore the Auction) is the only mechanism that could recover such an Auction; it should cover this case explicitly rather than by accident.

## Deferred from: code review of spec-3-2-minimum-bid-contention (2026-08-28)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-minimum-bid-contention.md`
  summary: `auction_contention_seeds` keys on `fantrax_player_id` alone, so a second contention opened on the same Player would abort the whole transaction rather than write a second seed.
  evidence: bmad-code-review (2026-08-28), blind-hunter layer. The migration's primary key is `fantrax_player_id text primary key` and carries no Auction or contention instance component, and `recordContentionSeed` in `src/lib/server/bidding.ts` inserts with no `on conflict` clause — so a duplicate key raises and rolls back the accepted Bid alongside it. This is unreachable today: a lottery cannot convert (3.2 refuses `$1,500,000` on `contention` by name) and a `$1,000,000` Bid onto a standard Auction is never accepted, so no Player can reach a second opening. It becomes reachable the moment a story lets an Auction re-open on the same Player — dissolution and re-nomination (3.3), or void and restore (Epic 7.2). The fix is a decision that belongs with whichever story creates the second opening: widen the key, or make the insert idempotent.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-minimum-bid-contention.md`
  summary: A live Minimum-Bid Contention whose `seedHash` folds to `null` renders no commitment block and no statement that the commitment is missing.
  evidence: bmad-code-review (2026-08-28), edge-case-hunter layer. `src/routes/auction/[fantraxPlayerId]/+page.svelte:669` guards the commitment on `{#if auction.seedHash !== null}` with no `{:else}`, so a lottery with no readable commitment is visually identical to one whose commitment simply did not render. `decide()` throws when an opening at `MINIMUM_BID` arrives with a `null` seed, and `readPayload` nulls a malformed `seedHash` rather than throwing, so the state is reachable only from a corrupt or hand-written log — which AD-4 forbids correcting in place. AD-14's trust guarantee rests on a Manager being able to see the commitment, so the absence of one is exactly the case worth naming out loud. Story 3.6 builds the reveal and the verification surface and should decide what an uncommitted lottery says for itself.
  resolved: 2026-08-31 by Story 3.6. The live-contention block now carries an `{:else}` printing the core's existing `SEED_COMMITMENT_UNVERIFIABLE` at `id="auction-seed-unverifiable"` — the same sentence the dissolution block already prints for the same absence, so the two surfaces cannot word one state two ways — and the block links `/verify` beside it. One sentence or the other, never both and never neither. Asserted in `tests/routes/auction-page.test.ts` ("states the commitment's ABSENCE out loud rather than rendering nothing"), which checks the `{:else}` sits inside the LIVE half of the panel and that the page still words neither sentence itself.
  status: resolved

## Deferred from: code review of spec-3-3-dissolve-a-minimum-bid-contention (2026-08-29)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-dissolve-a-minimum-bid-contention.md`
  summary: The corrupt-log path where a live contention's `seedHash` folded to `null` is proven at the core level only; no server-transaction test drives `placeBid` end to end through a dissolution against a null commitment.
  evidence: bmad-code-review (2026-08-29), blind-hunter layer. `decide()`'s "reveal against a commitment that folded to null rather than stranding the Auction" branch is asserted in `tests/core/bidding.test.ts`, and `readContentionSeed`'s null answer is asserted in `tests/server/bidding.test.ts`, but nothing joins the two: no test opens a contention whose `seedHash` is absent, dissolves it through `placeBid`, and confirms the transaction commits and `loadAuctionPage` then serialises what the page renders as the unverifiable-commitment sentence. The state is reachable only from a corrupt or hand-written log, which AD-4 forbids correcting in place, so the gap is narrow — but Story 3.6 builds the reveal-verification surface and is where an end-to-end assertion about an unverifiable commitment belongs.
  resolved: 2026-08-31 by Story 3.6. `tests/server/bidding.test.ts` — "dissolves a contention with NO commitment, commits, and the page says so" — opens a lottery whose `BidPlaced` carries no `seedHash` key at all, dissolves it through the real `placeBid` against a sealed seed row, asserts the transaction COMMITS rather than rolling back, and then hands the log that transaction produced to the real `loadAuctionPage`. The view comes back with `seed` set and `seedHash` null, and the test resolves the page's own `seedHash === null ? SEED_COMMITMENT_UNVERIFIABLE : SEED_REVEALED` expression against it.
  status: resolved

## Deferred from: code review of spec-3-4-close-an-auction-and-place-the-player (2026-08-31)

- **`closedWinnerFor` does not validate the `ClosedWinner` it is handed.** `src/lib/core/rules/close.ts` reads `winner.teamId`, `winner.teamName` and `winner.managerId` straight onto the `AuctionClosed` payload with no check that they are non-empty. `auction_events.manager_id`/`team_id` are `not null` and reference real rows, so a malformed drawn winner would fail at the FK rather than at the rule that could name the problem. Unreachable today: no drawer exists and `closeAuction` passes `null`.
  owner: Story 3.6 (draw a Minimum-Bid Contention winner) — validate where the winner is constructed, or add the guard to `closedWinnerFor`.
  resolved: 2026-08-31 by Story 3.6, in BOTH places the entry offered. `closedWinnerFor`'s `minimum_bid` branch now refuses an empty `teamId`, `teamName` or `managerId` with a `TypeError` naming which field was empty, before the payload is built — `tests/core/close.test.ts`'s "the winner it is handed is validated" drives all three. `drawnWinnerFor` in the new `src/lib/core/rules/draw.ts` makes the same check on the Contender it selects, so the producer refuses to construct one and the consumer refuses to accept one.
  status: resolved

- **The spec verification command `git diff --stat <read-only paths>` under-covers the authorised file set.** As written in Story 3.4's Verification section it guards only `src/routes/`, `src/lib/components/`, `src/lib/shell/` and `src/lib/core/rules/bidding.ts`. An inadvertent edit to `src/lib/core/types.ts` or `src/lib/core/rules/roster-import.ts` — both declared read-only by the Code Map — would pass the check silently. Verified by hand at review time that neither was touched, so nothing was breached in 3.4.
  owner: future story specs — prefer an allow-list check over a deny-list of four paths.
  status: open

## Deferred from: spec-3-5-the-tick-sweep-then-drain (2026-08-31)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-the-tick-sweep-then-drain.md`
  summary: The tick sweeps and closes with **no pause check** — a paused auction's Auctions would still be closed by `runTick` — because AD-13's pause does not exist to read yet.
  evidence: spec-3-5's **Never** list states it directly ("No pause check — Epic 7 owns pause, and no pause event or flag exists to read, so a check written now would be unverifiable"), and the codebase agrees: `grep -rn "pause" src/` finds no pause event type, no `paused` column on any table in `supabase/migrations/`, and no phase or flag `src/lib/core/projection/` folds that could answer "is the auction paused". AD-13 requires a pause to recompute close instants FORWARD rather than shift them in place, which means the gate the sweep would consult is not merely a boolean — it is whatever Epic 7 decides the resumed clock is, and inventing one now would be a second, unverifiable definition of it. `src/lib/server/sweep.ts` is deliberately the one place it will go: the check belongs between the version gate and the overdue derivation, before any close.
  deferred_reason: Epic 7 owns pause (AD-13). A check written against a flag that does not exist could not be tested and would have to be rewritten by the story that creates the real one.
  owner: Epic 7's pause story — add the check to `runTick` and a heartbeat outcome for a paused pass.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-the-tick-sweep-then-drain.md`
  summary: `tick_heartbeats` records every pass, but **nothing reads it** — a tick that stops running entirely writes no row and there is no external detector to notice the silence.
  evidence: spec-3-5's **Never** list ("No external heartbeat detector (8.2)"). AD-19's whole reason for the heartbeat is that "a pass that recorded nothing is indistinguishable from a dead tick", and Story 3.5 supplies the recording half only: `supabase/migrations/20260831000000_tick.sql` creates the table and the index on `ran_at desc`, and `src/lib/server/sweep.ts` writes exactly one row per pass on every path including a refusal and a failed pass. What is missing is the reader — something outside this database that alerts when the newest `ran_at` is older than a threshold. It cannot live in the tick itself: a tick that is not running cannot notice that it is not running, and a pg_cron job that checked would be the same single point of failure wearing a second hat.
  deferred_reason: Story 8.2 owns operational alerting. The detector must be external to the mechanism it watches, which is infrastructure this repository does not yet configure.
  owner: Story 8.2 — read `tick_heartbeats` from outside and alert on staleness.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-the-tick-sweep-then-drain.md`
  summary: Two tick passes can overlap, and the second reports benign contention as close FAILURES on the heartbeat — correctness holds, but AD-19's record becomes noisy.
  evidence: bmad-build review (2026-08-31), Blind Hunter and Acceptance Auditor layers, independently. The cron job POSTs via `net.http_post`, which is fire-and-forget: `timeout_milliseconds` abandons the response and cannot stop an Edge Function still sweeping, so the 10-second schedule fires again regardless. `src/lib/server/sweep.ts` states by design that it takes no lock across the pass. Two passes therefore re-derive the same overdue set; the per-close `GLOBAL_WRITE_LOCK_KEY` serialises them so nothing is closed twice — correctness is preserved — but the loser's `loadCloseState` finds the Auction already gone from `auctions.byPlayer`, `closedWinnerFor` throws on the null Auction, and the sweep records it in `failures` with `outcome: 'completed_with_failures'`. A monitoring surface built on that column would treat routine overlap as a fault. Not fixed here because both clean fixes are out of bounds: a second, pass-level advisory lock contradicts the spec's frozen "the lock is taken by `runTransactionalWrite` per close and by no second mechanism", and discriminating the benign case by matching a thrown `TypeError`'s message text would be brittle.
  owner: whichever story first reads `tick_heartbeats` for alerting (8.2), or Epic 7's pause story if it introduces a pass-level gate anyway.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-the-tick-sweep-then-drain.md`
  summary: `tick_heartbeats` grows by roughly 259,000 rows a month forever, with no retention, partitioning or cleanup — and `cron.job_run_details` grows alongside it on the same schedule.
  evidence: bmad-build review (2026-08-31), Blind Hunter layer. The migration `supabase/migrations/20260831000000_tick.sql` does the invocation arithmetic itself (~259,200/month at a 10-second interval, AD-10's own figure) and creates the table with an index on `ran_at desc` but no TTL, no partitioning and no pruning job. pg_cron's own `cron.job_run_details` has the identical unbounded-growth characteristic and is likewise unaddressed. On Supabase's free tier the database size ceiling is a silent-outage source of exactly the kind AD-19 says must be alerted on well before the ceiling.
  owner: Story 8.2, alongside the external detector that reads this table.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-the-tick-sweep-then-drain.md`
  summary: The heartbeat stores counts plus one prose sentence, so nothing can answer "which Auctions failed" without parsing English.
  evidence: bmad-build review (2026-08-31), Blind Hunter layer. `tick_heartbeats` carries `closed`, `skipped` and `failed` as integers; the Player ids exist only inside the free-text `detail` column, assembled by `detailFor` in `src/lib/server/sweep.ts` as `closed 2 (p-1, p-2); skipped 1 (p-9) — ...`. The deferred external detector (AD-19, Story 8.2) is meant to read this table and alert; it will have no structured way to report which Auction failed, or to distinguish a persistent per-Auction failure from a transient one, without regex over a sentence whose wording is not a contract.
  owner: Story 8.2 — decide the shape it needs (a jsonb column, or a child row per failure) when the reader exists to justify it.
  status: open

## Deferred from: spec-3-6-draw-a-minimum-bid-contention-winner (2026-08-31)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-6-draw-a-minimum-bid-contention-winner.md`
  summary: A closed Auction still 404s. The three facts a draw records — the seed, the ordered Contender list and the selection — are durable and readable through `drawsReducer`, but **no surface renders them**.
  evidence: spec-3-6's **Never** list states it outright ("No closed-Auction page. The Auction route still 404s on a closed Auction; EXPERIENCE.md:156's Closed state — winner, amount, placement, and the lottery's seed and list — is Epic 4's"). `src/routes/auction/[fantraxPlayerId]/+page.server.ts` raises `error(404, ...)` on a null read, and a close removes the Player from both `auctionsReducer` and `nominationsReducer`, so a closed Auction has no page at all. This story's obligation was to make the three facts DURABLE, and it discharged that: `src/lib/core/projection/draws.ts` folds `ContentionDrawn` and has no removal case, `tests/projection-draws.test.ts` asserts the draw survives the close that deletes the Auction, and `/verify` states the procedure a Manager applies to the values. What is missing is the surface that shows them. `drawForPlayer` currently has no caller outside the test suite, which is the visible shape of the gap.
  deferred_reason: Epic 4 owns the Closed state of the Auction page (EXPERIENCE.md:156), and a page rendering a winner, an amount, a placement, a seed and a list is a design decision belonging with the story that owns the whole state rather than being half-built beside a draw.
  owner: Epic 4's closed-Auction surface — render the Closed state and read `drawForPlayer` for the lottery half.
  resolved: 2026-09-09 by the closed-Auction state. `src/lib/core/projection/closed.ts` composes `contractsReducer` and `drawsReducer` — the two folds that survive the close that deletes the Auction and the nomination — into `closedAuctionFor`, which is `null` unless a contract exists, so a `ContentionDrawn` with no `AuctionClosed` behind it is still not a Closed Auction. That one derivation is read by all three surfaces: `src/routes/auction/[fantraxPlayerId]/+page.server.ts` no longer 404s on a closed Auction (`loadAuctionPage` returns a discriminated `{ kind: 'open' | 'closed' }` and the 404 is now only "no Auction of any kind"), `src/lib/core/board.ts` emits a `closed` board card from `closedAuctions`, and `src/lib/core/positions.ts` sets `WonCard.href` to `auctionPathFor(...)`. `drawForPlayer` has a production caller in both the board read and the Auction page read. The Closed page prints the winner, the final amount and the placement sentence, and for a lottery the published commitment, the revealed seed, the ordered Contender names in the fold's own `seq` order with the selected position marked, and a link to `/verify` — so the AD-14 procedure can be run against real values for the first time. Proven in `tests/core/closed.test.ts`, `tests/board.test.ts`, `tests/server/board.test.ts`, `tests/routes/board.test.ts`, `tests/server/auction-page.test.ts` and `tests/routes/auction-page.test.ts`.
  status: closed

## Deferred from: spec-3-7-the-league-clock-and-the-end-of-the-auction-phase (2026-09-01)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-7-the-league-clock-and-the-end-of-the-auction-phase.md`
  summary: The Auction Phase ends and **no Manager is notified** — the transition is stated on every page the moment somebody opens the app, and nowhere else.
  evidence: spec-3-7's **Never** list states it outright ("No notification and no outbox (Epic 5) — `enqueue` and the drain stay the no-op seams they are; log the notification obligation to `deferred-work.md` as 5.3's, following Story 1.11's identical deferral"). The codebase agrees: `src/lib/shell/write.ts`'s `enqueue` is still a documented unimplemented seam, `src/lib/server/sweep.ts`'s `drain` is still a named no-op, and no outbox table appears in any migration. `src/lib/server/phase-end.ts` deliberately passes neither. What a Manager gets today is `src/routes/+layout.svelte`'s banner, worded by `PHASE_ANNOUNCEMENTS` in `src/lib/server/phase.ts` — which requires them to open the app. The event carries `seq`, the expiry it was due at, the instant it was evaluated at and how many nominations it terminated, so the notice is fully reconstructible from the log when the outbox lands.
  deferred_reason: Epic 5 owns notification transport (AD-17, AD-18: Discord only). Building a one-off dispatcher here would be the second thing to rewrite when 5.1's outbox arrives, and this story would be its only caller.
  owner: Story 5.3 — notify every Manager that the Auction Phase ended and Contract Assignment began, from `ContractAssignmentOpened` and the `AuctionTerminated` events before it.
  resolved: 2026-09-04 by Story 5.3. `src/lib/server/phase-end.ts` now registers `enqueueBroadcastsAndMentions`, answering `[EVERY_TEAM]` for `ContractAssignmentOpened` — the one trigger where the whole league is the honest affected set, since the phase ends for everyone at once and the event carries a NULL `team_id`. `EVERY_TEAM` is resolved inside the enqueue (`select discord_user_id from managers where team_id is not null`) rather than enumerated at the write site, because `evaluateLeagueClock` runs on every ten-second tick and appends nothing on almost all of them. The notice is `adapters/discord/mention.ts`'s flat form — every addressee on one line and no per-Team naming — so thirty mentions cost a fraction of Discord's 2000-character ceiling; it rides the broadcast line `The Auction Phase has ended and Contract Assignment is open.` that Story 5.2 already composed. `AuctionTerminated` gets no mention of its own: it carries no Team, it is one row per unbid nomination, and the phase notice already states how many there were. Proven in `tests/server/phase-end.test.ts` and `tests/adapters/discord-mention.test.ts`.
  status: closed

- source_spec: `_bmad-output/implementation-artifacts/spec-3-7-the-league-clock-and-the-end-of-the-auction-phase.md`
  summary: `EXPERIENCE.md` contradicts itself about where the frozen readable Bid Board lives — :32 puts it in **Archived**, :320 puts it in Contract Assignment. Recorded rather than resolved.
  evidence: spec-3-7's **Never** list names the conflict and instructs that it be logged rather than resolved here ("EXPERIENCE.md:32 puts the frozen readable board in **Archived**, not Contract Assignment, which contradicts :320"). It matters now for the first time because Story 3.7 is what actually moves a league into Contract Assignment, so the question "what does a Manager see on the board the morning after the phase ends" has become answerable rather than hypothetical. `src/lib/server/destinations.ts:85`'s Contract Assignment catalog is deliberately untouched by this story and `/board` still has no route behind it at all (Story 4.3), so nothing in the code currently depends on either reading — which is exactly why this is the cheap moment to settle it.
  deferred_reason: This is a design-document contradiction, not a code defect, and `EXPERIENCE.md` is a planning artifact this story may not hand-edit. Resolving it by picking one reading in code would silently make a product decision the document has not made.
  owner: Story 4.3, the Bid Board — decide which phase the frozen readable board belongs to and correct whichever EXPERIENCE.md line is wrong, then build to it.
  status: open
  resolution: 2026-09-01 by Story 4.3 — **Archived** is adopted, which is the reading `src/lib/server/destinations.ts:93` already implements and has since Story 1.6: the `bid-board` catalog entry is live in Auction and in Archived, and absent from Contract Assignment. `src/routes/board/+page.server.ts` calls `requireLiveDestination(locals.session, locals.phase.name, 'bid-board')` before any read, so `/board` serves in those two phases and refuses with the guard's 403 in Setup and in Contract Assignment — asserted in `tests/routes/board.test.ts` for all four phases, including that the read is never reached on a refusal. No code was written to the `:320` reading and nothing now depends on it. The `EXPERIENCE.md:320` line remains wrong and is NOT corrected here: `_bmad-output/planning-artifacts/` is skill-owned and an implementation story may not hand-edit it. Escalated below.
  escalated: `EXPERIENCE.md:320` still places the frozen readable board in Contract Assignment, contradicting `:32` and now contradicting shipped, tested behaviour as well. Route to `bmad-correct-course` to amend `:320` to Archived — the same treatment `deferred-work.md`'s `--strip-figure-size` entry received for a DESIGN.md frontmatter conflict. Until it is amended, a reader of EXPERIENCE.md alone will believe the board is reachable in a phase where the route returns 403.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-7-the-league-clock-and-the-end-of-the-auction-phase.md`
  summary: The Audit Log must render a **null actor as the system**, not as a blank — this story is what first makes a null `manager_id`/`team_id` writable, and nothing reads one yet.
  evidence: spec-3-7's execution task names this entry outright ("the Audit Log (7.5) must render a null actor as the system rather than blank"). `supabase/migrations/20260901000000_system_actor.sql` drops `not null` from `auction_events.manager_id` and `team_id` behind `check ((manager_id is null) = (team_id is null))`, `src/lib/core/types.ts` widens `EventEnvelope`/`AppendedEvent` to `string | null`, and `src/lib/shell/write.ts`'s `actorId` maps the column through null-preserving rather than `String(...)` — which would have produced the four-character string `"null"`. `src/lib/core/rules/phase-end.ts` writes the first such row: `ContractAssignmentOpened` carries `managerId: null` and `teamId: null` together, because nobody acted — the tick read a clock. Every `AuctionTerminated` beside it still carries the NOMINATING Manager and Team, so exactly one event type in the product is unattributed today. No surface reads `AppendedEvent.managerId` at all, so there is currently nowhere for a null actor to render wrongly; `tests/shell-write.test.ts`'s `toAppendedEvent — the actor pair` block is what guards the mapping in the meantime.
  deferred_reason: Epic 7.5 owns the Audit Log, and how a system-originated event is NAMED to a Manager ("the system", "the League Clock", "Appspiration") is a product-voice decision belonging with the surface that renders it — not one to settle in a fold that nothing displays. Building a renderer here would be a second thing to rewrite when 7.5 arrives.
  owner: Story 7.5, the Audit Log — render a null `manager_id`/`team_id` pair as the system, never as an empty cell, and never as a Commissioner. AD-14's whole argument is that the phase end must not read as though a rival adjudicated it.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-3-7-the-league-clock-and-the-end-of-the-auction-phase.md`
  summary: `LeagueClock.resets` and `voidedSeqs` grow for the life of a league and are scanned linearly, so folding the clock is quadratic in the number of resets — and three independent call sites refold it per request.
  evidence: bmad-code-review (2026-09-01). `src/lib/core/projection/league-clock.ts`'s reducer calls `state.resets.some(...)` on every `NominationPlaced` and `BidPlaced` for idempotency, and `state.voidedSeqs.includes(...)` on every `BidVoided`; `leagueClockExpiry`'s backward walk calls `includes` again per reset it skips. Neither list is ever pruned — deliberately, because an insert-only log states what happened and a fold that dropped a voided reset would make the two lists disagree about what it had seen. Meanwhile `src/lib/server/auction-page.ts`, `src/lib/server/bidding.ts` and `src/lib/server/phase-end.ts` each fold the whole log independently, per request or per tick pass. Correct and comfortably bounded for one 30-team offseason (a few hundred Bids at most, and the walk stops at the first surviving reset), so nothing is built for it here. It is the same unbounded-full-log-read shape already logged against `loadEventsViaClient` under stories 1.9 and 1.11, now with an accumulating per-fold cost on top.
  deferred_reason: Optimising a fold nobody has profiled would trade a shape that is obviously correct for one that is merely fast, on a league whose real size is still a guess (AR-33). A `Set` for `voidedSeqs` and a running "latest surviving reset" would both work, and both are cheap to add once there is a measurement to justify them.
  owner: Whichever story first profiles the read path — shared with the existing bounded/snapshot-read entries for `loadEventsViaClient`.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-3-7-the-league-clock-and-the-end-of-the-auction-phase.md`
  summary: `LeaguePhase` is declared twice and must stay in sync with two exhaustive records, with no structural test guarding the pair against drift.
  evidence: bmad-code-review (2026-09-01). `src/lib/core/projection/phase.ts` and `src/lib/server/phase.ts` each declare `export type LeaguePhase = 'Setup' | 'Auction' | 'Contract Assignment' | 'Archived'`, deliberately duplicated rather than shared: AD-2 forbids the core importing anything outside itself, and TypeScript treats the two as the same type only as long as their literal sets agree. `PHASE_SENTENCES` and — since this story — `PHASE_ANNOUNCEMENTS` are both `Readonly<Record<LeaguePhase, ...>>` keyed off the SERVER declaration, so adding a phase to the core alone type-checks everywhere and silently gives the new phase no sentence and no announcement. `src/lib/core/types.ts` now imports the core declaration for `PhaseGateOutcome`, and `src/routes/auction/[fantraxPlayerId]/+page.svelte` imports it again for the serialised gate state — three consumers of one hand-maintained pair. This story added `Contract Assignment` handling to both sides by hand and nothing would have failed if it had done only one.
  deferred_reason: The exhaustive `Record` gives a compile error the moment a phase is added to the SERVER declaration, which is the direction every existing edit has taken; the uncovered direction is core-only, which no story has yet needed. A structural test asserting the two literal sets match is cheap (`tests/structure.test.ts` already reads source text for exactly this class of claim) but is a guard against a shape nothing has yet produced.
  owner: Whichever story next adds or renames a phase — `LeagueArchived` is the outstanding one — should add the source-text equality assertion at the same time.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-live-updates-and-the-freshness-contract.md`
  summary: `FreshnessNotice.svelte`'s rendering — the heading and statement per state, the age line, and the `role="alert"` region that must be empty except in Stale — is asserted only by matching the component's source text, never by mounting it.
  evidence: bmad-build review (2026-09-01), Blind Hunter and Verification Gap layers. Vitest runs `environment: 'node'` with no DOM and no `.svelte` renderer, so a mistake inside the component's `{#if}` nesting would ship green. Story 4.1's client logic was made testable by extracting the four `FreshnessPorts` and driving them, and the poll and channel callbacks now have real behavioural coverage — the component markup is the one part that could not follow, because nothing in this repository can render a component at all. This is the same gap `deferred-work.md:41` (component-test harness) already names; recorded here because 4.1 is the first story whose *announcement* semantics — an assertive live region that must fire once on degradation and never on recovery — depend on markup no test can execute.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-live-updates-and-the-freshness-contract.md`
  summary: The `auction_watermark` trigger's runtime behaviour is asserted only by grepping the migration's SQL text; no automated test applies the migration and observes the trigger fire.
  evidence: bmad-build review (2026-09-01), Verification Gap layer. `tests/watermark.test.ts` checks for substrings such as `after insert on public.auction_events`, which cannot observe whether the trigger actually raises the row, whether the `where seq < new.seq` guard prevents regression, or how it behaves under concurrent inserts from the SvelteKit shell and the Deno tick at once. The migration WAS applied and driven by hand against real local Postgres during implementation — seeding, firing, idempotency, the singleton constraint, and the `anon`/`authenticated` grants were all confirmed — but that verification is not repeatable and does not run in CI. The reviewer noted this is consistent with how every other migration in this repository is tested and is not newly introduced by this story; it compounds with `deferred-work.md:214`, where the integration suite self-skips when no local Postgres is reachable.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-the-persistent-strip.md`
  summary: The strip's Contract Assignment behaviour — "reports assignment progress rather than Maximum Bid" — ships as Roster Count alone, because the Year Allotment it would report against does not exist.
  evidence: bmad-build step-02 planning (2026-09-01). `_bmad-output/planning-artifacts/epics.md:1455` requires the strip to report assignment progress in Contract Assignment Phase. Nothing in the repository can answer that: there is no `ContractLengthAssigned` event, no Year Allotment constant in `src/lib/core/constants.ts`, no assignment projection under `src/lib/core/projection/`, and no `contract_length` column in any migration under `supabase/migrations/`. `_bmad-output/implementation-artifacts/epic-4-context.md:59` puts Year Allotment mechanics explicitly out of scope for Epic 4. The strip is therefore built with a phase table that renders Roster Count and omits the Maximum Bid half in that phase, which is honest about what it knows rather than inventing a progress figure.
  deferred_reason: The AC depends on an unbuilt seam. Story 6.1 is the story that first WRITES a contract length, so it is the first story able to read assignment progress; building a placeholder here would be a second thing to rewrite when the real allotment arrives.
  owner: Story 6.1, Assign contract lengths — extend the strip's phase table so Contract Assignment reports remaining Year Allotment and assignment completion, per epics.md:1455.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-the-persistent-strip.md`
  summary: `DESIGN.md` specifies the strip's figure at 17px while its own frontmatter type scale has no 17px step, so the size cannot be expressed as a `--size-*` token.
  evidence: bmad-build step-02 planning (2026-09-01). `DESIGN.md:173` reads "`ui` at 26px on a board card, 21px on a totals row, 17px in the persistent strip", and `DESIGN.md:222` repeats "the figure at 17px". Its frontmatter `typography.scale` yields the ten steps declared at `src/lib/styles/tokens.css:68-77` — 10, 11, 12, 12.5, 13, 15, 18, 19, 21, 26 — with no 17. `tests/tokens.test.ts:216-230` asserts exactly ten `--size-*` tokens matching that scale, so adding `--size-17` would fail the suite against a document no story may hand-edit. This story therefore declares `--strip-figure-size: 17px` in the "Declared beyond the DESIGN.md frontmatter" block, following the `--control-fill` precedent at `tokens.css:107-128`.
  deferred_reason: `_bmad-output/planning-artifacts/` is skill-owned (AD-25 discipline); a mock disagreeing with its own frontmatter is an amendment to make through the owning skill, not an inline edit from an implementation story.
  owner: `bmad-correct-course` — either add a 17px step to `DESIGN.md`'s `typography.scale`, or correct the two prose lines to a step the scale actually carries.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-the-persistent-strip.md`
  summary: One integration test hard-FAILS rather than self-skipping when no local Postgres is reachable, so `npm test` is red on a clean checkout.
  evidence: bmad-code-review (2026-09-01), orchestrator verification. `tests/integration/auction-events.test.ts:873` calls `writeGateway()` inside a test body rather than behind the `describe.skipIf` guard the rest of that file uses, so it throws `SUPABASE_DB_URL is not set. The transactional write path is unavailable.` instead of skipping. Confirmed pre-existing and NOT introduced by story 4.2: checked out the story's baseline `4c65c94` and ran the file there, where it fails identically. The rest of the suite is green at 2441 passing. This defeats the self-skip contract `deferred-work.md:214` describes, and it makes a genuinely failing suite indistinguishable from a machine without Docker.
  deferred_reason: Out of scope for a presentation story; the fix belongs with whoever next touches the integration harness, and conflating it with 4.2's diff would hide it in a UI review.
  owner: Unassigned — move the `writeGateway()` call behind the file's existing `describe.skipIf` guard, or gate it on the same `SUPABASE_DB_URL` check the sibling tests use.
  status: open

## Deferred from: spec-4-3-view-the-bid-board (2026-09-01)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-view-the-bid-board.md`
  summary: The board has **no Closed and no Terminated card**. An Auction that ends simply leaves the board, so half of the five Auction states `epic-4-context.md:57` names have no rendering anywhere.
  evidence: spec-4-3's **Never** list states it outright, and the projections agree. `ContentionState` (`src/lib/core/projection/auctions.ts:115`) has exactly three members — `awaiting_opening_bid`, `standard`, `minimum_bid` — and no fourth, so the board's three-state label record is the whole of what the fold can answer. A close removes the entry from `auctionsReducer` AND from `nominationsReducer` together (`auctions.ts:806-815`, `nominations.ts:455-471`), and an `AuctionTerminated` does the same through `readTerminatedPlayerId` (`nominations.ts:382`), which yields a Player id and no reason at all — so nothing durable distinguishes "nobody won them" from "somebody did". `tests/board.test.ts`'s `drops a card the moment its nomination is released` asserts the removal rather than papering over it. Rendering either state needs a closed-auction projection that does not exist; `drawsReducer` (`src/lib/core/projection/draws.ts`) is the one durable record of a finished lottery and `drawForPlayer` still has no caller outside the test suite.
  deferred_reason: Both cards need a projection nothing in the repository builds, and designing one inside a presentation story would make the board the owner of a fold every other closed-Auction surface would then have to agree with. This is the same gap already logged under spec-3-6 ("A closed Auction still 404s") seen from the board's side rather than the Auction page's.
  owner: Epic 4's closed-Auction surface built the projection and the Closed card; the TERMINATED half is re-owned to Epic 7.2/7.3, the override stories.
  narrowed: 2026-09-09 to its Terminated half. The Closed card exists: `BoardCardState = ContentionState | 'closed'` is declared in `src/lib/core/board.ts` beside the labels it keys — `ContentionState` itself is deliberately unchanged, because the close deletes the Auction so the fold can never produce `closed`, and widening the union would force a dead case into every bidding gate that switches on it. `boardCardsFor` now takes the contracts and draws folds and emits a closed card per `closedAuctions(...)`, `sortBoard` tiers closed last in all three orderings (a closed card carrying `closedAt` would otherwise sort FIRST under `closing`, which is ascending time remaining), `filterBoard` gained `open`/`closed`, and `BoardViewerState` gained `won` as its fifth and only new member.
  remaining: the TERMINATED card, and it is still blocked by the same fact. `AuctionTerminated` yields a Player id and no reason at all (`nominations.ts`'s `readTerminatedPlayerId`), so nothing durable distinguishes "nobody bid on them" from a Commissioner override, and a card claiming either would invent the half the log does not carry. No override can append a reason until Epic 7.
  owner_remaining: Epic 7.2/7.3 — when an override records WHY a nomination was terminated, add a `terminated` member to `BoardCardState` and its three label/icon records, and render the card. `src/lib/core/board.ts` is the one place the card states live.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-view-the-bid-board.md`
  summary: A board card's metadata line reads `NBA · POS` and can carry no salary and no contract years, because the Free Agent pool table holds neither column.
  evidence: `epics.md:1468`'s card anatomy specifies the metadata line as `NBA · POS · $0.0M · Nyr`, but `free_agent_players` carries `player_name`, `positions` and `nba_team` and nothing else — the same narrowing `src/lib/server/auction-page.ts:547-560` already makes for the Auction page's own metadata line, and for the reason its header states ("no salary or contract-length column exists on that table"). `src/lib/core/board.ts`'s `metadataLine` therefore takes two fields, and `tests/board.test.ts` asserts the two-field rendering and the omitted-when-absent case. Rendering the other two would mean inventing figures the app has never imported.
  deferred_reason: The columns are an import concern, not a presentation one: the pool CSV is parsed by `src/lib/adapters/fantrax/` and promoted by `import-promotion.ts`, so carrying a current salary and a contract length onto a card requires a migration, an adapter change and a re-import — none of which belongs in a read surface, and all of which would need the commissioner to re-supply the pool export before a single card could show them.
  owner: Whichever story next changes the Free Agent pool import — add the two columns to `free_agent_players` and to the adapter, then widen `metadataLine` and its one caller. The board renders whatever it is given; nothing else in this story has to change.
  status: open

## Deferred from: spec-4-4-your-positions-the-landing (2026-09-02)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-your-positions-the-landing.md`
  summary: The first group ships as **Won** rather than as the mockup's "Won while you slept", because nothing in the system records when a Manager last looked.
  evidence: `_bmad-output/planning-artifacts/ux-designs/ux-BBSL-Appspiration-2026-08-17/mockups/Positions.dc.html:28` labels the group "Won while you slept", and `epic-4-context.md`'s UX section repeats the phrase. Nothing in the repository can answer it. `auction_events` records no per-Manager read or session instant; `src/lib/server/session.ts` and `src/lib/server/auth.ts` carry no last-seen column; there is no `ManagerSaw*` event and no projection under `src/lib/core/projection/` that folds one. The only instants a close produces are `AuctionContract.closedAt` (`src/lib/core/projection/contracts.ts:102`, the Auction's own nominal expiry) and the event's own `occurredAt` — neither of which is about the reader. Any "while you slept" cut would therefore be an arbitrary window (a "last 24 hours") presented as a personal fact, which is worse than an unqualified group: a Manager who checked an hour ago would read "while you slept" over an Auction they watched close. `src/lib/core/positions.ts` ships `GROUP_HEADINGS.won = 'Won'` and `contractsWonBy` returns every Auction the viewer's Team has won this phase, newest `closedAt` first — bounded by construction at the roster ceiling (twelve Active/Bench plus three Minor League Slots), which is why no window is needed for size either. `tests/positions.test.ts` asserts both the heading and the absence of the phrase.
  deferred_reason: Recording a per-Manager last-seen instant is a STORAGE decision — a new event type or a new mutable column, plus a write on every page load — and this is a read-surface story with no basis for making it. Introducing one here would put a write on the landing page's load path, which is exactly what `loadPositions`' rollback-always discipline exists to rule out.
  owner: Unassigned — whichever story first needs a per-Manager read state (a notification-read receipt in Epic 5 is the likeliest natural home, since it must already record what a Manager has seen). Once such an instant exists, partition the Won group against it and change the heading; `src/lib/core/positions.ts` is the one place either changes.
  status: open

## Deferred from: spec-4-4-your-positions-the-landing (2026-09-02, code review)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-your-positions-the-landing.md`
  summary: A won Player's name on Your Positions is **not a link**. `WonCard.href` is `null` until the closed-Auction surface exists, because `auctionPathFor` on a won Player resolves to a page that 404s.
  evidence: Raised by the Acceptance Auditor in this story's review and confirmed at source. A close DELETES the Player from both `auctionsReducer` and `nominationsReducer` (`src/lib/core/projection/auctions.ts:806-815`), and `src/routes/auction/[fantraxPlayerId]/+page.server.ts:93` raises `error(404, 'There is no open Auction for this Player.')` on that null read — so the original `href: auctionPathFor(contract.fantraxPlayerId)` made every card in the **first** group on the landing page a link to a refusal. This duplicates the gap the spec-3-6 entry above already records (`A closed Auction still 404s`, `status: open`); Story 4.4 did not introduce it and cannot close it, since rendering a winner, an amount, a placement and the lottery's seed and Contender list is the closed-state design that entry assigns to Epic 4. The card was already self-sufficient without the link — it names the Player, the winning amount, the placement and the Cap Hit — so the fix was to drop the link rather than to build the page: `src/lib/core/positions.ts` types `WonCard.href` as `string | null` and sets it `null`, and `src/routes/positions/+page.svelte` renders a plain `<span class="display card-player">` in that branch. `tests/positions.test.ts` asserts `card?.href` is null and `tests/routes/positions.test.ts` asserts the `{#if card.href !== null}` branch exists; both were mutation-checked against the original.
  deferred_reason: The link is only correct once a closed Auction HAS a page, and that page is a design decision — winner, amount, placement, seed and Contender list — belonging with the story that owns the whole Closed state (`EXPERIENCE.md:156`), not half-built beside a landing surface. Shipping the link now would trade a missing affordance for a broken one on the first group a Manager sees each morning.
  owner: Epic 4's closed-Auction surface, the same owner as the spec-3-6 entry. When it lands, set `href: auctionPathFor(contract.fantraxPlayerId)` in `positionsFor`'s Won branch and delete the `{#if}` in the surface — `src/lib/core/positions.ts` is the one place the decision lives.
  resolved: 2026-09-09 by the closed-Auction state, exactly as this entry specified. `WonCard.href` is `string` rather than `string | null` and `positionsFor` sets it to `auctionPathFor(contract.fantraxPlayerId)`; `src/routes/positions/+page.svelte`'s `{#if card.href !== null}` and its `{:else}` are deleted, because a branch on a value that cannot be null is a branch a later reader has to work out is dead. The link resolves to the Closed page, which carries what the card cannot — the closed instant in full and, for a lottery, the revealed seed, the published commitment and the ordered Contender list. `tests/positions.test.ts` and `tests/routes/positions.test.ts` assert the inverse of what they pinned.
  status: closed

## Deferred from: spec-4-5-view-any-team (2026-09-03)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-view-any-team.md`
  summary: The Contract Assignment surface still shows slot columns rather than the **Year Allotment** column swap `epic-4-context.md:26` describes — the same seam Story 4.2 already logged from the strip's side.
  evidence: spec-4-5's **Never** list states it outright ("A Contract Assignment column swap to Year Allotment. No `ContractLengthAssigned` event, no allotment projection and no `contract_length` column exists; the same gap Story 4.2 already logged"), and the repository agrees. `src/lib/core/projection/contracts.ts:100` types `AuctionContract.contractYears` as `null` and its comment says "Recorded UNSET at a close. Epic 6 assigns it"; there is no `ContractLengthAssigned` event type anywhere under `src/lib/core/projection/`, no allotment reducer, and no `contract_length` column on any migration under `supabase/migrations/`. `src/lib/core/strip.ts:90-95` records the identical gap for the strip's Contract Assignment reading and points at Story 6.1. `/teams/[teamId]` therefore renders its slot bands unchanged in the Contract Assignment phase, which is honest about what the app knows; a Year Allotment column there would be an invented figure.
  deferred_reason: Swapping the columns requires the allotment mechanics themselves — an event, a fold and a persisted column — which is Epic 6's subject matter, not a read surface's. Half-building the projection inside a presentation story would make the Team view the owner of a fold every Contract Assignment surface would then have to agree with.
  owner: Story 6.1 — build `ContractLengthAssigned`, the allotment projection and the `contract_length` column, then swap the columns on `/teams/[teamId]` and on Story 4.6's index from that one derivation. `src/lib/core/team-view.ts` is the one place the Team view's own columns are decided.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-view-any-team.md`
  summary: `/teams/[teamId]` is reachable **only by direct URL**. Nothing in the product links to a Team view, because the index that would is Story 4.6.
  evidence: The `teams` destination resolves to `/teams` in three phases (`src/lib/server/destinations.ts:79,87,94`), and `/teams` has no route — Story 4.6 builds the thirty-row index there, and this story ships the single-Team view plus the computation the index will call. Nothing under `src/routes/` or `src/lib/components/` emits a `/teams/<id>` href: `src/lib/core/auction-link.ts` is the only path builder in the core and it builds Auction paths alone. So the destinations sheet and the header menu both offer `Teams`, which 404s, while the page this story built is reachable only by typing an id. The route itself is complete and guarded (`requireLiveDestination` first, 404 on an unknown Team) and its tests execute both.
  deferred_reason: Building the index here would be building Story 4.6 — the thirty-row list, its sort chips, its `30 teams` count and the League Median are that story's whole subject, and spec-4-5's **Never** list forbids each by name. The seam is deliberate: `teamViewFor` takes one Team so 4.6 is a `map` over it, which is what makes "the index and the Team view cannot disagree" (`epic-4-context.md:56`) structural.
  owner: Story 4.6 — build `/teams`, link each row to `/teams/<id>`, and call `teamViewFor` per Team rather than re-deriving any figure.
  status: closed by Story 4.6 (2026-09-03). `/teams` now exists (`src/routes/teams/+page.server.ts`, `+page.svelte`), guarded by `requireLiveDestination(..., 'teams')` before any read, and every row links to `/teams/<id>` through `teamPathFor` in `src/lib/core/teams-index.ts` — the one place that path shape is spelled, following `core/auction-link.ts`'s precedent. The index calls `teamViewFor` once per Team over one fold (`src/lib/server/teams-index.ts`) and computes no figure of its own, so the destinations sheet and the header menu now resolve to a page rather than a 404.

## Deferred from: code review of spec-4-5-view-any-team (2026-09-03)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-view-any-team.md`
  summary: `loadTeamView` reads the whole event log and runs all five folds before it checks the Team exists, so an unknown or typo'd `teamId` pays for a full fold to reach a 404.
  evidence: `src/lib/server/team-view.ts:145-159` — `loadEventsViaClient`, five folds and `select now()` all precede `loadTeamIdentity` at `:159`. Identity resolution reads only `teams left join managers` and depends on no event.
  deferred_reason: Reordering is a real improvement but the cost is irrelevant at 31 users and a few thousand events, and moving the identity read above the clock read would separate `figuresAt` from the fold it anchors. Not caused by this change in any behavioural sense.
  owner: Any later story touching this read path.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-view-any-team.md`
  summary: `formatTeamManagers` joins three or more Managers with a flat ` & ` (`A & B & C`) rather than a comma list with a trailing `&`, and no test exercises more than two.
  evidence: `src/lib/core/team-identity.ts:69-77` joins on `MANAGER_SEPARATOR` unconditionally. `tests/team-view.test.ts:413` covers the two-Manager case only.
  deferred_reason: No Team in this 30-team League is co-managed by three, and inventing a list form now would be a wording decision made without a real case to check it against.
  owner: Whichever story first meets a three-Manager Team.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-view-any-team.md`
  summary: `loadTeamIdentity`'s `order by m.display_name asc` has no secondary sort key, so two Managers sharing a display name have an incidental order (AD-1 forbids incidental order elsewhere in the same diff).
  evidence: `src/lib/server/team-view.ts:104`. Every other ordering this story adds tie-breaks totally — roster rows on `fantraxPlayerId` (`core/team-view.ts:341-344`), Auction entries likewise (`:369`).
  deferred_reason: Pre-existing: the query is `server/auction-open.ts:87-115`'s, copied deliberately, and fixing it here would fork the two. `managers.display_name` is also unique in practice.
  owner: A story that revisits `server/auction-open.ts`'s join; fix both together.
  status: open

## Deferred from: spec-4-6-the-teams-index-and-the-league-median (2026-09-03)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-6-the-teams-index-and-the-league-median.md`
  summary: The Teams index renders its cap and slot columns unchanged in the **Contract Assignment** phase. The **Year Allotment** column swap `epic-4-context.md:26` describes is not built — the third story in a row (4.2 from the strip, 4.5 from the Team view, 4.6 from the index) to log the same missing projection.
  evidence: spec-4-6's **Never** list resolves it outright on 2026-09-03: "The Contract Assignment Year Allotment column swap — deferred to Epic 6. No `ContractLengthAssigned` event, no allotment projection and no `contract_length` column exists (`constants.ts:133` is a bare constant)". The repository agrees, unchanged since Story 4.5 logged it: `src/lib/core/constants.ts:133` declares `YEAR_ALLOTMENT` as a bare number that no event, reducer or column backs; `src/lib/core/projection/contracts.ts:100` still types `AuctionContract.contractYears` as `null` with the comment "Recorded UNSET at a close. Epic 6 assigns it"; there is no `ContractLengthAssigned` anywhere under `src/lib/core/projection/` and no `contract_length` column on any migration under `supabase/migrations/`. `tests/routes/teams-index.test.ts` drives the route through all three phases whose catalog carries `teams` and asserts the load succeeds identically in each, which pins the current honest behaviour rather than the intended one.
  deferred_reason: Swapping the columns requires the allotment mechanics themselves — an event, a fold and a persisted column — which is Epic 6's subject matter. Rendering a Year Allotment figure the app cannot derive would be inventing one on the surface a Manager reads thirty Teams from; rendering the cap and slot columns is honest about what the app knows.
  owner: Story 6.1 — build `ContractLengthAssigned`, the allotment projection and the `contract_length` column, then swap the columns on `/teams`, on `/teams/[teamId]` and in the strip from that one derivation. `src/lib/core/team-view.ts` decides the Team view's columns and `src/lib/core/teams-index.ts` decides the index's; both read the same `TeamView`, so the swap is one change reaching both.
  status: open

## Deferred from: code review of spec-4-6-the-teams-index-and-the-league-median (2026-09-03)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-6-the-teams-index-and-the-league-median.md`
  summary: Database `roster_slot_kind` values are cast straight to `RosterSlotKind` with an unchecked `as`, so an unrecognised slot kind is accepted silently and lands in the roster counts and the Cap arithmetic.
  evidence: `src/lib/server/team-roster.ts:188` maps `String(row['roster_slot_kind']) as RosterSlotKind` with no membership check; the counting loop then tests only `=== 'active_bench'` and `=== 'minor_league'`, so an unknown value counts as neither and a Team silently reads a smaller Roster Count than it holds. Raised by the Edge Case Hunter against Story 4.6's diff.
  deferred_reason: Pre-existing and not caused by this story — the cast is Story 4.5's, inherited verbatim when `loadLeagueRosterDetail` was extracted from `loadTeamRosterDetail`, and the column is constrained at the database. Fixing it here would put a validation rule in a read path this story only widened, and the same unchecked cast appears on every other reader of the table, so it should be fixed in one place for all of them rather than forked.
  owner: A story that revisits `team_rosters` reads, or the first one to add a `roster_slot_kind` value.
  status: open

## Deferred from: spec-5-1-the-transactional-outbox-and-its-dispatcher (2026-09-03)

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-the-transactional-outbox-and-its-dispatcher.md`
  summary: There is no **Commissioner-visible screen** for dispatcher failures. A notice that Discord refused is durably recorded and retried, but the only ways to see it are the `tick_heartbeats` table and the event log — neither of which any surface renders, and neither of which a Manager (or the Commissioner acting through the browser) can read at all.
  evidence: Every failed attempt is recorded twice over. `src/lib/server/outbox.ts`'s `drainOutbox` appends one `NotificationDispatched` per attempted intent before it reports anything, carrying `dispatch_outcome = 'attempted'` and `delivery_outcome` of `delivered`/`rate_limited`/`failed` plus the transport's `detail` and any `retryAfterMs` — and it then throws for a genuine failure, which `src/lib/server/sweep.ts:400-406` catches into `drainFailure` and `detailFor` (`:548-550`) writes onto the pass's one `tick_heartbeats` row. Both records are structurally unreadable from the browser: `20260821020000_auction_events.sql:100-104` revokes `anon` and `authenticated` and grants `service_role` `select, insert` only, `20260831000000_tick.sql` does the same for `tick_heartbeats`, and `20260903000000_notification_outbox.sql` repeats it for the outbox itself — all three with RLS enabled, forced, and no policy. So the facts exist, are complete, and have no reader. No route under `src/routes/` selects from `tick_heartbeats` or from `auction_events` for this purpose, and the Audit Log that would (Story 7.5) does not exist.
  deferred_reason: Deferred by explicit user decision on 2026-09-03, and the spec's **Never** list names it: "no Commissioner screen (deferred by user decision 2026-09-03)". Building one here would mean deciding what an operator surface for the tick looks like — which failures are actionable, what a stale heartbeat means, how a backlog is presented — inside the story whose subject is the write-and-drain mechanism. The mechanism does not need a screen to be correct, and a half-designed one built beside it would be the thing 7.5 and 8.2 then had to agree with.
  owner: Story 7.5 (the Commissioner surfaces and the Audit Log) for the human-readable view of `NotificationDispatched` outcomes, and Story 8.2 (the external heartbeat/backlog detector) for the alert that reaches a sleeping operator — AD-19 requires that detector to sit outside both vendors and outside the outbox path it reports on, so it is deliberately not a screen at all. `src/lib/server/outbox.ts` and `src/lib/server/sweep.ts` are where the facts already are; neither story needs a new column or a new event to render them.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-the-transactional-outbox-and-its-dispatcher.md`
  summary: **No production write registers `enqueueIntents`**, so the outbox is never written to outside tests and the drain always finds nothing. The mechanism is complete and proven; it is simply unwired.
  evidence: `enqueueIntents` is exported from `src/lib/server/outbox.ts` and referenced only by `tests/server/outbox.test.ts` (`:243` drives it through a real `runTransactionalWrite`; `:266` and `:366` exercise it directly). All seven production callers of `runTransactionalWrite` pass `load`, `decide` and sometimes `projections`, but none passes `enqueue` — `src/lib/server/auction-open.ts:183`, `src/lib/server/bidding.ts:443`, `src/lib/server/close.ts:188`, `src/lib/server/eligibility.ts:272`, `src/lib/server/import-promotion.ts:273`, `src/lib/server/nomination.ts:585`, `src/lib/server/phase-end.ts:159`. The seam itself is live: `src/lib/shell/write.ts:300-301` calls `enqueue` inside the transaction, before the `COMMIT` at `:304`.
  deferred_reason: Registering it on all seven writes now would enqueue an intent for every appended event carrying a `team_id`, and the drain would post a generic sentence to Discord for each — which is exactly what the spec's **Never** list forbids ("no message composition, no mention text, no event-type-to-copy mapping: that is 5.2 and 5.3"). Deciding WHICH events deserve a notification is the same decision as deciding what each one says, and both belong to the stories that own the copy. The spec's Execution list named none of these seven files.
  owner: Story 5.2 (broadcast intents and the channel post) and Story 5.3 (mention intents and their trigger set) — between them they enumerate the event types, and registering `enqueue: enqueueIntents` on the writes those events come from is the same change. Until then the outbox is inert by construction, not by accident.
  resolved: 2026-09-04 by Story 5.3. `enqueueIntents` is gone, and it could not simply have been registered: it keyed recipients on the event's own `team_id` — the ACTING Team — and every mention trigger names somebody else (the outbid Manager is not the bidder, the Manager whose Slot released is not the winner, and `ContractAssignmentOpened` carries no Team at all). `src/lib/server/outbox.ts` now exports `enqueueMentions(affectedTeams)`, a factory taking an `AffectedTeamsFn` the WRITE SITE supplies from the state it already folded, plus `enqueueBroadcastsAndMentions` for the three writes that owe both. Registered at `src/lib/server/bidding.ts` (the displaced leading Team), `src/lib/server/close.ts` (the Contenders on `ContentionDrawn`; the leader and the nominating Team on `AuctionClosed`) and `src/lib/server/phase-end.ts` (`EVERY_TEAM` on `ContractAssignmentOpened`). `src/lib/server/eligibility.ts` and `src/lib/server/import-promotion.ts` still register no `enqueue` at all, and `tests/server/outbox.test.ts` reads their source to keep it that way. The outbox is no longer inert: `tests/server/bidding.test.ts`, `tests/server/close.test.ts` and `tests/server/phase-end.test.ts` each assert the intents the write actually filed.
  status: closed

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-the-transactional-outbox-and-its-dispatcher.md`
  summary: **The drain batches by channel alone**, so one Discord message can cover several Teams' events and mention every Manager affected by any of them. Nobody is mentioned about nothing — each addressee is affected by at least one event in the batch — but the message does not say which item is whose, and the granularity is a design choice 5.2 and 5.3 inherit unexamined.
  evidence: `src/lib/server/outbox.ts` groups `due` into `batches` keyed on `intent.channel` only; `postBatch` then sends one message per channel with `recipients: [...new Set(batch.map((intent) => intent.recipient))]` and a body from `genericBodyFor(batch)` naming every event in the batch. Surfaced by the step-04 Blind Hunter layer on 2026-09-03 and confirmed against the source. A regression test added under this story documents the shape rather than changing it.
  deferred_reason: Not a correctness defect against any acceptance criterion — FR-27 asks that an affected Manager be mentioned, and each one is. It becomes a real question only when 5.2 and 5.3 attach per-event copy and 5.4 adds per-category muting: a batch that mixes a mutable category with an unmutable one has to decide what muting means for the message as a whole. Deciding that here would be deciding 5.4's semantics inside the mechanism story.
  owner: Story 5.2 (channel post shape) and 5.3 (mention triggers), with 5.4 as the forcing function — whichever first attaches real copy should revisit grouping by `(channel, recipient)` instead, which also makes the per-pass request budget mean what its comment says.
  resolved: 2026-09-04 by Story 5.3, and by placing the mention rather than by regrouping the batch. `adapters/discord/mention.ts`'s `mentionSuffixFor` composes each event's `<@id>`s INLINE on that event's own notice, and `composeBatch` appends the suffix under the notice it belongs to — so a post covering five events now says which line is whose. `adapters/discord/webhook.ts`'s `contentFor` no longer prepends anything, which was the thing that made the batch ambiguous; addressing (`allowed_mentions.users`) stayed there and is now narrowed to the snowflakes the composed body actually spells (`mentionsPresentIn`). Grouping by `(channel, recipient)` was therefore not needed and was not done — one post still covers several Teams, which is AD-18's batching, and the copy carries the granularity instead. The related budget concern IS addressed: `duePendingIntents` now slices on EVENT boundaries, so an event's broadcast row and its mention rows can never be split across two passes. Proven in `tests/server/outbox.test.ts` (the two-Team batch's body, the boundary slice, the over-budget single event) and `tests/adapters/discord-webhook.test.ts` (“never prepends a mention to the head of the message”).
  status: closed

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-the-transactional-outbox-and-its-dispatcher.md`
  summary: **A permanently broken webhook is retried forever.** A 401 or 403 from a revoked or rotated `DISCORD_WEBHOOK_URL` is folded into the same `failed` branch as a transient 503, and the backoff caps at one hour and reuses it indefinitely. There is no dead-letter state and no distinction between "will succeed later" and "will never succeed".
  evidence: `src/lib/adapters/discord/webhook.ts` returns `{ kind: 'failed', detail: ... }` for every `!response.ok` status without inspecting it; `backoffMsFor` in `src/lib/server/outbox.ts` caps at the last ladder entry and reuses it. Surfaced by the step-04 Blind Hunter layer on 2026-09-03.
  deferred_reason: The spec's design is deliberately insert-only with no mutable per-intent state, so a dead-letter flag would need either the first `update` grant in the repository or a new event type — both larger decisions than this story's scope. Retrying forever is also the safer failure mode: it costs one request an hour and loses nothing, where dropping an intent would silently cost a Manager a notification.
  owner: Story 8.2 (detect that the tick has stopped) is the natural home — an operator alert on a backlog that is not draining is the thing that actually resolves a revoked webhook, and AD-19 already requires that detector to sit outside both vendors.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-the-transactional-outbox-and-its-dispatcher.md`
  summary: **Nothing guards Discord's 2000-character message limit.** `genericBodyFor` joins an unbounded list of notices, and the batch grows with the per-pass budget, so a large sweep could compose a message Discord refuses.
  evidence: `src/lib/server/outbox.ts`'s `genericBodyFor` performs no truncation and has no length ceiling; the batch it renders is capped only by `PER_PASS_BUDGET`. Surfaced by the step-04 Blind Hunter layer on 2026-09-03. Unreachable at today's generic one-line-per-event body and a budget of five, which is why it is recorded rather than fixed.
  deferred_reason: The limit binds on message COPY, and this story is forbidden by its own **Never** list from composing any. Whatever 5.2 writes determines how many events fit, so a truncation rule written now would be guessing at a body that does not exist yet.
  owner: Story 5.2 — it owns the post's content and is the first point at which a realistic character budget can be computed.
  resolved: 2026-09-03 by Story 5.2. `genericBodyFor` is gone; `broadcastBodyFor` (`src/lib/adapters/discord/broadcast.ts`) replaces it and takes the ceiling as a parameter defaulting to `DISCORD_MESSAGE_CEILING = 2000`, Discord's own documented limit on `content`. It joins WHOLE notices, one per line, until the next would not fit, and reports how many it included. `composeBatch` in `src/lib/server/outbox.ts` records an outcome for only the intents behind the included notices, so an excluded notice has no `NotificationDispatched`, re-derives as pending, and goes out on the next pass — never dropped, never split mid-notice (AD-17). The one case that truncates is a SINGLE notice already over the ceiling (a draw with thirty contenders): it cannot be split and dropping it would stall the outbox forever, so it is cut to fit with one U+2026 and marked delivered. Both halves are proven in `tests/adapters/discord-broadcast.test.ts` ("posts whole notices up to the ceiling and leaves the remainder out", "truncates the ONE notice that alone exceeds the ceiling", and a real 30-contender draw asserted under the limit) and end to end in `tests/server/outbox.test.ts`.
  status: closed

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-the-transactional-outbox-and-its-dispatcher.md`
  summary: **A Team with no Manager rows produces no intent and no record that a notification was owed.** `enqueueIntents` inserts one row per Manager of the event's Team; a Team with none silently yields zero, and nothing anywhere observes that the notice went nowhere.
  evidence: `src/lib/server/outbox.ts`'s `enqueueIntents` resolves recipients per Team from the registry and inserts one intent per Manager returned. `managers.team_id` is nullable (`supabase/migrations/20260821010000_teams.sql:57-70`), so a Team with no Manager assigned is a supported state, not a corrupt one. Surfaced by the step-04 Edge Case Hunter layer on 2026-09-03.
  deferred_reason: Deciding what is owed to an unmanaged Team is a question about who deserves a mention, which the spec's **Never** list assigns to 5.3. It is also unreachable during a live auction, where every Team has a Manager by the time the auction opens.
  owner: Story 5.3 — it enumerates the mention triggers and is where "affected Manager" gets its definition.
  resolved: 2026-09-04 by Story 5.3, deliberately as silence. `enqueueMentions` resolves an affected Team to its Managers and files nothing when there are none, and nothing observes it — which is the decision this item asked 5.3 to make, not an oversight left standing. After Story 5.2 the broadcast post already names the Team and states what happened, so the RECORD exists and only the ping is absent; that is precisely the state Story 5.4 will let a Manager choose for themselves. An unmanaged Team is a supported state (`managers.team_id` is nullable) and is unreachable during a live auction, so a second observability mechanism here would be dead code. Stated in `src/lib/server/outbox.ts`'s `enqueueMentions` header and proven in `tests/server/outbox.test.ts` (“files nothing for a Team with no Manager rows, and never loses the broadcast”).
  status: closed

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-the-transactional-outbox-and-its-dispatcher.md`
  summary: **The drain's read-then-POST phase is not serialized across tick invocations.** Only the outcome write takes the global advisory lock, so two overlapping passes could both read the same due intents and both POST before either records an outcome.
  evidence: `readDueIntents` and `postBatch` in `src/lib/server/outbox.ts` run outside any lock; only `appendOutcomes` goes through `runTransactionalWrite`, which takes `GLOBAL_WRITE_LOCK_KEY`. The tick is scheduled every 10 seconds with `timeout_milliseconds := 8000` (`supabase/migrations/20260831000000_tick.sql:230`), so overlap requires a pass to exceed its own timeout. Surfaced by the step-04 Blind Hunter layer on 2026-09-03.
  deferred_reason: The exposure is the same at-least-once floor the spec's Design Notes already state for a non-idempotent HTTP sink, and the mitigation — holding the global write lock across an outbound HTTP call — would be strictly worse, blocking every bid and nomination for the duration of a Discord round trip.
  owner: Story 8.1 (replay against a synthetic clock) or 8.2, where burst and overlap behaviour first becomes observable; AD-10's single schedule is what keeps it theoretical today.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-broadcast-auction-events-to-the-league-channel.md`
  summary: **A drain pass reports no count of what the message ceiling excluded.** `DrainSummary` now counts only intents actually posted, so ceiling-driven backlog is invisible in the tick's own response — an operator sees a healthy pass whether nothing was excluded or four notices were.
  evidence: `drainOutbox` in `src/lib/server/outbox.ts` counts `message.posted.length`; the excluded count exists only as `due.length - attempted` and is returned nowhere. `supabase/functions/tick/index.ts` serialises `DrainSummary` as the tick's response body. Surfaced by the step-04 Blind Hunter layer on 2026-09-03.
  deferred_reason: Unreachable at today's figures — a full 30-Team draw renders at roughly 670 characters against a 2000 ceiling, so nothing is excluded in practice. Adding a counter now would ship an operator signal with no way to observe it firing, and the right shape depends on what a backlog detector actually consumes.
  owner: Story 8.2 (detect that the tick has stopped), which owns the growing-outbox-backlog alert AD-19 requires; the ceiling exclusion is one more input to that same detector.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-broadcast-auction-events-to-the-league-channel.md`
  summary: **The batch packer stops at the first notice that does not fit, rather than skipping it.** Given a small notice, an oversized one, and a second small one, the pass posts only the first — the third is held even though it would have fit.
  evidence: `broadcastBodyFor` in `src/lib/adapters/discord/broadcast.ts` breaks out of its loop on the first notice exceeding the remaining room instead of continuing past it. Surfaced by the step-04 Blind Hunter layer on 2026-09-03.
  deferred_reason: Not a defect against AD-17, which requires that a notice is never dropped, not that it is delivered in the earliest possible pass — the held notice goes out next tick, ten seconds later. Skip-ahead packing would also reorder the channel against log order, which the drain deliberately preserves, so the fix is a real trade rather than a cleanup.
  owner: Story 8.1 or 8.2, where burst behaviour against the 30 req/min ceiling is first observed and the delivery-shape decision is revisited.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-assign-contract-lengths-against-the-year-allotment.md`
  summary: **The Year Allotment column swap on the persistent strip, `/teams` and `/teams/[teamId]` is still not built** — the fourth deferral of the same work, but the first one where the projection it needs actually exists.
  evidence: Three prior entries (`:460` from Story 4.2's strip, `:517` from 4.5's Team view, `:556` from 4.6's index) each name Story 6.1 as owner and each predicted the same three prerequisites: "an event, a fold and a persisted column". Story 6.1 builds the first two — `CONTRACT_LENGTH_ASSIGNED_EVENT` and the fold in `src/lib/core/projection/contracts.ts` — and establishes that the third was a wrong prediction: `contracts.ts:1-45` states there is no contracts table, so an Auction Contract is a fold and a stored length column would be derived state in a table. The blocker those entries described is therefore discharged; what remains is purely the presentation threading, and it is larger than they assumed. `src/lib/core/teams-index.ts` has no `LeaguePhase` import, no phase parameter and no phase branch anywhere (it is phase-agnostic by design, with gating upstream at the route guard), and the `TeamView` type in `src/lib/core/team-view.ts:207-287` carries no phase field — `teamViewFor` takes `phase` only to pass it to `baselineCapOutcome`. `src/lib/core/strip.ts:77-99` branches on phase through two booleans only, and its `:90-95` comment points here explicitly.
  deferred_reason: Ratified at spec-6-1's step-02 checkpoint as a distinct, independently shippable deliverable rather than a fourth silent carry. Swapping the columns means threading phase through two deliberately phase-agnostic core modules and widening a type three surfaces share, which is presentation work of its own size; folding it into the story that writes the allotment rules would put a rules change and a cross-surface presentation change in one diff, and `BMAD-EFFORT-TRIAGE.md` bands 6.1 as Safe on the strength of the rules half alone ("the Year Allotment is counting to 1 / 1 / 2 / unlimited"). The three surfaces meanwhile keep rendering what they can honestly derive, which is the behaviour every one of the prior three entries chose deliberately.
  owner: Unassigned — no longer blocked on a missing seam, so it can be scheduled against any story in Epic 6 or taken as its own. `src/lib/core/team-view.ts` decides the Team view's columns and `src/lib/core/teams-index.ts` the index's; both read one `TeamView`, so the swap is one derivation reaching all three surfaces once phase is threaded to them.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-assign-contract-lengths-against-the-year-allotment.md`
  summary: **An `AuctionClosed` folded in for a Team that has already submitted as final creates a contract nothing can ever assign a length to** — `refuseAssignment` refuses every assignment once a Team is final, so the new contract is stuck at `contractYears: null` and blocks Story 6.3's export with no in-app path to clear it.
  evidence: bmad-build step-04 (2026-09-04), Blind Hunter layer, confirmed against source. `contractsReducer` (`src/lib/core/projection/contracts.ts`) and `assignmentsReducer` (`src/lib/core/projection/assignments.ts`) fold independently, and nothing couples them: a `ContractAssignmentsSubmitted` for a Team places it in `SubmittedTeams`, after which `refuseAssignment` returns `already_final` before it reaches the contract at all. A later `AUCTION_CLOSED_EVENT` naming that same Team still folds a fresh `AuctionContract` with `contractYears: null`. `refuseSubmission` cannot help either — the Team is already final and a second submission is refused.
  deferred_reason: Unreachable under the phase sequencing that exists today, which is why it is not a Story 6.1 defect: closes are produced by `src/lib/server/sweep.ts` during the Auction Phase, submissions are only possible in Contract Assignment, and `src/lib/core/rules/phase-end.ts` terminates every still-open Auction at the transition — so no close can land after any Team has submitted. It stops being theoretical the moment a Commissioner override can reopen or re-close an Auction after the phase turns, which is Story 7.2's void-and-restore and Story 7.3's remaining overrides.
  owner: Story 7.2 or 7.3, whichever first lands a Commissioner path that can append an `AuctionClosed` during Contract Assignment. The cheap guard is for the override to refuse when the receiving Team is already final, rather than for the fold to grow a coupling to the submission projection.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-6-2-track-completion-and-a-deadline-that-resolves-nothing.md`
  summary: A reminder interval longer than the window a deadline was set with makes the reminder fall due immediately, so it fires on the very next tick instead of one interval before the deadline.
  evidence: bmad-build step-04 review (2026-09-05), Blind Hunter and Edge Case Hunter independently. `refuseReminderInterval` in `src/lib/core/rules/assignment-deadline.ts` range-checks the hour count alone (1-168), and `refuseDeadline` never consults the standing interval; nothing cross-checks `reminderInstantFor(deadline, intervalHours)` against `now` at either set time or tick time. Setting a deadline three hours out with a 24-hour interval puts the reminder instant in the past, and `decideAssignmentDeadlineTick` then appends `AssignmentRemindersSent` on the next pass. The misleading copy was fixed here — the mention clause no longer claims the deadline is "one reminder interval away" — so what remains is a behaviour wrinkle rather than a false statement, which is why it was not patched: the honest fix is either a refusal naming the combination or a monitoring-page sentence stating the reminder is already due, and both are choices about what the Commissioner should be told rather than defects in what the tick does.

- source_spec: none (found during Story 9.1, 2026-09-05)
  summary: `tests/integration/auction-events.test.ts` resolves its database TWO different ways, so its fixtures and the code under test can silently address different databases.
  evidence: `tests/integration/auction-events.test.ts:31-32` sets `LOCAL_DB_URL = process.env['SUPABASE_DB_URL'] ?? 'postgresql://postgres:postgres@127.0.0.1:54322/postgres'` — raw `process.env`, which Vitest does not populate from `.env`. The code it exercises reads the same variable through `$env/dynamic/private` (`src/lib/shell/db.ts:36`), which SvelteKit backs with Vite's `.env` loading. Run `npx vitest run` without exporting the variable while a local stack is up and a `.env` names a hosted project, and the two diverge: `beforeAll` seeds `managers`/`teams` into the LOCAL database while `runTransactionalWrite` through `writeGateway()` writes to the HOSTED one. Observed 2026-09-05 as `insert or update on table "auction_events" violates foreign key constraint "auction_events_manager_id_fkey"` — 19 of 20 passing and the one test that uses the real shell failing, which reads as a bug in `write.ts` and is not one. Exporting the variable (`set -a; . ./.env; set +a`) makes all 20 pass. The fix is to resolve the URL once, the same way the code under test does, rather than to remember the invocation.
- source_spec: none (found during Story 9.1 AC 5, 2026-09-05)
  summary: BLOCKING — the security headers in `netlify.toml` reach static assets only. Every SSR page response carries no CSP, no X-Frame-Options, no Referrer-Policy, no X-Robots-Tag, no Permissions-Policy and no COOP.
  evidence: `curl -I https://pilot--bbslapp.netlify.app` on the first real branch deploy (2026-09-05) returns `X-Content-Type-Options` and `Strict-Transport-Security` — both Netlify/SvelteKit defaults — and NONE of the seven headers `netlify.toml`'s `[[headers]] for = "/*"` block declares. The same request against `/_app/immutable/assets/0.dCsRNIUe.css` returns all seven. The difference is `X-Sveltekit-Page: true`: Netlify applies `[[headers]]` rules to CDN-served static files and not to responses generated by Functions, and with `adapter-netlify` every page in this app is a Function response. So the app a human loads is framable, indexable, and has no Content-Security-Policy — the exact state this file recorded on 2026-08-20 as "escalated ... a private league's app is currently indexable", which Story 1.3 was believed to have closed. Story 1.3 closed the DECLARATION half, and `tests/headers.test.ts` proves the file declares them; netlify.toml's own comment states the serving half "is NOT verified here and is not something the repository can verify" and names this `curl -I` as the outstanding check. It is now performed, and it fails. `src/hooks.server.ts:42` already exports a `handle`, which is the natural single place to set these on every SSR response; note that doing so alongside the existing netlify.toml block means two sources, and if both ever emit a CSP on one response the browser enforces the intersection. Assigned to Story 9.3, which already owns the CSP.
  resolved: 2026-09-05 by Story 9.3. `src/lib/server/security-headers.ts` is the single source and `src/hooks.server.ts` applies it to every response this app generates — pages, form actions, the OAuth callback and the liveness endpoint alike, with no path allow-list to keep in step with the routes. netlify.toml keeps covering static assets; `tests/headers.test.ts` asserts the two sources are byte-for-byte equal on every constant header AND that the SSR set covers everything netlify.toml declares, so neither half can drift or silently become the weaker one. `auth/callback` stopped restating its two, which were the only security headers that path had. Verified against the live pilot deploy rather than against the file: 7/7 on the SSR landing page, 7/7 on /signin, both spot-checked headers on /api/watermark, and 7/7 still on a static asset. This closes the escalation this file opened on 2026-08-20 — the app is no longer framable, indexable or without a Content-Security-Policy.

- source_spec: none (found during Story 9.4, 2026-09-05)
  summary: BLOCKING for Story 9.7 — Story 1.7's "each file resolves independently to exactly one of the 30 Teams by name" is unsatisfiable against real Fantrax exports. All thirty roster files carry the same name, and the file content identifies no Team.
  evidence: Fantrax names a team-roster export after the LEAGUE, not the team. The real 2026-09-05 download is thirty-one files named `Fantrax-Team-Roster-Bring Back the Sonics.csv` and `...Sonics(1).csv` through `...Sonics(29).csv`, plus `Fantrax-Players-Bring Back the Sonics.csv` for the pool — "Bring Back the Sonics" being the league. `resolveTeamByFileName` (`src/lib/server/team-registry.ts:61-79`) normalises to letters and digits and matches a Team whose normalised name is CONTAINED in the file stem, so every one of the thirty stems is `fantraxteamrosterbringbackthesonics` plus at most two digits: no Team name can distinguish them, and any name that matched one would match all thirty as ambiguous. The content cannot resolve it either — Story 9.5 removed `ROSTER_COLUMNS.fantraxTeamId` because the export has no team column at all (`src/lib/adapters/fantrax/roster-file.ts`), so the only signal distinguishing one roster from another is the order in which the Commissioner downloaded them. This compounds the loss already recorded in the 9.5 entry: with no team column there is no content-altitude check that a file reached the right Team, and now there is no file-altitude one either, so a mis-slotted roster stages cleanly and looks correct. Three remedies, none free: (a) rename all thirty files to Team names before upload, no code change but thirty manual renames per import with an undetectable failure mode; (b) pair each file with an explicit Team selector in the `/import` UI, which is a real change to Story 1.7's surface and its "resolves by name" AC; (c) derive identity from a Commissioner-supplied ordering, which is (a) with a script. This is a correct-course-scale decision, not an implementation detail, and it blocks the pilot's import test rather than sign-in.
  decided: 2026-09-05 — remedy (c) shipped, remedy (b) deferred past the auction, by the Commissioner. `scripts/name-roster-exports.js` (`npm run name:rosters`) turns the download order into filenames in one declared, reviewable place and copies rather than moves, so a wrong ordering survives. Verified across the thirty real exports: every renamed file resolves through the real `resolveTeamByFileName` to exactly the intended Team and no other, every file parses through the real adapter with no refusal, and no roster breaches Story 1.7's slot ceilings — though Philadelphia sits at exactly 12 Active/Bench and eleven Teams at exactly 3 Minor League, so a single roster addition before setup day starts refusing that file. The ordering was confirmed by hand against the printed highest-paid-player table on 2026-09-05, which retires the risk for THIS download.
  reasoning: (b) — an explicit Team selector on the `/import` surface — remains the only remedy that removes the error CLASS rather than mitigating it, and is the right durable fix. It was not built before the auction for four reasons, recorded so the trade is auditable rather than remembered. First, the failure is not truly undetectable: Story 1.9's per-Team preview precedes promotion, and the seven pilot moderators each know their own roster, so a mis-slot is caught by the people best placed to catch it. Second, the risk is concentrated in one human act — the download order — not distributed across ongoing use, and that act has now been verified. Third, the import runs perhaps three times ever (pilot, setup day, one re-run), and (b)'s value scales with frequency. Fourth, (b) changes a shipped story's acceptance criteria and needs a pairing interface that works at 375px with its own tests, in a week that still owes Story 9.3's live security defect, 9.6, 9.7, 9.8, 7.4 and three Epic 8 stories.
  revisit: (b) becomes correct the moment either of two things is true. If the league ever re-imports MID-AUCTION — a Commissioner correction, or a botched setup redone under time pressure — then (c)'s risk stops being one-time. Or if a future offseason's setup is run by anyone who was not present for this one, since the ordering knowledge lives in a person rather than in the data. Raise it at the Epic 9 retrospective. Two mitigations are owed alongside (c) and are assigned: the highest-paid-player check becomes a REQUIRED step in Story 9.6's runbook rather than a convenience, since it is the only thing that can catch a bad ordering before the data is live; and Story 9.7's script must have each moderator confirm their own roster before promotion, which converts the pilot into a real check on seven of the thirty placements.
  ruled_out: there is no fix at source. Fantrax names every export after the league, the pool file included (`Fantrax-Players-<league>.csv`), so no export mode hands back team-named files.

- source_spec: none (found during Story 9.7 preparation, 2026-09-06)
  summary: `supabase functions deploy tick` failed on its first ever attempt — the tick's import map named `postgres` by its deno.land URL, which cannot resolve its own five `@std` dependencies when Supabase bundles it.
  evidence: `deno.json` mapped `postgres` to `https://deno.land/x/postgres@v0.19.5/mod.ts`. Deploy returned `Failed to bundle the function (reason: Relative import path "@std/fmt/colors" not prefixed with / or ./ or ../ and not in import map from "https://deno.land/x/postgres@v0.19.5/connection/connection.ts")`. Fetching `https://deno.land/x/postgres@v0.19.5/deno.json` shows the module declares five bare-specifier dependencies of its own — `@std/bytes`, `@std/crypto`, `@std/encoding`, `@std/fmt`, `@std/path` — which the deno.land URL form does not carry to the bundler. Fixed by importing the same package at the same version from JSR, `jsr:@db/postgres@0.19.5`, which brings its own import map; pinning the five transitive versions by hand was the alternative and would have put five more exact versions under the pin contract for no benefit. `scripts/check-pins.js` accepts the JSR form (`DENO_SPECIFIER_FORMS`) and the version is still an exact semver, so the pin gate is unaffected — `tests/pins.test.ts` mentions the deno.land URL only as a negative example. This could not have been caught before now: the Edge Function had never been deployed, `npm test` does not bundle it, and `deno check` in CI resolves imports without going through Supabase's bundler.
  resolved: 2026-09-06, and verified beyond the deploy succeeding. A manual invocation with the `x-tick-invocation-secret` header returned HTTP 200 and a full sweep-then-drain result (`outcome: ok`, 0 closed, 0 failures, League Clock evaluated, assignment deadline evaluated); without the header it returned 401. That is the first execution of the tick anywhere, and it also confirms Supabase's auto-injected `SUPABASE_DB_URL` reaches the function — the name is reserved and cannot be set as a function secret, so the write path depends on that injection. A `tick_heartbeats` row was written, proving the database path end to end.
  summary: The import staged one row per INSERT statement, so a real Free Agent pool took 65 seconds to stage and the whole thirty-one-file import took 87 — against Netlify's 10-second synchronous function budget. The Commissioner saw "This function has crashed. An unknown error has occurred".
  evidence: `src/lib/server/pool-import.ts` and `roster-import.ts` each ran `for (const row of rows) await client.query('insert ... values ($1..$n)')`. A BBSL pool is ~1,470 Players, so that was ~1,470 sequential round trips inside one transaction. Measured against the hosted dev project: 86,927 ms for the batch, 65,241 ms of it the pool file alone, median 747 ms per roster. Netlify kills a synchronous function at 10 seconds and reports a generic crash naming neither the file nor the cause, which is why the error was uninformative. No test could have caught it: every fixture in the suite is two or three rows, where a loop and a batch are indistinguishable, and `npm test` never touches the hosted database. It also could not surface before Story 9.4 produced importable files at real size.
  resolved: 2026-09-06. Both paths now build one multi-row INSERT — the pool in batches of 500, bounded because a parameterised query carries at most 65,535 bind parameters and four columns per row would otherwise cap out near 16,000 Players. Re-measured against the same database: the pool file 65,241 ms -> 445 ms, the full thirty-one-file import 86,927 ms -> 8,980 ms, still 31/31 staged. The fake gateways in `tests/server/pool-import.test.ts` and `roster-import.test.ts` now unflatten the batched parameters so every existing per-row assertion still reads one Player at a time, and both assert the parameter count divides evenly by the column count.
  residual: 8,980 ms was measured from a developer machine at 228 ms round-trip latency. A Netlify function in us-east-1 reaching the dev project in us-west-2 has roughly a third of that, so the real figure should be nearer 3 seconds — but that is an estimate, not a measurement, and the margin against a 10-second ceiling is not enormous. If the pool grows substantially, or the import is ever run against a further region, the durable answer is uploading the thirty-one files as separate requests rather than one, which removes the ceiling from the equation instead of staying under it.

- source_spec: none (found during Story 9.7 preparation, 2026-09-06)
  summary: Promotion carried the same row-at-a-time INSERT defect the staging paths did, and crashed the same way — the batching fix to `pool-import.ts`/`roster-import.ts` did not reach `import-promotion.ts`.
  evidence: `writeLiveTables` in `src/lib/server/import-promotion.ts` ran a nested `for (const team) for (const row)` over `team_rosters` and a second loop over `free_agent_players`, one INSERT statement each. A real league is ~300 rostered Players and ~1,470 Free Agents, so promotion was ~1,770 sequential round trips inside the single transaction that also appends `ImportPromoted`. Netlify killed it at 10 seconds and reported "This function has crashed. An unknown error has occurred". Staging had just been fixed and the Commissioner reached promotion for the first time, which is the only way this could surface — the two paths are separate files and fixing one told nobody about the other.
  resolved: 2026-09-06. `writeLiveTables` now builds multi-row INSERTs, chunked at 500 rows for the same bind-parameter ceiling the staging fix documents. Atomicity is unchanged: every statement still runs inside the transaction `promoteImport` opened, so a failure anywhere still rolls the event and both tables back together — only the round-trip count changes. Measured against the hosted dev project with the real files: staging 31 files 9,064 ms, promotion 871 ms, committing 303 roster rows, 1,467 Free Agents and one ImportPromoted event.
  audit: every remaining `await client.query()` inside a loop across `src/lib/server/` and `supabase/functions/tick/` was checked after this fix. The others are bounded by construction — `nomination.ts:390`, `outbox.ts:446` and `bidding.ts:392` iterate the one or two events a single write appends, and `outbox.ts:385` iterates a Team's co-Managers. The import paths were the only ones iterating league-scale data, and all three are now batched.

- source_spec: none (found during Story 9.7 preparation, 2026-09-06)
  summary: A pilot with fewer moderators than Teams cannot open the auction — `refuseAuctionOpen` requires every Team to have a Manager, and one Discord account binds to exactly one Team.
  evidence: `src/lib/core/rules/auction-open.ts:190` refuses with `unbound_teams` when any live Team has no Manager, which is correct: an unbound Team can neither nominate nor bid, so its roster and cap space would sit inert while the auction ran around it, and opening is irreversible. Seven moderators were seeded against thirty Teams, so 23 were unbound and the open control stayed disabled with its reason stated. Co-management does not help — two `managers` rows may share one `team_id`, but `discord_user_id` is unique, so one person cannot hold two Teams.
  resolved: 2026-09-06 for the pilot only. `npm run seed:league -- --placeholders` gives every Manager-less Team an inert Manager whose `discord_user_id` carries a NON-NUMERIC prefix (`pilot-placeholder-<abbrev>`). A Discord snowflake is an unbroken run of digits, so a placeholder can never collide with a real account and nobody can sign in as one: it adds a Team binding without adding an identity, which is what makes it safe rather than a back door. The flag refuses outright when `SUPABASE_ENVIRONMENT` is `prod`, checked BEFORE connecting so the guard does not depend on reaching a database.
  cost: those 23 Teams are inert. The pilot exercises every gate, refusal, close and draw, but with seven active bidders over ~1,470 Free Agents rather than thirty — thinner competition than the real auction, and the burst behaviour Story 8.1 wanted observed against the 30 requests/minute webhook ceiling will be correspondingly quieter than the real thing. Story 9.8 seeds all thirty-one real Managers and must wipe these first; a placeholder surviving into prod would let the auction open with Teams nobody owns, which is the exact failure this gate exists to prevent.
- source_spec: none (found during Story 9.7 preparation, 2026-09-07)
  summary: Sign-in did nothing on mobile. `form-action` omitted the Supabase origin, and WebKit enforces that directive across a form submission's whole redirect chain — so every iOS browser refused the OAuth navigation silently while desktop worked.
  evidence: The policy served on SSR pages read `form-action 'self' https://discord.com`. A sign-in POSTs to this origin, 303s to `https://<ref>.supabase.co/auth/v1/authorize`, continues to `discord.com`, returns to Supabase's callback and finally back here. WebKit applies `form-action` to EVERY hop of a chain a form submission started; Chrome and Firefox check only the immediate action. The Supabase hop was therefore refused on iOS — where every browser is WebKit — with nothing rendered to explain it, while the identical build signed in on desktop. This is a mobile-first product (the UX contract is one-handed at 375px) failing exclusively on mobile. It was introduced by Story 9.3: before that change SSR responses carried no CSP at all, so no form navigation was ever checked, and the directive had been wrong-but-inert since Story 1.3 wrote it.
  resolved: 2026-09-07. `form-action` is now derived alongside `connect-src` from PUBLIC_SUPABASE_URL and admits `'self'`, `https://discord.com` and the deployment's own Supabase origin — nothing else, and a host that is not a plain hostname still degrades rather than being interpolated, so a wildcard cannot reach the policy by any input. Two regression tests were added: one asserts every hop of the sign-in chain is admitted by name, the other that hostile input never widens the directive past `'self'` and Discord.
  lesson: the existing tests asserted the CSP's SHAPE — no wildcards, no third-party host beyond Discord, the inline allowance confined — and every one of them passed while sign-in was impossible on half the devices. A policy can be provably tight and still wrong about what the application actually does. The new tests assert the chain the product needs rather than the properties the policy should avoid.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-1-the-outstanding-bid-allowance-in-the-slots-gate.md`
  summary: `EXPERIENCE.md` still documents the slots gate row as one form ("Roster Count would be 10 of 12"); Story 10.1 gave it four.
  evidence: `gateFigure`'s slots case now renders four distinct forms (P=0 stash, allowance met, allowance spent, precondition failed). The UX doc names only the original, and `bidding.ts` cites it as the shape it follows verbatim — a citation that is no longer accurate.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-1-the-outstanding-bid-allowance-in-the-slots-gate.md`
  summary: The `CORE_VERSION` 1 -> 2 bump has no accompanying deploy/runbook guidance for events already logged under version 1.
  evidence: AD-20 fail-stops the tick when the Node and Deno deployments disagree on `CORE_VERSION`, so this bump makes a split deploy refuse to run. Story 9.6's setup runbook does not mention the version-bump procedure, and Story 9.7's pilot will be the first deploy to carry one.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-1-the-outstanding-bid-allowance-in-the-slots-gate.md`
  summary: Decide whether a Commissioner-overridden Team above the roster ceiling should still pass a zero-addition (Minor League stash) bid.
  evidence: The pre-10.1 comparison `rosterCount + projectedAdditions <= 12` refused it; FR-37's rewritten `P = 0` branch admits it, because the win lands in a Free Minor League Slot and adds nothing to Active/Bench. Defensible and faithful to FR-37's text, but it is a silent behaviour change in an override state, and Story 7.x owns overrides.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-3-cancel-the-surplus-commitment-at-close.md`
  summary: `auctionsLimitedTo` scans with `Array.prototype.includes` inside an `Object.entries().filter()`, giving O(n*m) per `commitmentStands` call inside the cascade's re-test loop.
  evidence: Real but not this story's problem — the shape predates the cascade and only bites a league with many concurrent Auctions. A `Set` lookup is the obvious fix and is a one-line change whenever auction volume justifies it.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-4-restore-the-next-highest-bidder.md`
  summary: `tests/integration/auction-events.test.ts` fails against real Postgres with a foreign-key violation on `auction_events_manager_id_fkey`.
  evidence: Pre-existing and unrelated to this story — reproduced at baseline commit 56a1e66 with the branch's changes stashed, failing identically. The seeded manager row the test inserts against does not satisfy the FK, so the whole suite reports 1 failure on every run and masks any genuine new failure in that file.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-5-a-lottery-whose-contenders-were-cancelled.md`
  summary: `src/lib/adapters/discord/broadcast.ts` still comments that an empty Contender list "is malformed, not merely uninteresting ... describes a draw that cannot have happened", which Story 10.5 made false.
  evidence: Real but deliberately not fixed here — the behaviour is already correct (the renderer returns null and falls through to the fallback line, no crash), and the spec forbids touching notice copy, which is Story 10.6's. 10.6 rewrites that renderer to word the empty draw, and the stale comment must be corrected in the same pass rather than leaving two stories editing one function.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-6-say-it-on-the-board-and-in-discord.md`
  summary: The Discord half of Story 10.6 — two new `outbid` clauses in `clauseFor` for the cancelled and restored Managers, the `BidCancelled` league-channel line in `broadcast.ts`, and the stale empty-Contender comment at `broadcast.ts:395-400` that Story 10.5 falsified.
  evidence: Split from spec 10-6 on 2026-09-09 at the scope gate — the full story measured ~5,050 tokens against the 1,600 standard. The two halves share no code: the Discord half touches only `src/lib/adapters/discord/mention.ts`, `broadcast.ts` and their two test files, and the only fact common to both is `BidCancelledPayload` (`core/rules/close.ts:675-699`), which is frozen and read-only on either side. Recipient targeting already exists (`server/close.ts:331-347`), so until this lands both notices degrade to the correct-but-terse `A BidCancelled was recorded (event #n)` fallback rather than being wrong. Constraints that must survive the carve: the existing `outbid` category only (no fourth member), `MUTABLE_NOTIFICATION_CATEGORY` stays `slot_release`, the Story 5.4 refusal wording is unchanged, and the two clauses are selected by recipient `teamId` inside `clauseFor` because `CLAUSE_FOR_CATEGORY` holds one clause per category. Carries AC UX-DR37.

## Deferred from: spec-closed-auction-state (2026-09-09, code review)

- source_spec: `_bmad-output/implementation-artifacts/spec-closed-auction-state.md`
  summary: FR-23 still scopes the Bid Board to "all **open** Auctions", while `EXPERIENCE.md`'s State Patterns table and `epic-4-context.md:57` both require the board to render all five Auction states including Closed. The board now shows closed cards, so the FR and the UX spec disagree in writing.
  evidence: Surfaced during this story's review while checking a (false) claim that `epics.md` caps the board at three filters. `epics.md:73` FR-23 reads "Any Manager can view all open Auctions with live state ... sortable and filterable"; `epics.md:1537` adds only "filterable to Auctions the viewer's Team is leading or contending in", stating a minimum and no maximum. The tension is PRE-EXISTING and was resolved deliberately by the human at this story's planning gate, who chose retention for the whole Auction Phase because "managers like to browse the closed auctions anyway" and required closed cards be filterable so the board stays readable. The code follows that decision; FR-23's wording was not amended because a story may not rewrite an FR. Not a defect — a requirements-level inconsistency that should be reconciled the next time `epics.md` is edited, so nobody re-derives the board from FR-23 alone and deletes the Closed card.
  owner: Unassigned — whichever story next edits `epics.md` FR-23, or an Epic 4 retrospective.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-closed-auction-state.md`
  summary: `src/lib/core/rules/contract-assignment.ts:46` still names `positions.ts` as the home of `PLACEMENT_LABELS`, which this story relocated to `projection/closed.ts`.
  evidence: Raised by the Acceptance Auditor and confirmed at source. The comment reads "`SLOT_LABELS`, never `positions.ts`'s `PLACEMENT_LABELS`" and its REASONING still holds exactly — the two records are the article form and the readout form — but the file it names is no longer where the record lives. Not fixed in this story because the spec's own Verification criterion asserts that no file under `src/lib/core/rules/` is modified, and that guard is a clean mechanical check worth more than a comment correction; a comment-only edit would still break it. Two sibling references the auditor also flagged were checked and are NOT stale: `rules/roster-import.ts:133` and `server/team-roster.ts:132` name the record without claiming where it lives.
  owner: Whichever story next has a legitimate reason to edit `src/lib/core/rules/contract-assignment.ts`; correct the one word in the same pass.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-closed-auction-state.md`
  summary: `tests/server/auction-page.test.ts`'s fake gateway resolves Team names by JavaScript string matching, so the closed lottery's "a non-uuid id must produce a MISSING name, not a 500" claim is asserted in prose but never exercised against Postgres's real `::text` casting.
  evidence: Raised by the Blind Hunter layer. `loadContenders`' docblock states the `::text = any($1::text[])` cast exists so a malformed historical payload cannot 500 the page — the same reasoning `loadMetadata` and `loadAuctionPage` already give for their own casts. The harness models the query with `.includes(id)`, which cannot reproduce a cast failure, so the guarantee rests on the SQL being read correctly rather than on a test. Pre-existing pattern across this whole test file rather than something this story introduced, and the only real proof would be an integration test against live Postgres — which is the suite already documented as failing on a database mismatch (`README.md:311`).
  owner: Unassigned — naturally belongs with Story 8.x or whoever repairs the integration suite, where a real Postgres connection already exists.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-import-thirty-team-roster-files.md`
  summary: Evaluate replacing FR-1's thirty-file CSV roster import with a single `getTeamRosters` API call, as a v1.1 candidate for the next offseason.
  evidence: Probed 2026-09-10 against the real BBSL league (`leagueId=wkusoprtmo8qu3hm`). `GET https://www.fantrax.com/fxea/general/getTeamRosters?leagueId=…&period=1` answered **unauthenticated** with all 30 Teams, 303 roster rows and 35KB in one call, carrying every field FR-1's thirty files establish: `salary` (the Cap Hit), `contract.name` (the end year or rookie-scale code the importer derives years from), `status` (all four Roster Slot kinds — ACTIVE, RESERVE, MINORS, INJURED_RESERVE), a Fantrax team id the CSV never had, and `salaryCap: 165000000.0` matching `SALARY_CAP` exactly. Every Team passed FR-1's slot ceilings when the live payload was checked against them. This contradicts addendum §A finding 1 ("salaries and contracts are absent from the documented read surface"), which was believed on the strength of `go-fantrax`'s `RosterItem { ID, Position, Status }` — a struct modelling three fields of a payload that carries six. Setup day is described in the addendum as "high-stress and low-patience", and one GET replacing thirty file uploads is the largest single reduction available to it.
  counter-evidence, and why this is NOT being done for the 2026 auction: **the CSV is the safer source for establishing state.** The API emits money as scientific-notation floats carrying representation error — the live payload contains `23499999.999999993`, `46499999.999999985` and `28499999.999999993`. Truncating those puts the figure a dollar low AND off the $500,000 grid that the entire money rendering depends on (AD-8, and the addendum's own "abbreviated money never reaches a CSV cell" rule); 4 of 303 rows corrupt under `int()`, 0 under `round()`. The CSV carries exact integers with thousands separators and no float anywhere in the path. Swapping a lossless source for a lossy one to save thirty uploads is the wrong trade on the offseason where the rules engine is being trusted for the first time. Two further integration costs would also have to be paid first: player ids are bare here (`01eon`) but asterisk-wrapped in the CSV (`*04ewu*`) which the importer stores verbatim, and the `teams` table holds no Fantrax team id, so Team resolution would need a new explicit mapping rather than the current UI selection.
  decision: 2026-09-10 — logged as a **v1.1 candidate**, not scheduled. Revisit before the next offseason's setup day, starting from this evidence rather than rediscovering it. Any implementation must round-then-assert-the-grid at the adapter boundary and normalise ids in both directions before a single figure reaches the core.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-the-commissioner-control-class-and-the-reason-sheet.md`
  summary: Story 7.2's first override route must chain three separate guards (Commissioner-only, overridable-phase, reason-required) with no combinator ensuring all three are called in a safe order.
  evidence: 7.1 ships requireCommissioner, requireOverridablePhase and requireOverrideReason as independent functions. The module docblock argues structural enforcement beats remembering, but composition across the three is still remembered. Worth a single composed guard at the first real call site.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-the-commissioner-control-class-and-the-reason-sheet.md`
  summary: The reason-required refusal throws SvelteKit `error()`, which discards an in-progress override rather than re-displaying the sheet with the typed values intact.
  evidence: Mirrors the commissioner-guard.ts precedent the spec named, so it is correct for 7.1. But the sheet is framed as a correctable second step of an ordinary form; a JS-disabled submit that reaches the server loses the whole override to a generic error page. Whichever story adds the first override route should decide between `error()` and `fail()` there.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-the-commissioner-control-class-and-the-reason-sheet.md`
  summary: `OverrideState` is an open string-keyed map, so a record whose `before` and `after` describe different fields is constructible.
  evidence: buildOverrideRecord takes before/after independently with no key-set check. Harmless while no override emits one; once 7.2 writes real states to the Audit Log, a mismatched pair would assert a change on a field with no counterpart. Add the symmetry check when real states exist.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-the-commissioner-control-class-and-the-reason-sheet.md`
  summary: The override reason has no maximum length, client or server, and is written permanently to an insert-only log.
  evidence: No maxlength on the textarea and no server bound in validateReason. Only the Commissioner can submit one, so the risk is low and a bound was not specified; decide the limit alongside the first route that actually appends the payload.
- source_spec: `_bmad-output/implementation-artifacts/spec-7-6-dead-money-and-the-rookie-scale-designation.md`
  summary: `server/nomination.ts`'s under-contract query reads `team_rosters` unfiltered by `roster_slot_kind`, so once Story 7.8 writes a Dead Money row the dropped Player will read as still under contract to the Team that dropped him.
  evidence: `src/lib/server/nomination.ts:177-183` runs `select t.name from team_rosters r join teams t on t.id = r.team_id where r.fantrax_player_id = $1` with no slot-kind predicate, under a comment reading "A Player who was promoted into a roster is under contract even if a stale pool row still names them." Story 7.6 admitted `'dead_money'` as a fourth `RosterSlotKind` whose whole meaning is that the Player is NOT on the Team — only his money is — which the new migration's own column comment states ("occupying no Roster Slot"). This is the SQL-layer analogue of the AD-32 exhaustiveness hazard: TypeScript cannot reach inside a query string, so none of the compile errors that found the other consumers found this one. Confirmed by sweep — it is the only unfiltered `team_rosters` read that draws a conclusion; the two selects in `server/team-roster.ts` project `roster_slot_kind` and their counting loop ignores Dead Money correctly. **Zero impact today**: no code path can write a `dead_money` row until 7.8 ships. Deferred rather than patched because whether a dropped Player becomes nominatable again is a product decision FR-43 does not settle, and Story 7.6's spec explicitly scoped the Drop path to 7.8.
  owner: Story 7.8 (Record a Drop) — must resolve before it writes its first Dead Money row.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-6-dead-money-and-the-rookie-scale-designation.md`
  summary: The rookie-scale round captured by `CONTRACT_END_YEAR` is unbounded — `9RK31` and `0RK29` parse to rounds 9 and 0, though the NBA draft has two rounds.
  evidence: `src/lib/adapters/fantrax/roster-file.ts:133` is `/^(?:(\d)(?:RK|rk))?(\d{2}|\d{4})$/`; verified by probe that rounds 0 and 3-9 are accepted and reach `ParsedRosterRow.rookieScaleRound`. Acceptance is UNCHANGED from before Story 7.6 — the pre-story regex matched the same digit and merely discarded it — so this is not a regression the story introduced; what changed is that a nonsense round now becomes structured data a later story reads. Not patched because tightening the regex would narrow what the importer accepts, a behaviour change no acceptance criterion asked for and one that would land on a `core/`-adjacent deploy path governed by AD-20. Consequence is low: FR-43's exception tests for round **2** specifically, so an out-of-domain round falls through to the safe default of being treated as an ordinary contract.
  owner: Story 7.8, or whoever next revisits the Fantrax roster adapter.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-7-record-a-roster-move.md`
  summary: Consolidate the three exact-dollar money renderers into one.
  evidence: Story 7.7 added `formatExactDollars` to `core/money.ts` for refusal prose. Two older private spellings remain — `teams-index.ts`'s grouped `exactDollars` and the ungrouped off-grid branch of `import-preview.ts`'s `renderCapSpace`. Folding them in changes rendered output on two shipped surfaces, so it needs its own story rather than a drive-by.
  owner: Whoever next revisits money rendering; no story depends on it.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-the-league-visible-audit-log.md`
  summary: The Audit Log reads, renders and serialises the entire event log on every page view and every export, with no bound.
  evidence: `event-log.ts:98-118` already warns that this read grows with the log and names the remedy — a bounded read in that module. Story 7.5 renders every row twice per request (once with no references to harvest party ids, once for real) and the export repeats the whole cost per download. Accepted for now because every page in the app already folds the full log and filters are the specified remedy; pagination was on the spec's Ask First list and was not taken. The fix belongs to the reader, not to this surface.
  owner: Whoever adds the bounded read to `server/event-log.ts`; no story depends on it yet.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-the-league-visible-audit-log.md`
  summary: The repo has no Prettier config and no Prettier dependency, so any agent running `npx prettier` silently reformats files against house style.
  evidence: During Story 7.5 an implementation agent ran `npx prettier --write` on new files; with no config present it pulled defaults and rewrote them to double quotes and 2-space indent, against the repo's tabs and single quotes. It was caught and reverted in-session and no pre-existing file was touched, but nothing in the repo would have failed on it — there is no formatter gate in `npm run check` or in the test suite. Either add a `.prettierrc` matching house style or a check that fails on drift.
  owner: Unassigned; a repo-hygiene change, not a story.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-8-record-a-drop.md`
  summary: A fixed gate set's one-edit property holds when a name is ADDED but not when one is removed — true of `PLACE_BID_GATES`, `RESTORE_LEADING_BID_GATES`, `RECORD_ROSTER_MOVE_GATES` and now `RECORD_DROP_GATES` alike.
  evidence: Each `*GateResults` type is declared independently of its frozen name list, so adding a name breaks `every((gate) => gates[gate].passed)` at compile time, while removing one leaves the orphaned key populated and silently unchecked. Story 7.8's spec instructed "copy this shape exactly" from the Move, so the hole was inherited rather than introduced. The fix is one shared derivation (`Record<(typeof GATES)[number], …>`) applied to all four command types at once, which is a cross-cutting change no single story should make alone.
  owner: Unassigned; belongs with whoever next touches `core/types.ts`'s gate declarations.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-8-record-a-drop.md`
  summary: The Drop and Move pickers mark a won Contract inline but give no warning for a Player contested by an open Auction or nomination, though both are refused identically at commit.
  evidence: `routes/roster-drop/+page.server.ts`'s `pickerRowsFor` carries `won` through to the markup; nothing carries the contested state, which the core checks first and refuses on. A Commissioner can tick a contested Player and only discover it after building the whole act. Not a correctness issue — the server refuses — but it costs a round trip on the surface whose entire design goal is stating consequences before commit.
  owner: Whoever next touches the Commissioner override surfaces; applies to `roster-move` equally.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-8-record-a-drop.md`
  summary: `import-preview.ts` now selects `rookie_scale_round` but nothing renders it, so a Commissioner cannot see a rookie-scale designation before promoting an import.
  evidence: Story 7.8 added the column to the PostgREST select and to `StagedRosterRow` so the preview's row shape matches the table, but added no surface for it. Either render it in the preview — it is the one fact that decides whether a future Drop clears or carries — or narrow the select back to what the preview actually shows.
  owner: Whoever next touches the import preview.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-8-record-a-drop.md`
  summary: No Commissioner override route proves the reason sheet was shown for the exact selection being committed.
  evidence: Both `roster-move` and `roster-drop` read the selection from the query string and the reason from the posted form, with nothing binding the commit to a preceding preview step — a direct POST with a reason is evaluated and can succeed. The gates and `requireOverrideReason` still run, so nothing unlawful commits; what is missing is the guarantee that the before/after the Commissioner saw is the one they committed. Applies to every destination built on Story 7.1's sheet.
  owner: Whoever next touches `server/override-guard.ts` or the sheet.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-8-record-a-drop.md`
  summary: User-facing copy and docblocks across the codebase refer to every Player as "he"/"his".
  evidence: These are real league players whose gender the app does not know and has no reason to assert. Story 7.8 removed the pronouns from the new reason-sheet copy it added, but the convention is repo-wide — `roster-move.ts`, `contracts.ts`, `team-view.ts` and the PRD itself all carry it. A sweep is a single mechanical pass, but it touches shipped surface strings and so needs its own change rather than riding along with a rules story.
  owner: Unassigned; repo-wide copy change.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-11-rearrange-a-roster-s-slot-placements.md`
  summary: FR-44's "refused while the auction is paused" (AD-13) is not implemented, because no pause state exists anywhere in `src/`.
  evidence: There is no `paused`, no `pausedAt` and no pause guard in the codebase — Story 7.4 owns that state and is still `backlog`, and the epic explicitly does not make 7.11 depend on it. Inventing a pause flag here would decide 7.4's design in a story that cannot test it against a real pause. `/roster-move` therefore has `requireLiveDestination` and `requireOverridablePhase` and no pause refusal, and a Move placed during a pause would currently commit. Story 7.4 must add its guard to every act route it finds, `/roster-move` included, with the distinct pause wording FR-44 requires.
  owner: Story 7.4; blocking for FR-44's pause criterion only.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-7-11-rearrange-a-roster-s-slot-placements.md`
  summary: §10 example 45's Maximum Bid figures assume a prospective Bid that `evaluateActCap` deliberately does not project, so the app computes $17,000,000 where the PRD prose reads $18,000,000.
  evidence: `rules/roster-act.ts`'s `evaluateActCap` calls `teamSolvencyFiguresFor` with `prospectiveBidIsExempt: true` — a roster act places no Bid, and `false` would refuse §10 example 39's Team F over an addition nobody asked for. `projectedAdditionsFor` then counts Active/Bench Overflow and no prospective `+ 1`, giving `12 − (11 + 0) = 1` free Slot and a $1,000,000 Roster Reserve after the demotion. The PRD's prose reaches `12 − 12 = 0` by counting `rosterCount + 1` for "bidding once" and no overflow term; the two models agree on the before figure ($7,000,000) by coincidence and diverge by exactly one Slot's reserve after. The gain is therefore $10,000,000 in the app and $11,000,000 in the prose. The DIRECTION — Cap Space spent, Maximum Bid risen — is identical, which is the claim the example exists to make, and `tests/examples/example-45-…` asserts it. Changing the figure would mean changing `evaluateActCap`, which Story 7.11's spec put on Ask First. Either the PRD's arithmetic should be restated in the roster-act model, or `evaluateActCap` genuinely should project a prospective Bid for a sheet's Maximum Bid display — a decision for whoever owns the shared evaluator.
  owner: Whoever next touches `core/rules/roster-act.ts` or PRD §10 example 45.
  resolved: 2026-09-12, in Story 7.11 itself, and the diagnosis above is superseded in one respect. The two models do NOT merely differ after the act: the PRD's BEFORE figure is wrong on its own terms. It counts the prospective Bid but omits Active/Bench Overflow, which is `1` before the Move — two eligible leads against one free Minor League Slot — and `0` after. So its `$7,000,000` should read `$8,000,000`, and its `$11,000,000` gain is a model mixed halfway through; both self-consistent readings give `$10,000,000`. Human decided the sheet quotes the **Manager-facing** Maximum Bid (the one counting the Bid being placed, FR-12 and PRD §3) rather than the gate's solvency headroom, via a new `managerMaximumBidFor` in `core/rules/roster-rearrange.ts` — a second CALL of `teamSolvencyFiguresFor` with `prospectiveBidIsExempt: false`. `evaluateActCap` is unchanged and stays exempt. PRD §10 example 45, `epics.md` UX-DR41 and the Story 7.11 AC line were corrected in place with dated notes.
  status: resolved

- source_spec: `_bmad-output/implementation-artifacts/spec-7-11-rearrange-a-roster-s-slot-placements.md`
  summary: A refused Roster Trade whose Team leads no open Auction is told to "wait for it to close or void the Bid" in the same breath as being told there is no open Auction.
  evidence: bmad-build review (2026-09-12), Blind Hunter layer, found on the Move and fixed there. `rosterTradeRefusalDetail` (`src/lib/core/rules/roster-trade.ts`) appends `Wait for it to close or void the Bid; the Trade will not cancel it.` after `auctionsInWords(gate)` unconditionally, and `auctionsInWords` returns `It leads no open Auction.` when `leadingAuctions` is empty — a legitimate state, since a Team that leads nothing can still fail the cap gate on Roster Reserve alone (§10 example 37 is the shape). Story 7.11 fixed the identical construction in `rearrangeRefusalDetail` by gating the remedy sentence on `leadingAuctions.length > 0`; the Trade's copy was left alone because it is not this story's code and changing a shipped refusal's wording is a behaviour change the spec put on Ask First. The Drop's `dropRefusalDetail` should be checked for the same pattern at the same time.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-11-rearrange-a-roster-s-slot-placements.md`
  summary: `/roster-move` reads the whole Team list and the whole event log on every GET step, including the two steps a Manager's own-Team flow never uses the Team list for.
  evidence: bmad-build review (2026-09-12), Blind Hunter layer. `previewRosterRearrange` (`src/lib/server/roster-rearrange.ts`) always runs `TEAMS_SQL` plus `loadEventsViaClient` and the roster read, then the route discards the Team list for a Manager (`teams: commissioner ? preview.teams : []`). A Manager's flow is two GET steps plus the sheet, so the Team-list query runs three times per Move and is never rendered. Harmless at thirty Teams and one auction's worth of log, and it is the same unbounded full-log read `deferred-work.md` already logs against `loadEventsViaClient` on other read paths — the fix is shared with that entry rather than local to this route.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-11-rearrange-a-roster-s-slot-placements.md`
  summary: FR-44's Manager half is built and gated off — `/roster-move` shipped Commissioner-only by operator decision, so a Manager cannot record a Move on their own Team.
  evidence: Operator decision, 2026-09-12, after Story 7.11 landed. FR-44 gives a Manager the Move on their own Team and makes the Commissioner's on-behalf version the exception; Story 7.11 built both halves. The Manager half is now unreachable because `destinations.ts` marks `roster-move` `commissionerOnly` in both the Auction and Contract Assignment lists, and `requireLiveDestination` resolves through that same filter — so a Manager is refused server-side on `load` AND on the action, not merely shown no menu row. Nothing was deleted: the session-resolved Team, the `FOREIGN_TEAM_REFUSAL` guard, `confirmSheetView`/`ConfirmSheetView`, `ManagerSheet.svelte` and the route's Manager branch are all still present, and `tests/routes/roster-move.test.ts` asserts each is still there plus that a Manager is refused today. **Re-enabling is two edits**: set the `commissionerOnly` argument to `false` on the `roster-move` entry in both phase lists in `src/lib/server/destinations.ts`, and move the id from the `commissionerOnly` array to the `manager` array in both phases in `tests/destinations.test.ts`. The behavioural Manager-branch tests were deleted rather than skipped — a skipped test asserting a refused path rots — so re-enabling should restore them from git history at `428d515`.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-11-rearrange-a-roster-s-slot-placements.md`
  summary: The `pause-resume` and `operational-health` destinations were removed from the Auction catalog because neither had a route behind it.
  evidence: Operator decision, 2026-09-12. Both were catalog rows added against `EXPERIENCE.md`'s Information Architecture table ahead of their surfaces, and `src/routes/` holds no directory for either — so every click was a 404, which is worse than an absent row. Story 7.4 owns Pause/Resume and Epic 8 owns Operational health; each should add its row back in the same change that adds its route, and `destinations.ts`'s module header now says so. `EXPERIENCE.md` still lists both and was NOT edited — the table is the target state, and the catalog is what has shipped.

