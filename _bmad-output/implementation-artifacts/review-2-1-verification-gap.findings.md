## Verification Gap Review — Story 2.1 diff

### The nomination submit's disable/confirm gate is verified only by source-text matching, never executed

- **Changed surface:** `src/routes/nominate/+page.svelte:75` — the `blocked` derived value: `const blocked = $derived(!pool.slotAvailable || chosen === null || !confirmed);`, which gates the submit button at `src/routes/nominate/+page.svelte:1634` (`disabled={blocked}`).
- **Impacted consumer or site:** the Nominate submit `<button>` at `src/routes/nominate/+page.svelte:1631-1638`, the client-side guard meant to keep the two-part act (choose → confirm → submit) from collapsing into a one-click submit.
- **Existing test evidence:** `tests/routes/nominate.test.ts`'s `describe('the nomination surface', ...)` block, specifically `it('disables the submit from one flag and always states the reason, by a stable id', ...)` (around line 2957), only asserts `expect(SOURCE).toContain('disabled={blocked}')` against the raw file text read via `readFileSync` — it never evaluates the `blocked` expression. No test anywhere renders `+page.svelte`: `vite.config.ts` sets `environment: 'node'`, and a repo-wide grep for `render(`, `@testing-library`, `jsdom`, `happy-dom` turns up no component-test harness. `_bmad-output/implementation-artifacts/deferred-work.md:41-42` documents this harness gap as repo-wide, and `deferred-work.md:186-187` already records the identical gap for the sibling `/auction-open` page's own `blocked` derivation (`!report.ready || !confirmed`), with the same style of demonstration.
- **Missing verification:** no assertion evaluates the `blocked` expression itself — nothing checks that dropping or weakening the `!confirmed` term (or the `chosen === null` term) changes whether the button is disabled.
- **Demonstration:** change line 75 to `const blocked = $derived(!pool.slotAvailable || chosen === null);`, dropping the confirmation requirement. Every assertion in `tests/routes/nominate.test.ts` still passes: `disabled={blocked}` and `bind:checked={confirmed}` are both still literally present in the source, and no assertion reads the boolean operator that combines them. The server-side action (`src/routes/nominate/+page.server.ts:1355`, `form.get('confirm') !== 'yes'`) still independently refuses an unconfirmed submit, so no bad write ships — but the client-side "must confirm before the button enables" guarantee would silently regress.
- **Consequence:** a future edit that weakens or reorders a term in `blocked` (or flips `||`/`&&`) enables the submit button under the wrong client-side condition, and nothing in the test suite would fail — only a manual browser check would catch it.
- **Suggested test shape:** the same remedy already proposed for the sibling gap (`deferred-work.md:187`): extract `blocked` and the availability-sentence branching into a pure, non-Svelte helper `(slotAvailable, chosen, confirmed) => boolean` that a vitest unit test can call directly with each combination, mirroring how `destinations-view.ts` already splits view logic out of `DestinationsList.svelte`.