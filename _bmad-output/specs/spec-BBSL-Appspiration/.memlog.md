---
topic: BBSL Offseason Free Agent Auction — v1 web app running the league's open ascending auction end to end
updated: 2026-09-10T10:06
---

- (decision) Slug inherited as BBSL-Appspiration from prd-BBSL-Appspiration-2026-08-16 and architecture-BBSL-Appspiration-2026-08-16; create operation, no prior memlog.
- (decision) PRD, addendum, and ARCHITECTURE-SPINE listed as adopted companions, not sources: all three are upstream-owned and downstream must still read them (FR consequences, section 10 worked examples, AD-1..AD-27, CSV column shapes). Duplicating 700+ lines into spec-authored companions would guarantee drift.
- (decision) sources[] left empty — nothing was fully absorbed with no remaining downstream value.
- (decision) Capabilities cut at feature-group granularity (roughly PRD section 4 groups), not one per FR: 36 FRs would bury the kernel. FR-level detail stays in the adopted PRD, cited per capability.
- (decision) Three operational capabilities lifted from the spine that the PRD never states as capabilities: time-compressed replay rehearsal (AD-3), external liveness and quota monitoring (AD-19), offsite export with rehearsed restore (AD-21). Each is mandated build work with a testable success, not a constraint.
- (decision) Kernel Constraints holds ~12 spec-level decision-benders and cites ARCHITECTURE-SPINE for AD-1..AD-27 in full, rather than restating 27 ADs in the kernel.
- (decision) One spec-authored companion: traceability.md, mapping CAP-N to FRs, ADs, and section 10 examples. Tables belong in a companion per Spec Law.
- (capability) CAP-1 Fantrax import and validated auction open (FR-1, FR-2, FR-3): hard validation, per-team preview, refuse to open on bad import, re-runnable until open.
- (capability) CAP-2 Identity, team binding, commissioner role (FR-4, FR-5, FR-6): Discord OAuth, pre-registered accounts only, no email ever, 30-day sessions, per-manager attribution, Discord-independent commissioner sign-in.
- (capability) CAP-3 Nomination lifecycle (FR-7 to FR-10): one slot per team, released only at Auction Close, nomination obliges no bid, dead nominations made loud.
- (capability) CAP-4 Maximum Bid computed and displayed per-auction with components broken out (FR-12, FR-25), recomputed within 1s of any state change.
- (capability) CAP-5 Bid placement with server-side refusal showing the arithmetic (FR-11, FR-13, FR-15); no retraction.
- (capability) CAP-6 Capital commitment and release including Minors Exposure and unbounded eligible bidding (FR-14, FR-35).
- (capability) CAP-7 Auction Clock and Auction Close with automatic Slot Placement (FR-16, FR-21); close within 60s of expiry with nobody watching.
- (capability) CAP-8 Minimum-Bid Contention lifecycle — enter, join, dissolve, auditable draw (FR-17 to FR-20).
- (capability) CAP-9 League Clock and Auction Phase end (FR-22); reset by nomination and accepted bid only.
- (capability) CAP-10 Live Bid Board, auction detail, and team views (FR-23 to FR-25), mobile-first, updating without refresh.
- (capability) CAP-11 Discord broadcast and per-manager mention notification (FR-26, FR-27); delivery never reverses auction state.
- (capability) CAP-12 Contract assignment against Year Allotment with completion tracking and deadline default (FR-28, FR-29).
- (capability) CAP-13 Reviewed Fantrax export of contracts and full rosters, plus archive (FR-30, FR-36, FR-31).
- (capability) CAP-14 Commissioner overrides, pause/resume, and league-visible append-only audit log (FR-32 to FR-34).
- (capability) CAP-15 Replayable auction against a synthetic clock — time-compressed rehearsal against a fake 30-team league (AD-3). Required capability, not an optional test.
- (capability) CAP-16 Liveness and free-tier quota monitoring from outside both vendors and not via Discord (AD-19).
- (capability) CAP-17 Continuous offsite export of the event log plus the reference data needed to fold it, with a restore rehearsed before the auction opens (AD-21).
- (constraint) Rule correctness outranks throughput, latency and elegance: a rules bug is a worse failure than an outage (PRD section 5).
- (constraint) Functional core / imperative shell with an event-sourced auction domain; core is pure, one source location, two runtimes, no fork (AD-1, AD-2).
- (constraint) State mutation serialized globally by one advisory lock of one fixed arity, not per-auction — the team is raced, not the auction (AD-6, supersedes PRD section 5 as originally written).
- (constraint) Derived money quantities are never persisted and are evaluated against post-bid hypothetical state (AD-7).
- (constraint) Money is integer dollars, branded at every runtime boundary; winning amount and Cap Hit are distinct persisted fields (AD-8, AD-23).
- (constraint) auction_events is insert-only for every role including the commissioner; corrections append compensating events (AD-4).
- (constraint) Expiry is authoritative for validation the instant it passes, regardless of whether the sweep has recorded the close (AD-12).
- (constraint) Lottery seed is commit-reveal and unreadable by any client-facing role before the draw — the builder is also the commissioner and a competing manager (AD-14).
- (constraint) No client write path; manager-to-team binding resolved server-side, never from JWT metadata (AD-9, AD-15).
- (constraint) Discord is the only outbound transport and the only identity provider; this system sends no email for any purpose (AD-18, FR-4).
- (constraint) PRD section 3 glossary terms are the identifier names verbatim; a synonym is a defect.
- (constraint) Zero budget: stacked free tiers with no SLA, two Supabase projects total, Netlify credit ceiling and Supabase Edge invocation ceiling are both silent-outage sources.
- (constraint) Mobile-first at 375px, WCAG 2.1 AA, touch targets at least 44x44px, auction state never conveyed by colour alone.
- (constraint) No urgency design — countdown pressure, one-tap raises without confirmation, and suggested amounts are rejected (SM-C1); the app is referee, not croupier.
- (constraint) PRD section 10 examples 1-23 are the executable specification; a rule change altering an outcome changes the PRD in the same commit (AD-25).
- (constraint) Stack pinned: SvelteKit 2.70.2, adapter-netlify 6.0.4 with edge false, Node 24 LTS, Deno for the tick, Postgres at least 15.1.1.61.
- (decision) Non-goals lifted verbatim in substance from PRD section 6 plus the explicit rejections in section 7.2 (proxy bidding, overnight freeze, anti-snipe) — rejections bend design and belong in Non-goals, not merely in a backlog.
- (decision) Success signal anchored on SM-1 (zero disputed outcomes) and SM-5 (auction completes and exports first-try), which are the two demonstrable world-change moments.
- (assumption) Fantrax free agent export carries the minor-league-eligibility flag; fallback is a commissioner toggle (PRD FR-2, OQ-2). FR-35 gives the flag economic weight, so a wrong flag is a wrong cap.
- (assumption) Bids are whole multiples of 100,000 dollars — inferred from the 500k increment and 1M minimum, not stated in league rules (PRD FR-11, OQ-4).
- (assumption) Unassigned players default to 1-year contracts at the assignment deadline, as the fallback spending no scarce allotment (PRD FR-29, OQ-3).
- (assumption) Minors Exposure is sized to the largest surplus Eligible Leading Bids, because Slot Placement follows close order and no bidder can steer which win lands in minors (PRD FR-35).
- (assumption) Outbid notices are unmutable, on the grounds that muting them undermines the fairness premise of a 24/7 clock (PRD FR-27).
- (assumption) An unbid nomination holds its team's Nomination Slot for the rest of the auction; confirmed by the league as the accepted consequence of the rules as written (PRD FR-9).
- (assumption) Verifiable randomizer fairness treated as a hard requirement, not a nicety (PRD FR-20).
- (question) OQ-1 — Is there an upper limit on Active/Bench Slots? Needed to cap Roster Count and validate the export.
- (question) OQ-2 — Does a real Fantrax free agent export carry the under-82-games eligibility flag? Check early; FR-35 makes a wrong flag a wrong cap.
- (question) OQ-3 — Should export block until every team assigns, instead of defaulting unassigned players to 1-year deals?
- (question) OQ-4 — Confirm bid granularity is 100,000 dollars, or state the real granularity.
- (question) OQ-5 — Does the auction have a hard calendar end date in addition to the 48-hour League Clock?
- (question) OQ-6 — Do free agents not won require any export treatment, or do they simply remain free agents in Fantrax?
- (question) OQ-7 — Voiding a bid (FR-32) removes the event that last reset the League Clock (FR-22). Is the League Clock recomputed without it? This changes which unbid nominations die at phase end. Unspecified in both PRD and spine; AD-22 pins the reset set but cannot decide this.
- (question) OQ-8 — PRD section 5 targets no more than 1 hour of unplanned unavailability (99.8% over ~504 hours) on stacked free tiers with no SLA and Discord now on the sign-in path. The spine states this is not achievable as written and should be restated as a target with a documented recovery posture. Restate it?
- (question) OQ-9 — The section 5 outage recovery procedure (how a >15-minute outage maps onto pause plus a compensating clock adjustment) must be written before the auction opens. Who writes it and when?
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 and 8): PASS. All 17 capabilities carry intent and success; CAP IDs 1-17 unique and first-issued; 11 non-goals; success signal is demonstrable (contested-result count, override count under 5, first-try export). Noted weakness accepted: CAP-2's intent names Discord OAuth and CAP-17's names Supabase, which reads as HOW — both are decisions of record carried as constraints, not implementation prescription.
- (event) Self-validate pass 2 (preservation): PASS after two fixes. Every FR-1..FR-36 lands in CAP-1..CAP-14 and every AD-1..AD-27 is bound by a kernel constraint or listed in traceability.md. Two PRD section 5 NFRs had been dropped and were added as constraints: the performance floor (2s views, 1s bid ack, 1s Maximum Bid recompute, 5s board propagation) and 'auditability over convenience' as the design tiebreaker.
- (event) Wrapper-only content dropped deliberately: PRD section 0 document-purpose narrative, section 2's user journeys UJ-1..UJ-4 and jobs-to-be-done prose, section 9 risk rationale, addendum section A rejected-alternatives research and section G deferred-feature rationale. All remain readable in the adopted companions; none is load-bearing for a build decision the spec does not already carry.
- (direction) Meakel answered all nine open questions 2026-08-17. Resolutions to be applied across PRD, addendum, ARCHITECTURE-SPINE, SPEC and traceability.
- (decision) OQ-1 RESOLVED: Active/Bench capacity is exactly 12 slots (8 active + 4 bench), a hard ceiling, not merely a minimum. New rule FR-37: a Bid is refused when Roster Count + Projected Active/Bench Additions would exceed 12. No carve-out for eligible overflow — Meakel chose the hard ceiling over the overflow-exempt variant.
- (decision) FR-37 corollary: a Team at Roster Count 12 cannot bid on any non-eligible Player at all, and can bid on a Minor League Eligible Player only while a Free Minor League Slot absorbs him. This is reachable straight from import, so it must be stated on the board rather than discovered.
- (decision) FR-37 forces an edit to PRD section 10 examples 18-20: Team P's Roster Count changes from 12 to 11. At 12 the new ceiling refuses example 20's bid on capacity, contradicting its stated 'permitted' outcome. Verified at 11 all three examples keep their original lessons: 18 permitted (unbounded, reserve 1M coverable), 19 refused on Minors Exposure alone with capacity passing at 11+1=12, 20 permitted at 11+1=12 with exposure 1M against 2M room.
- (decision) FR-37 adds an upper bound to the FR-30 export check. Combined with the existing 'block if Roster Count below 12', every Team must end the auction at exactly 12 Active/Bench players.
- (decision) OQ-2 RESOLVED: the Fantrax export does NOT carry the eligibility flag. Minor League Eligible becomes Commissioner-set app data via new FR-38, set before auction open. Import defaults every Player to NOT eligible — the conservative direction, since defaulting to eligible would hand out unbounded bidding by omission. FR-3 reports the flagged count for confirmation but does not block, because nothing external defines 'complete'.
- (decision) OQ-3 RESOLVED yes: export blocks until every Team assigns. FR-29 loses its 1-year auto-default entirely; the deadline becomes a nag, and a Team that misses it is resolved by Commissioner assignment under FR-32 with a logged reason — which is exactly what UJ-4's edge case already described.
- (decision) OQ-4 RESOLVED: bid granularity is 500,000 dollars, not 100,000. Granularity and Minimum Increment now coincide, so no grid-valid Bid can be sub-increment and the 1M-to-1.5M lottery dead zone contains no grid-valid amount at all. Both are now reachable only by an off-grid submission. PRD section 10 examples 2 and 10 keep their amounts and outcomes; their stated refusal grounds gain the granularity ground.
- (decision) OQ-5 RESOLVED: no hard calendar end date. The League Clock is the only terminator.
- (decision) OQ-6 RESOLVED: free agents not won simply remain free agents in Fantrax and need no export treatment. Stated explicitly in FR-30 so nobody builds an unwon-players export.
- (decision) OQ-7 RESOLVED yes: voiding a Bid recomputes the League Clock without that Bid's reset. AD-22's reset set becomes a fold that treats a voided Bid as not-a-reset.
- (assumption) Derived from OQ-7 and not directly confirmed: when the recomputed League Clock is already past, the Auction Phase ends at the next tick evaluation and does NOT retroactively invalidate Nominations or Bids accepted in the interim. The alternative — retroactive invalidation — contradicts AD-4's never-mutate-history and AD-12's late-not-wrong posture.
- (decision) OQ-8 RESOLVED yes: PRD section 5's availability NFR is restated as a target with a documented recovery posture, dropping the unachievable 99.8 percent figure. The spine's 'owned by no AD' conflict is thereby closed.
- (decision) OQ-9 RESOLVED: the outage recovery procedure will be written as scheduled work inside the planned epics rather than up front. It stops being an open question and becomes a tracked deliverable blocking auction open.
- (decision) Four new worked examples added to PRD section 10 (24-27) covering the rules that arrived with these answers: roster-full refusal, roster-full with an eligible absorb versus an eligible overflow, off-grid bid refusal, and League Clock recomputation on a bid void. AD-25's suite becomes examples 1-27.
- (capability) CAP-18 Commissioner-set Minor League Eligibility (FR-38), defaulting to not eligible so omission fails safe.
- (capability) CAP-19 Roster Capacity enforcement (FR-37): Roster Count + Projected Active/Bench Additions at most 12, a second refusal ground independent of Maximum Bid.
- (event) Applied the answers across four documents: prd.md (status callout, glossary, FR-1/2/3/11/12/22/24/29/30/32/35 amended, FR-37 and FR-38 added, section 5 availability restated, section 9 risks, section 10 examples 2/10/18/19/20 amended and 24-27 added, section 11 rewritten as resolutions, section 12 reindexed), addendum.md (section B eligibility column removed, open action narrowed), ARCHITECTURE-SPINE.md (binds FR-1..FR-38 and examples 1-27, AD-7 second rejection ground, AD-22 League Clock fold, AD-24 eligibility not a Fantrax concern, AD-25 examples 1-27, two upstream conflicts closed, deferred list updated, capability map, vocabulary and config conventions, source tree comment), SPEC.md and traceability.md re-derived.
- (event) Self-validate pass 1 (coherence) after the update: PASS. 19 capabilities, all with intent and success; CAP-18 and CAP-19 took the next unused ids rather than renumbering; Open Questions section removed as empty per template; one live inference retained in Assumptions (prospective-only League Clock recomputation).
- (event) Self-validate pass 2 (preservation) after the update: PASS. FR headings count 38, section 10 examples verified contiguous 1-27, every FR maps to exactly one realizing capability in traceability.md. Stale-reference sweep across all four documents found one hit — the spine's source-tree comment still read 'examples 1-23' — now corrected. Remaining 100k and 1-year-default mentions are deliberate retrospective wording in resolution notes, plus historical review files which are process records and were left untouched.
- (question) Story Breakdown not yet run. Offered to Meakel; stories.yaml does not exist.
- (change) CAP-1 IMPORT CORRECTED 2026-08-18. The two-file import assumption is retired. CAP-1 previously read 'the auction refuses to open unless BOTH imports are committed'; the real Fantrax workflow produces thirty-one exports -- one Free Agent pool plus one per Team. CAP-1 now specifies: batch supply of all 31, independent per-file resolution (a file matching no Team or an already-supplied Team is refused naming the FILE, not a row), per-file parse status, outstanding Teams NAMED not counted, single-Team re-supply without redoing the other 29, per-Team preview across all 30 before an all-or-nothing commit, and an open gate requiring the pool import plus all thirty Team imports. PRD FR-1 and FR-3 corrected to match, so the contract does not self-contradict. Surfaced by UX journey work (bmad-ux, 2026-08-17) when the Commissioner narrated opening night.
- (constraint) ARCHITECTURE SPINE REFRESHED 2026-08-18: 27 ADs to 30. New: AD-28 import is staged in Team-keyed (plus pool-keyed) staging tables and promoted in ONE all-or-nothing transaction, so a half-imported League is never reachable and 31 files survive a refresh on setup day; AD-29 read freshness is explicit -- every projection read carries one global max-seq watermark, the client derives Live/Reconnecting/Stale from connection status AND a positive periodic liveness check, and in anything but Live money carries its age or the control it would authorise disables; AD-30 League phase and viewer role resolve server-side per request into ONE destination list rendered twice, and a destination not live in the current phase is not rendered and its route refuses server-side. The binding clause in Constraints moves from 'AD-1 through AD-27' to 'AD-1 through AD-30'.
- (constraint) CORE CONTRACT WIDENED (AD-1 amended). The core now exposes evaluate(state, command, now) -> GateResults returning EVERY gate's outcome with its own arithmetic, and decide() returns Rejected<GateResults> rather than a singular reason. decide() obtains its outcomes by calling evaluate(); the read path calls evaluate() directly to disable controls with stated reasons and render the Maximum Bid breakdown. One evaluator, two consumers. Forced by the refusal reporting the gate that PASSED alongside the one that refused -- the alternative put a fragment of the Roster Capacity rule in the view layer, which is the second implementation the one-core constraint exists to forbid. Neither gate short-circuits the other.
- (constraint) AD-29 CRITICAL NUANCE for this league specifically: a stalled watermark must NEVER imply staleness. The auction runs as a relay of timezone clusters with long genuine silences -- six quiet hours at 4am Pacific is a healthy auction, which is why the League Clock is 48 hours. Liveness asks 'can I still reach the server?', never 'has anything changed?'. Verified 2026-08-18 that Supabase Realtime reports SUBSCRIBED/CHANNEL_ERROR/TIMED_OUT/CLOSED on any status change, but that a channel can report SUBSCRIBED while delivering nothing (supabase/realtime#1414), so connection status alone is insufficient in both directions.
- (constraint) MONEY RENDERING BOUNDARY (AD-8 amended): the abbreviated one-decimal rendering ($14.5M) is correct in the UI AND in Discord payloads -- same figures, same readers -- and is FORBIDDEN in CSV export cells, where it would silently corrupt the Fantrax round-trip. Exports emit exact integer dollars. Lossless only because every BBSL figure sits on the $500,000 grid.
- (decision) ADOPTED COMPANIONS: DESIGN.md and EXPERIENCE.md from the UX run (planning-artifacts/ux-designs/ux-BBSL-Appspiration-2026-08-17/, both status final 2026-08-17) added to companions:. They are adopted, not spec-authored -- bmad-ux owns them and bmad-spec never edits them. Downstream must read them for the visual identity contract (tokens, measured contrast, component anatomy) and the behavioural contract (IA, states, key flows) that the kernel cites but does not restate.
- (event) Self-validate pass 1 (coherence) after the 2026-08-18 update: PASS. 19 capabilities, 19 intents, 19 successes -- no capability id changed, none added, none renumbered. Five new constraints added and all five rule something out: the widened core contract, one-evaluator-two-consumers, the money rendering boundary, staged-then-promoted import, freshness-with-disabling, and phase/role surface gating. Non-goals and Success signal unchanged and still concrete. Binding clause moved to AD-1 through AD-30; no stale AD-27 range reference survives in SPEC.md or traceability.md.
- (event) Self-validate pass 2 (preservation) after the 2026-08-18 update: PASS with one gap found and closed. Walked the ten spine changes: AD-28, AD-29, AD-30, amended AD-1, AD-7, AD-8 and AD-24 all landed in Constraints or a capability success. CAP-10's success gained the landing surface and the freshness obligation; CAP-5's gained the both-gates refusal. GAP FOUND: amended AD-15's non-enumerating sign-in refusal and the expired-versus-never-existed distinction had not landed -- added to CAP-2 success. DESIGN.md and EXPERIENCE.md adopted as companions, and traceability.md gained a 'UX contract by capability' table so a builder knows which capabilities must read them rather than the kernel alone.
- (event) spec re-derived 2026-08-18 against the 30-AD spine and the final UX spines
- (event) Update run opened 2026-09-08, driven by the approved Sprint Change Proposal 2026-09-07. The PRD, architecture spine, UX spines and epics were all amended earlier the same day; this spec was the last artifact still carrying the pre-allowance rule, found by the sprint-planning readiness gate rather than by the correct-course impact analysis -- which listed the SPEC in scope but never routed a step to it.
- (decision) CAP-19 (Roster Capacity) AMENDED, id preserved. Its rule is no longer 'Roster Count + Projected Active/Bench Additions <= 12'. A Bid now passes when Projected Active/Bench Additions = 0, OR the Team holds at least one Free Active/Bench Slot and Projected <= Free + 1 -- the Outstanding Bid Allowance, which exists so a Manager can chase two Players with one open slot rather than idling a day for a close. The 12-slot ceiling is unchanged; the allowance is a bidding permission, never a thirteenth Slot. The free-slot PRECONDITION is load-bearing: without it a Team at Roster Count 12 holds one bid, wins it, and lands on 13 with no other close available to cancel it.
- (capability) CAP-21 ADDED (next unused id; CAP-1..CAP-20 preserved, none renumbered) -- Cancel a surplus commitment and restore the Auction. Realizes FR-40. Intent: when an Auction Close leaves a Team holding commitments with no Roster Slot to receive them, the system stands them down and returns each affected Auction to its next-highest bidder. This is the other half of CAP-19's allowance and is not separately shippable: the allowance without the cascade lets a Team win a thirteenth Player.
- (decision) CAP-8 (Minimum-Bid Contention) AMENDED: entries leave the roster-capacity gate entirely. A Team with at least one Free Active/Bench Slot -- or, for an eligible Player, a Free Minor League Slot -- may be a Contender in ANY number of open Contentions; entries contribute nothing to Projected Active/Bench Additions and do not consume the allowance. Cap space is the only quantitative limit. Rationale from the commissioner: the expected outcome of a lottery is losing, so a capacity rule sized as though a Team would win them all throttles the mechanism that clears cheap Players. Safe only because CAP-21's cascade removes a Team from its remaining lotteries before they draw, which AD-11's sequential closing guarantees. Also: the recorded Contender list is the post-cancellation list, and an empty list closes with no winner.
- (constraint) TWO OVERFLOW FIGURES, NOT ONE. Overflow Count (money side) counts Contention entries and feeds Minors Exposure; Active/Bench Overflow (slots side) excludes them and is the only overflow reaching Projected Active/Bench Additions. Forced by CAP-8's exemption: entries leave the capacity gate but stay in Committed Bids. They were one figure from 2026-08-17 to 2026-09-08 and are one subtraction apart, so the natural implementation -- compute once, use twice -- is now wrong in a way no single test announces, over-counting capacity for a Team in lotteries and refusing bids the league allows.
- (constraint) ONE RESTORER, TWO CALLERS (AD-31, new). A cancellation appends one BidCancelled compensating event carrying its own decision -- cancelled seq, cause, and either the restored Team/seq/amount or null -- and the projection READS that rather than re-deriving it by re-running gates inside a fold. close.ts is the sole appender: AuctionClosed first, then each BidCancelled in cascade order; restore.ts decides and appends nothing; restoration is evaluated against post-close state. The selection algorithm lives in ONE pure function consumed by both FR-40's cancellation and CAP-14's Commissioner void, which differ only in three parameters (retain-vs-erase in the fold, leave-vs-restore the Auction Clock, keep-vs-remove the League Clock reset). No migration: event_type is a generic text column.
- (decision) CAP-11 (Discord notification) AMENDED -- the cancellation and restoration mentions are carried by the EXISTING outbid category, adding two clauses to clauseFor in adapters/discord/mention.ts. No fourth category; the mutable set stays exactly slot_release as Story 5.4 left it, and both new mentions are therefore unmutable server-side. Decided by the commissioner 2026-09-08 when the sprint-planning readiness gate found that Epic 10 Story 10.6 asserted 'no fourth category' without saying which one, leaving a developer to invent it. The category name now covers three things that are not literally an outbid -- being outbid, having a Bid cancelled, being restored -- accepted deliberately: it is the 'your position in an Auction changed without you' class, and a Manager must not be able to mute the notice telling them they lost a Player through no act of their own.
- (decision) Smaller amendments carried by the same change: CAP-5 (FR-15) -- no VOLUNTARY retraction; the system may cancel under FR-40 and the Commissioner may void, but the Team still may not. CAP-6 (FR-14) -- capital releases when a Team stops leading for ANY reason including cancellation, and is re-committed for a Restored Leading Bidder. CAP-7 (FR-21) -- the Close applies the cascade inside AD-11's sequential ordering, committed before the next Close is evaluated. CAP-20 (FR-39) -- the Teams index publishes outstanding bids against the allowance, because Roster N of 12 alone now answers the screen's own question wrongly in both directions.
- (event) Self-validate pass 1 (coherence) after the 2026-09-08 update: PASS. 21 capabilities, 21 intents, 21 successes. CAP-1..CAP-20 preserved with no renumbering and no reuse; CAP-21 took the next unused id. Every new constraint rules something out: the two-overflow split forbids compute-once-use-twice, the one-restorer rule forbids a second restoration implementation, the records-its-own-restoration rule forbids re-deriving the leader inside a fold. Non-goals and success signal unchanged and still valid.
- (event) Self-validate pass 2 (preservation) after the 2026-09-08 update: PASS with one gap found and closed. Walked the change claim by claim against SPEC.md. FR-40, the allowance, the free-slot precondition, the cascade, restoration-with-re-validation, the cleared clock, the both-Slot-kinds trigger, the unlimited lotteries, the post-cancellation contender list, the empty-lottery close, AD-31, the outbid-category decision and the two-overflow split all landed. GAP FOUND: four amendments were logged (CAP-5 FR-15 narrowing, CAP-6 release-and-re-commit, CAP-7 cascade-inside-the-sequential-close, CAP-20 bids column) but had not been rendered into SPEC.md -- the log and the artifact would have diverged, which is the precise failure this skill's derive-from-log discipline exists to prevent. All four applied before presenting.
- (event) spec re-derived 2026-09-08 against the 31-AD spine, the FR-1..FR-40 PRD and the amended UX spines. 21 capabilities. traceability.md updated: CAP-21 row added, CAP-19/CAP-8/CAP-7 rows amended, referenced-documents line moved to FR-1-FR-40 / examples 1-35 / AD-1-AD-31.
- (capability) CAP-22 ADDED (next unused id; CAP-1..CAP-21 preserved, none renumbered) -- Record a Roster Move or a Drop. Realizes FR-41 and FR-43. Intent: when managers trade or drop players in Fantrax during the auction, the Commissioner records the resulting roster state and every affected Team's cap and slot position recomputes from it, so the app's arithmetic never silently diverges from the league of record. A Move names two Teams and moves Contracts in BOTH directions as ONE act with ONE gate evaluation at the end -- a side may be empty (a salary dump is a Move), and applying halves in sequence refuses a legal trade on a state that never existed. Slot Placement is re-evaluated on receipt and Cap Hit FOLLOWS placement, so a stashed Player can arrive charging his full amount.
- (capability) CAP-23 ADDED -- Detect a Roster Divergence from Fantrax. Realizes FR-42. Intent: the app notices a trade or drop nobody reported, rather than computing against a roster that stopped being true. getTeamRosters read at most hourly, compared excluding Players won in this auction. MEMBERSHIP IS THE ONLY FACT TAKEN -- the endpoint carries no salary and the app needs none, holding every Cap Hit already. Marked CONTINGENT in the spec: it depends on an undocumented endpoint never exercised against BBSL, and if it does not answer usably CAP-23 is dropped and CAP-22 stands alone. First capability in this spec carrying a contingency marker.
- (decision) THREE NON-GOALS REWRITTEN. (1) 'Not a Fantrax replacement' -- trades removed from the list of things staying in Fantrax; their EXECUTION stays there, their effect on rosters is recorded here. (2) 'Not a draft tool and not a trade machine' -> 'not a trade BROKER': no proposal, negotiation or acceptance flow; committed capital never moves, and a leading Bid, a contention entry and Cap Space cannot be traded. (3) 'Not writing to Fantrax' restated as a PROPERTY OF THE INTEGRATION rather than a scope choice -- verified 2026-09-10, every method in both community wrappers is a GET, so there is no write path to reverse-engineer. v1 now reads one endpoint for reconciliation only; a read can raise a proposal and can never change app state.
- (decision) SUCCESS SIGNAL AMENDED: counts CORRECTIVE Commissioner overrides only, explicitly EXCLUDING Roster Moves and Drops. The prior wording ('fewer than five Commissioner overrides across the whole auction') would have counted a recorded trade as an override, so a trade-heavy offseason read as a failing product. The metric must measure what the app got wrong, not what the league did -- and a metric that cannot tell them apart pressures the Commissioner not to record. Mirrors PRD SM-2, amended the same day for the same reason.
- (constraint) REFUSE, NEVER CASCADE (new constraint, rules out a design): a Roster Move or Drop leaving either Team failing the money or slots gate is refused outright with nothing written. It never cancels a Bid to make room. This forbids extending AD-31's cancellation trigger, which is deliberately bounded to 'an Auction Close and nothing else' to stop cascade re-entrancy -- and forbids an administrative act stripping a Player from a Team that took no action. Also rules out a second affordability implementation: re-evaluation calls the EXISTING pure gates in core/rules/bidding.ts, the same one-implementation-two-callers posture AD-31 takes for the restorer.
- (event) Self-validate pass 1 (coherence) after the 2026-09-10 update: PASS. 23 capabilities, 23 intents, 23 successes. CAP-1..CAP-21 preserved with no renumbering and no reuse; CAP-22 and CAP-23 took the next unused ids. Spec Law 1 holds (both fields on both new capabilities), 2 holds (intents state what, not how -- the migration and the adapter confinement live in constraints and AD-32, not in an intent), 3 holds (the new 'world change refuses, never cascades' constraint rules out three designs: extending AD-31's trigger, an administrative act cancelling a Team's Bid, and a second affordability implementation), 4 holds (non-goals rewritten, still explicit and now narrower), 5 holds (success signal sharpened to corrective overrides -- more testable than before, not less), 6 holds, 8 holds. CAP-23 carries a 'contingent' key, the first in this spec; recorded deliberately so a capability that may not survive contact with a real endpoint does not read as settled.
- (event) Self-validate pass 2 (preservation) after the 2026-09-10 update: PASS, no gap. Walked the change claim by claim against SPEC.md and traceability.md. Landed: both capabilities with all their load-bearing rules (bidirectional-one-act, Cap Hit follows placement, the charged-Cap-Hit Drop rule across all three Slot kinds, the 2RK exception, refuse-can-be-caused-by-the-sender, never-cancels, the length-clearing, membership-is-the-only-fact, read-never-writes, stopped-not-silent); three non-goals; the success signal; the new constraint. traceability.md: CAP-22 and CAP-23 rows added, referenced-documents line moved to FR-1-FR-43 / examples 1-43 / AD-1-AD-32, dated update note added, two open items added (the getTeamRosters probe, and how Fantrax exports Dead Money). EXPLICITLY CHECKED for the 2026-09-08 failure mode -- every logged entry (103-107) rendered into the artifact; log and artifact do not diverge.
- (event) spec re-derived 2026-09-10 against the FR-1..FR-43 PRD. 23 capabilities. NOTE: traceability.md now cites AD-32, which bmad-architecture writes in the same session immediately after this run -- verify it exists before treating the citation as sound.
