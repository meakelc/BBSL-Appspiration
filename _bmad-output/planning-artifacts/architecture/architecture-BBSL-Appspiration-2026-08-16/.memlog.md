---
scope: BBSL Offseason Free Agent Auction - single-tenant web app running a 30-team dynasty fantasy basketball auction: nomination, ascending bids with cap enforcement, 24h/48h clocks, minimum-bid lottery, contract assignment, Fantrax CSV round-trip
purpose: build-substrate
altitude: feature
updated: 2026-09-10T10:17
---

- (constraint) Inputs: prd.md (status final, 36 FRs, 6 open questions) + addendum.md (Fantrax research, CSV shapes, timer/concurrency/randomizer mechanism guidance). No prior architecture, no project-context.md, empty repo - true greenfield cold start.
- (constraint) PRD NFR inversion: 'rule correctness is the product; a rules bug is a worse failure than an outage' and 'scale is not a concern - 31 users, ~30 concurrent auctions, a few thousand events over 3 weeks. Do not design for load; design for correctness.' Architecture must optimize for determinism/testability/auditability, never throughput.
- (constraint) Operational envelope is load-bearing, not boilerplate: 24/7 reachability through Auction Phase, <=1h unplanned unavailability total, no single outage >15min, Auction Close within 60s of nominal expiry with no user present and surviving process restarts. Solo builder-operator (bus factor 1).
- (direction) User chose Coaching path; spine is the only deliverable (no deck, no solution design, no work-split view).
- (constraint) Builder context (from user): has shipped Godot projects and live websites; hosts small projects on Netlify; recently set up a mini PC (Ryzen 7 PRO 5850U, 32GB, on residential LAN, also runs llama-swap LLM inference). Weeks until the auction; wants something testable within a few days. Nothing pre-settled.
- (version) Netlify verified 2026-08-16: scheduled functions are cron-format, minute-level granularity floor, with reported jitter (support thread shows 31/29-min drift on a 30-min schedule). Sync function timeout 10s; streaming functions 60s. SSE possible only via Edge Functions (Deno, 50ms CPU cap). Free tier 125k function invocations/mo; Pro is credit-metered (3000 credits/$20 mo, compute 10 credits/GB-hour).
- (version) Fly.io / Railway / Render verified 2026-08-16: Fly smallest shared-cpu-1x 256MB ~$2/mo (dedicated IPv4 +$2/mo, volumes $0.15/GB/mo); Railway Hobby $5/mo + metered, no default hard spend cap; Render Starter $7/mo fixed 0.5CPU/512MB, egress $0.15/GB after an Apr 2026 allowance cut.
- (version) SvelteKit verified 2026-08-16: SvelteKit 3 entered Release Candidate 2026-08-13 (three days ago). Stable line is 2.70.2. DECISION INPUT: do not bind a hard-deadline money auction to a days-old RC.
- (version) node:sqlite verified 2026-08-16: still Release Candidate (Stability 1.2) as of Node 25.7 (Feb 2026), not fully stable. better-sqlite3 is at 13.0.3 and added Node 26 support in the 12.x+ branch (12.6.2 and earlier exclude Node 26). Node 26 is current LTS.
- (version) Litestream verified 2026-08-16: v0.5.6 and v0.5.7 contain a SILENT replication-failure bug (issue #1083, triggered when SQLite reuses WAL space after checkpoint) - no error emitted, backups simply stop. Issue closed via PR #1087 but no confirmed fixed release. Pin v0.5.5 and re-verify a fixed release exists before relying on it. A silent backup failure during a money auction is a catastrophic-class risk.
- (decision) [ADOPTED] Host = Netlify. User's explicit choice after being shown the NFR frictions and an alternative recommendation (managed always-on container + SQLite). Decision stands; architecture adapts to it.
- (version) Supabase Cron verified 2026-08-16: supports SUB-MINUTE schedules (every 1-59 seconds) on Postgres 15.1.1.61+, not just the classic pg_cron 1-minute floor. This removes the FR-21 60s-close objection to a serverless host - the close sweep runs inside Postgres, independent of Netlify's cron and its jitter.
- (version) Supabase Realtime verified 2026-08-16: free tier gives 200 peak concurrent connections and 2M messages/mo (256KB max message). 31 managers is far inside that. Removes the FR-23 objection - live board updates need no polling and incur no Netlify function invocations.
- (constraint) Supabase free-tier GOTCHA verified 2026-08-16: free projects PAUSE after one week of inactivity, and free accounts are capped at two active projects. The 48h League Clock means the project cannot idle a week DURING the auction, so free tier survives the Auction Phase - but it collides with FR-31 (archived auction 'remain viewable by all Managers indefinitely') and with this app's 11-months-dormant shape. Pro is $25/mo/project.
- (question) PRD §5 says 'Bid acceptance must be serialized per Auction.' That is UNDER-SPECIFIED and per-auction serialization is provably insufficient: Maximum Bid depends on the bidding Team's leading positions in OTHER auctions (Committed Bids, Minors Exposure per FR-35), so two concurrent bids by the same Team on two different auctions can each pass a per-auction check and jointly breach the cap. This is exactly addendum §D.3/§D.4. Resolution: serialize globally, not per-auction. Surface back to the PRD.
- (decision) [ADOPTED] Framework = SvelteKit, pinned to the 2.x stable line (2.70.2 as of 2026-08-16). Explicitly NOT SvelteKit 3, which entered RC 2026-08-13. Chosen for server-rendered + progressive-enhancement forms as the default path (matches the server-authoritative posture NFR §5 demands), smallest client bundle for the 2s-on-mobile-data target, and no server/client component boundary to misreason about.
- (version) @sveltejs/adapter-netlify verified 2026-08-16: stable 6.0.4. A 7.0.0-next.0 prerelease exists (2026-06-05) - do not use. Adapter emits Netlify Functions v2 (ESM, Request/Response). Default edge:false deploys to Node-based Functions, which is what we want: the Deno edge runtime is the wrong place for the transactional rules core.
- (decision) [ADOPTED] Supabase free tier throughout. User's explicit choice with the pause behaviour known.
- (constraint) Supabase free tier verified 2026-08-16: NO automatic backups, NO point-in-time recovery, NO SLA. Combined with the chosen free tier this is the single largest data-loss exposure in the design; the architecture must compensate rather than the platform. Mitigating asset: FR-26 already requires every Nomination, Bid, Auction Close and Randomizer draw be posted to Discord, which the PRD itself calls 'a public, permanent record' - i.e. an independent, offsite, append-only copy of the event stream already exists as a requirement.
- (question) FR-31 requires an archived auction remain viewable by all Managers 'indefinitely', but a free-tier Supabase project pauses after one week of inactivity and the app is dormant ~11 months a year. Deferred by user decision; must appear in the spine's Deferred section with this revisit condition, not silently dropped.
- (decision) [ADOPTED] State model = append-only event log as source of truth for the AUCTION DOMAIN ONLY (nominations, bids, closes, draws, overrides, phase transitions), with current-state projection tables rebuilt in the SAME transaction. Imported reference data (teams, players, existing contracts) stays ordinary tables - do not event-source the world. Binds FR-32/FR-33/FR-20 and addendum §D.4. Prevents: an audit log that drifts from reality, a hand-rolled unwind for bid voiding, and cached Minors Exposure.
- (decision) [ADOPTED] Randomizer = commit-reveal. Publish hash(seed) when a Minimum-Bid Contention opens; reveal seed at the draw. Addendum §E flagged it as worth raising; user adopted. Rationale worth preserving: the user is simultaneously commissioner AND author of the app, which is precisely the conflict commit-reveal neutralises.
- (decision) Rules core = a single pure, I/O-free TypeScript module exposing decide(state, command, now, seed) -> Accepted<Event[]> | Rejected<reason, arithmetic>. No Date.now(), no Math.random(), no DB access, no imports outside the TS standard library. Server time and lottery seed are ARGUMENTS. Consequences: all 21 §10 worked examples become dependency-free unit tests; clock skew is impossible by construction; FR-20 reproducibility is definitional; and the module runs unmodified in BOTH Node (Netlify) and Deno (Supabase Edge).
- (decision) Close sweep + notification dispatch run as Supabase Edge Functions triggered by Supabase Cron at ~10s, NOT via Netlify. Forced by arithmetic: a 10s sweep over 3 weeks = 181,440 invocations vs Netlify's 125,000/mo free budget, while Supabase Edge free tier is 500,000/mo. Prevents: blowing the invocation budget before a single page load. Buys: auction closes keep firing during a Netlify outage, decoupling the most damaging failure mode (a missed close silently extending an auction, per SM-1) from the web host.
- (decision) The pure rules core must exist in exactly ONE source location imported by both runtimes. Duplicating it between the SvelteKit app and the Supabase Edge Function is the single most dangerous divergence available in this design - two copies of the cap arithmetic would produce an auction that closes under different rules than it bids under.
- (decision) Money is represented as INTEGER DOLLARS everywhere - bigint in Postgres, integer arithmetic in TypeScript. No floating point at any point in the money path, no decimal library, no cents (bids are whole multiples of $100,000, so dollars are already five orders of magnitude finer than the domain needs).
- (decision) No client-facing database role holds ANY write privilege. Every mutation goes through server-side code using the service role; the browser's anon/authenticated key is read-only, used solely for Supabase Realtime subscriptions to projection tables. Prevents: NFR §5's 'server-side against committed state, not against state the client held' being quietly violated by Supabase's default client-side-write ergonomics, which the platform actively encourages.
- (decision) Global serialization: every state-mutating transaction (bid, nomination, close, draw, override, phase change) acquires the SAME single pg_advisory_xact_lock before reading state. Transaction-scoped, not session-scoped, so it survives Supavisor transaction-mode pooling. Supersedes PRD §5's 'serialized per Auction', which is insufficient - see the open question logged earlier.
- (decision) [ADOPTED] Two Supabase projects: dev (freely wipeable) + prod (never touched by hand), exhausting the free tier's 2-project cap. Schema changes are migration files in the repo applied dev-first; NOTHING is typed into the Supabase dashboard, or prod and dev diverge silently and it surfaces on setup day.
- (decision) [ADOPTED] Rehearsal is an architectural commitment: clock time is injected everywhere, never ambient, so a full auction can be replayed against a synthetic clock at arbitrary speed. This is the only way to exercise the 24h Auction Clock, the 48h League Clock, and close-during-restart before 31 real people are pointed at it.
- (constraint) Supabase email verified 2026-08-16: the BUILT-IN email service is capped at 2 emails per hour and is documented as not for production - unusable for FR-27. Custom SMTP is mandatory. Second gotcha: after configuring custom SMTP, Supabase applies a DEFAULT auth-email rate limit of 30 messages/hour, adjustable only in the dashboard. Setup day sends 31 magic links at once and would hit that cap; it must be raised before the auction opens.
- (constraint) Email volume arithmetic vs free tiers, computed 2026-08-16: FR-27 requires outbid notices, close notices, slot-release notices and dead-nomination warnings. Estimating ~150 auctions x ~6 bids = ~900 outbid emails, plus ~450 close notices, plus slot releases and warnings = roughly 1500-2500 emails across the ~21 day auction. Average ~95-120/day but HEAVILY spiked on busy days. Resend's free tier is 3000/month with a HARD 100/day cap; SendGrid free is also 100/day. A spike day would silently drop mail and break SM-3 ('every outbid manager received a notification', target 100%), which is a PRIMARY success metric.
- (decision) Email provider is bound by REQUIREMENT not vendor: must sustain >=500 messages/day burst with no hard daily cap that silently drops mail. Viable: Resend Pro (~$20 for the one month, 50k emails) or Amazon SES (~$0.30 total at $0.10/1000, but needs domain verification and a production-access request that takes ~24h, so it cannot be left to setup day). NOT viable: any 100/day-capped free tier. Vendor pick deferred to build time; the burst requirement is the invariant.
- (decision) Outbound side effects (Discord webhook, email) go through a transactional OUTBOX: the event transaction writes the intent, a separate dispatcher drains it with an idempotency key. Satisfies FR-26 'at most once per event even if delivery is retried' and 'a delivery failure never blocks or reverses the underlying auction action'. Also absorbs Discord's webhook rate limit (5 req/2s) when a close sweep resolves several auctions at once.
- (decision) Paradigm = functional core / imperative shell, with an event-sourced auction domain. Chosen because it names the one property every hard NFR depends on: the rules are a pure function, and everything that touches the world is a thin shell around it.
- (event) Coaching complete; all load-bearing decisions settled. Proceeding to distill the spine.
- (question) RECONCILE GAP 1: PRD §5 'Measurability' did not land in the spine. It requires every Bid, Nomination, Auction Close and notification dispatch be recorded with enough context to compute the §8 metrics 'without retrofitting instrumentation' - specifically DEVICE CLASS on bids (for SM-4, 'a majority of bids are placed from a phone') and DISPATCH + DELIVERY OUTCOME on notifications (for SM-3, a primary metric at 100% target). In an append-only event model this MUST be designed into the event payload now; adding it later cannot backfill history. Needs an AD.
- (question) RECONCILE GAP 2: addendum §B states 'winning amount and cap hit are not the same column' - a Minor League placement resolves to a $0 Cap Hit while the winning amount stands as the contract value (FR-21, FR-35, §10 example 18). The spine never pins this. Two implementers could trivially conflate them, and conflating them silently breaks both the cap arithmetic and the Fantrax export. Needs an AD or a hard convention.
- (question) RECONCILE GAP 3: PRD §5 performance NFRs ('board and auction views interactive within 2 seconds on mobile data; a Bid submission acknowledged within 1 second') are not acknowledged anywhere in the spine, despite serverless cold start being a known and accepted residual risk on the chosen Netlify host. The spine should state the accepted degradation rather than leave it silent.
- (event) Reviewer Gate ran 5 parallel lenses. Verdicts: rubric PASS-WITH-FINDINGS, rules-correctness PASS-WITH-FINDINGS (3 critical), operations PASS-WITH-FINDINGS (3 critical), adversarial FAIL (11 critical). Full reviews in reviews/.
- (question) PRD BUG, found independently by the rules-correctness and adversarial lenses - MUST go back to the PRD, cannot be fixed in the spine. §10 example 19 computes 'M = 1' for Team P, but Team P has ZERO occupied Minor League Slots at that moment (the $30M auction has not closed). The §3 Glossary defines Free Minor League Slots M = 3 minus currently-occupied, i.e. M = 3. Example 18 uses M = 3 and example 20 uses the glossary definition correctly; only example 19 diverges. Under the glossary the $1,000,000 bid in example 19 is PERMITTED (N=2, M=3, Overflow Count 0, Minors Exposure $0), but the example states it is REFUSED. The spine declares §10 the executable specification AND the glossary binding verbatim, so the two cannot both be satisfied and the test suite cannot go green. Likely intent: example 19 means M is evaluated against PROSPECTIVE occupancy (slots the team's own open leading bids will consume), which is a different rule than the glossary states.
- (constraint) MY ERROR, caught by the operations lens: the AD-10 invocation arithmetic presented to the user counted only the close sweep (~181K/mo at 10s) and omitted AD-13's outbox dispatcher. Two 10s cron jobs is ~518K/month against Supabase free's 500,000 cap - OVER, and the cap is shared org-wide with the dev project and the AD-3 rehearsal. Exceeding it kills the sweep silently. Fix: ONE cron tick invoking ONE Edge Function that sweeps then drains (~259K/mo), plus dev-project cron disabled by default.
- (decision) Gate-driven amendments to apply: (1) pin advisory lock KEY and ARITY to one named constant - the two-arg and one-arg forms are disjoint Postgres lock spaces; (2) close sweep must process overdue auctions ONE AT A TIME in a pinned deterministic order with slot occupancy carried forward between closes; (3) pin whether Maximum Bid is evaluated against pre-bid or POST-bid hypothetical state (FR-12 says post-bid, AD-6/AD-7 implied pre-bid); (4) fold events by monotonic insertion sequence, never by occurredAt, which is non-monotonic against commit order under the lock; (5) lottery seed must live OUTSIDE the league-readable event log until reveal - three lenses found commit-reveal dies otherwise; (6) qualify AD-5's byte-identical rebuild - folds read mutable reference data; (7) an auction past its close time is CLOSED FOR VALIDATION, refusing late bids independently of whether the sweep has run; (8) sweep observability promoted out of Deferred to an AD - heartbeat + external detector + independent alert channel; (9) an AD governing deploys during a live auction, with the core version stamped on every event and a mismatch fail-stopping the sweep; (10) pin Manager->Team binding server-side, NOT in self-assignable JWT metadata; (11) outbox idempotency key must be (event, recipient, channel) or co-managers lose one of their two required emails; (12) pin int8 deserialization at the runtime boundary; (13) break-glass pause reachable without Netlify; (14) secrets management + read-side RLS; (15) League Clock resets on Nomination and valid Bid and NOTHING ELSE; (16) measurability fields (device class, delivery outcome) in the event payload from day one.
- (version) CORRECTION verified by research lens: Netlify moved from per-invocation metering to a CREDIT model on 2026-04-14. The '125,000 invocations/month' figure no longer exists. Free = 300 credits/month; functions bill at 10 credits/GB-hour and PRODUCTION DEPLOYS COST 15 CREDITS EACH (~20 deploys/month). Credit exhaustion serves 'Site not available' across every project on the account. For a multi-week build plus a 3-week live auction this is a practical blocker during development, not just a risk during the auction.
- (version) CORRECTION verified by research lens: Node 26 is NOT LTS as of 2026-08. It is the Current line and becomes LTS in October 2026. Node 24 is Active LTS and is Netlify Functions' documented default. Spine corrected to Node 24 LTS.
- (constraint) Verified by research lens: pg_cron -> pg_net is FIRE-AND-FORGET - no retry, no alert on 5xx - and a paused or unhealthy Supabase project silently stops every schedule. Reinforces that sweep liveness monitoring must be an AD, not a deferred item, and that the detector must sit outside both vendors.
- (version) Research lens confirmed correct and worth preserving: pg_advisory_xact_lock survives Supavisor transaction-mode pooling (highest-stakes claim); the Postgres 15.1.1.61 sub-minute cron gate; Supabase's 2/hour built-in email and 30/hour post-SMTP auth limits; 500k Edge invocations; 200 Realtime connections / 2M messages; no backups/PITR; 1-week pause; 2-project cap; Resend's 100/day cap; SvelteKit 2.70.2 vs 3.0-RC pinning. Also noted: Supabase Queues (pgmq) provides AD-13's outbox semantics as a managed primitive - worth considering at build time.
- (question) Amazon SES figures were optimistic: $0.10/1000 is a-la-carte only (Essentials tier is $0.16/1000), and AWS promises only an INITIAL RESPONSE within 24h on production-access requests with multi-day waits documented. The sandbox ceiling is 200/day to verified recipients only. Under AD-14 production access is therefore a hard precondition with an unbounded lead time, not a convenience.
- (event) Spine revised against all 5 reviewer lenses: 25 ADs grew to 26, with AD-11 (sequential deterministic closes), AD-12 (expiry authoritative for validation), AD-14 (seed outside the readable log, contender order pinned), AD-15 (server-side identity binding), AD-16 (read-side RLS + secrets), AD-19 (external liveness detection, promoted out of Deferred), AD-20 (core version stamping + fail-stop deploys), AD-22 (League Clock reset set), AD-23 (winning amount != Cap Hit) added, and AD-4/5/6/7/8/10/13/17/21 tightened. New 'Conflicts to Resolve Upstream' section carries the 2 blocking PRD defects plus the Netlify credit-model and availability-target findings. Stack corrected: Node 24 LTS not 26; Netlify credit model documented.
- (event) spine finalized
- (constraint) User constraint added 2026-08-17: ZERO budget. No paid tiers on any vendor.
- (version) CORRECTION to the Netlify credit finding, verified 2026-08-17: DEPLOY PREVIEWS AND BRANCH DEPLOYS COST 0 CREDITS. Only PRODUCTION deploys cost 15. The earlier '~20 deploys/month' framing was therefore misleading - iteration is free, only promotion to production is metered. Full free-plan meter set: production deploys 15 credits, bandwidth 20 credits/GB, compute 10 credits/GB-hour, web requests 2 credits/10,000, AI inference. Hitting 300 credits pauses the sites until the next month.
- (decision) Projected auction-month Netlify credit burn: ~5 production deploys (75) + ~50k web requests (10) + ~1.5-3GB bandwidth (30-60) + ~1 GB-hour compute (11) = roughly 130-160 credits against a 300 budget, about 2x headroom. Build month is deploy-dominated but survivable because previews are free. CONCLUSION: free Netlify IS viable for this project; the earlier 'free tier will not carry this' finding is withdrawn.
- (version) Oracle Cloud Always Free checked 2026-08-17 as a $0 always-on VM alternative: still exists but Oracle SILENTLY halved the Ampere A1 allowance from 4 OCPU/24GB to 2 OCPU/12GB effective 2026-06-15 with no announcement, and is TERMINATING over-limit instances from 2026-08-18. 2 OCPU/12GB is ample for this app, but demonstrated willingness to change terms silently and terminate instances makes it a poor bet under a live money auction.
- (constraint) Free-tier email landscape verified 2026-08-17 - NONE meets AD-18's >=500/day burst requirement on a free plan: Brevo 300/day (9000/mo, no card) but independently tested deliverability problems with Yahoo/AOL and Outlook/Hotmail spam classification; Mailjet 6000/mo but ~200/day; SMTP2GO only 1000/mo though best-in-class deliverability. Email, not hosting, is the binding zero-budget constraint - and deliverability failure directly breaks SM-3 (a primary metric at 100% target).
- (decision) PROPOSED zero-budget notification restructure: make Discord the PRIMARY per-manager alert channel via @mention in the league channel (free, unlimited at this volume, push-notifies phones, and the PRD already requires every event be posted there), demoting email to secondary/backup. This drops email volume from ~1500-2500 to roughly auth magic links plus phase notices - a few hundred - which sits comfortably inside any free tier and removes deliverability from the critical path. AD-17's outbox already makes transport pluggable, so this is a channel-priority change, not an architectural one. Requires a manager -> Discord user ID mapping and a PRD amendment to FR-27, whose stated job-to-be-done is 'tell me the moment I'm outbid, ON THE CHANNEL I ACTUALLY READ'.
- (decision) [ADOPTED] Host stays Netlify + Supabase, both free tiers. User's choice after the credit finding was corrected and both zero-budget options shown to be viable.
- (decision) [ADOPTED] DISCORD ONLY - no email anywhere in the system. User's explicit instruction: 'use discord only, forget email'. Consequences worked through: (a) FR-4's magic-link auth is replaced by DISCORD OAUTH via Supabase Auth (a listed provider, signInWithOAuth({provider:'discord'})), which the PRD anticipated by tagging magic-link as an [ASSUMPTION] with 'an equally reasonable substitute'; (b) FR-27's email notices become Discord @mentions in the league channel, with FR-27's three mutable categories implemented as mention-or-no-mention so the public record stays complete either way; (c) SMTP, its credentials, the vendor, Supabase's 2/hour built-in cap and the 30/hour post-SMTP auth cap all disappear from the design.
- (decision) Discord OAuth collapses two problems into one: authentication AND the Manager -> Discord user id mapping that @mention notifications require are the same fact, obtained once at sign-in.
- (version) Verified 2026-08-17: Supabase Auth lists Discord as a supported OAuth provider (signInWithOAuth({provider:'discord'})). Discord webhooks are limited to 30 requests/minute per webhook URL, independent of bot global rate limits, and support allowed_mentions to control which mentions actually notify.
- (decision) New AD-27: Discord is now a single point of failure for BOTH authentication and notification, so its outage is total. Mandated mitigations: >=30 day sessions so an outage does not log anyone out; a commissioner break-glass sign-in independent of Discord so FR-34 pause stays reachable; and AD-19's liveness alert must not route through Discord since it may need to report Discord is down.
- (event) Spine re-finalized for zero budget + Discord-only. 27 ADs. Lint clean.
- (event) UPDATE RUN 2026-08-18. Two drivers: (1) CAP-1 import corrected upstream in SPEC.md and PRD FR-1/FR-3 -- the Fantrax import is thirty-one CSV files, one Free Agent pool plus one per Team, not two; (2) absorb the now-final UX spines at planning-artifacts/ux-designs/ux-BBSL-Appspiration-2026-08-17/ (DESIGN.md, EXPERIENCE.md). Inherited without re-asking: Coaching path, spine is the only deliverable, zero budget, Netlify+Supabase free tiers, Discord-only.
- (constraint) Import is THIRTY-ONE files, not two (SPEC CAP-1, PRD FR-1/FR-3 corrected 2026-08-18). Architecturally this is not a bigger version of the same operation: 31 files arrive across multiple requests, a single Team's file can be re-supplied without redoing the other 29, per-file parse status must persist between requests, outstanding Teams must be NAMED, and commit is all-or-nothing. That requires import state that survives between requests -- a staging concern the current AD-24 does not address, since it assumed one file replacing prior state in one transaction.
- (question) NEW STRUCTURAL FORK found while reconciling EXPERIENCE.md: the refusal screen reports BOTH gates -- 'Cap refused' AND 'Slots passed, Roster Count would be 10 of 12' -- on every refusal. The current AD-1 contract returns Rejected<reason, arithmetic>, a SINGULAR reason, which cannot express the outcome of a gate that passed. Reporting both gates therefore changes the core's public return type, not just the view. Surfaced to the user rather than decided silently.
- (decision) [ADOPTED] CORE CONTRACT WIDENED. The core exposes evaluate(state, command, now) -> GateResults, returning EVERY gate's outcome (passed or refused, each with its arithmetic), and decide() returns Rejected<GateResults> rather than a singular reason. One evaluator, two consumers: decide() calls it to authorise, and the read path calls it directly to disable controls with stated reasons and to render the Maximum Bid breakdown. Forced by EXPERIENCE.md's refusal pattern, which reports the gate that PASSED alongside the one that refused -- unrepresentable in a singular Rejected. Prevents the alternative the user rejected: a fragment of the capacity rule living in the view layer to reconstruct the passing gate, which is the second-implementation divergence AD-2 exists to forbid.
- (decision) [ADOPTED] IMPORT IS STAGED, THEN PROMOTED ATOMICALLY. Parsed rows from the 31 files land in staging tables keyed by Team; per-file parse status persists between requests; a single Team's file re-supply replaces only that Team's staged rows; the per-Team preview reads staging; commit is one transaction promoting staging to live reference tables. Chosen over client-held-until-commit (a refresh loses all 31 on setup day with 30 managers waiting) and over raw files in Storage (per-file status and the preview would need re-parsing on every view, or a status table anyway). A partially promoted League is never a reachable state.
- (decision) READ FRESHNESS IS EXPLICIT AND SERVER-STAMPED. Every projection read carries the event seq it was folded to; the client compares that watermark and its Realtime connection health to derive exactly one of Live / Reconnecting / Stale. In anything but Live, money either carries its age or the control it would authorise is disabled. Prevents the failure EXPERIENCE.md calls the worst in the product: a stale board that still looks live, where a manager reads a figure, believes it current, and bids against it. AD-9 already makes the server the only authority; this makes the client's uncertainty visible rather than silent. Countdowns are exempt -- they derive from absolute close times the client already holds (AD-3), so they never freeze on disconnect.
- (decision) PHASE AND ROLE GATE THE SURFACE FROM ONE SERVER-RESOLVED SOURCE. The League phase (Setup / Auction / Contract Assignment / Archived) and the viewer's role resolve server-side per request, from the same application tables AD-15 binds identity to, and produce ONE destination list. EXPERIENCE.md renders that list twice -- the persistent Maximum Bid strip's sheet and the header menu -- and the two can never offer different sets. A destination not live in the current phase is not rendered and its route refuses server-side; hiding UI is never the check (existing Authorisation convention). Prevents two navigation surfaces drifting, and a client-held phase copy outliving a transition.
- (constraint) EXPORT PRECISION TRAP found while reconciling DESIGN.md: money renders abbreviated to one decimal EVERYWHERE in the UI ($14.5M), which is lossless only because every BBSL figure sits on the $500,000 grid. The Fantrax export must still emit exact integer dollars -- an abbreviated figure reaching a CSV cell would be a silent corruption of the round-trip. AD-8's 'formatting happens in the view layer only' now names the export explicitly as not-a-view.
- (version) Supabase Realtime subscribe-status API verified 2026-08-18: channel.subscribe((status, err) => ...) reports SUBSCRIBED / CHANNEL_ERROR / TIMED_OUT / CLOSED, and the callback fires on ANY status change, not only the initial subscribe -- so AD-29's client-side connection detection is buildable as specified. Caveat found in the same check: supabase/realtime issue #1414 documents channels going CHANNEL_ERROR after hours of running, and a channel can report SUBSCRIBED while delivering nothing, so connection status alone is not sufficient evidence of freshness.
- (decision) AD-29 TIGHTENED by the adversarial lens, and this is the highest-value catch of the pass: a STALLED WATERMARK MUST NEVER IMPLY STALENESS in this league. The auction runs as a relay of timezone clusters with long genuine silences -- six quiet hours at 4am Pacific is a healthy auction, which is exactly why the League Clock is 48 hours. The liveness question is 'can I still reach the server?', never 'has anything changed?'. Freshness therefore derives from connection status AND an explicit lightweight periodic re-read of the watermark, never from the absence of pushed messages. A client conflating the two would disable bidding across every quiet stretch of the night. FRESHNESS_WINDOW and STALE_WINDOW are named constants in core/constants.ts.
- (decision) Further adversarial-lens holes closed: (1) the freshness watermark is ONE GLOBAL max-seq read from one source, not a per-table stamp -- two units stamping their own tables would let the board and the Maximum Bid strip report different ages; (2) the Free Agent pool stages too, keyed as a distinguished 'pool' source, rather than being exempt from AD-28 for having no Team; (3) the GateResults gate set is FIXED PER COMMAND TYPE in core/types.ts, so a PlaceBid result always carries both cap and slots and no caller receives a partial record to guess at; (4) phase is a projection folded from the log obeying AD-5, never a hand-set flag -- a commissioner appends the causing event, they do not flip it; (5) the abbreviated money rendering is permitted in the UI AND Discord payloads (same figures, same readers) and forbidden in CSV exports.
- (event) Spine updated for the 31-file import and the UX spines. 27 ADs grew to 30: AD-28 (import staged then promoted atomically), AD-29 (read freshness explicit, stale money disables), AD-30 (phase and role gate the surface from one server-resolved source). Amended in place, IDs stable: AD-1 (evaluate/decide contract), AD-7 (both gates always evaluated, displayed Maximum Bid is read-path evaluate output), AD-8 (abbreviated rendering permitted in UI and Discord, forbidden in CSV), AD-15 (unregistered refusal discloses nothing, expiry distinct from sign-out), AD-24 (31 files, file-level vs row-level refusal, outstanding Teams named). New Naming, Freshness and Visual identity conventions; Accessibility convention updated to measured contrast and the greyscale acceptance test. Import sequence diagram added. Lint clean, 0 findings.
- (event) spine finalized
- (event) Update run opened 2026-09-08, driven by the approved Sprint Change Proposal 2026-09-07 and the PRD amended the same day (fourth pass: FR-37 rewritten, FR-40 added, examples 29-35). Upstream was updated FIRST so the spine amends a PRD that already carries the rule, not one it contradicts.
- (decision) AD-31 ADDED: a cancellation is compensating, records its own restoration, and shares one restorer with the commissioner void. Binds FR-40 and FR-32. Two halves. (1) BidCancelled carries its DECISION -- cancelled seq, cause, and the restored team/seq/amount or null -- and the projection reads it rather than re-deriving; re-deriving would mean re-running the gate suite over candidate bidders inside a fold, and the fold's answer could drift from the decision the close actually made. Same posture AD-14 takes for the randomizer draw and AD-23 for winning-amount-vs-cap-hit. (2) The selection algorithm -- walk surviving history down, re-evaluate cap and slots, skip failures, stop at the first pass -- lives in ONE pure function core/rules/restore.ts consumed by both FR-40 and Story 7.2's void.
- (decision) The void and the cancellation differ in exactly THREE ways and each is a parameter, not a different algorithm: the void erases its bid from the fold while the cancellation retains it as history stripped of leadership; the void restores the prior Auction Clock value while the cancellation leaves the clock alone; the void removes the bid's League Clock reset while the cancellation leaves it standing. Two implementations would disagree about the skip rule, and the disagreement surfaces only when a commissioner voids a bid on an auction whose under-bidders have since filled their rosters -- the worst moment to find it. Story 7.2's spec must cite restore.ts before 7.2 is built; building 10.4 first makes 7.2 substantially cheaper.
- (decision) AD-22 AMENDED with the sharpest hazard in this change: BidVoided and BidCancelled are one line apart in any reducer and have OPPOSITE League Clock semantics. A void removes the reset (clock recomputes shorter, FR-32/OQ-7); a cancellation leaves it standing, because the bid was valid when placed and only its landing place disappeared. A reducer treating them alike shortens the Auction Phase every time a roster fills, ending the auction early for reasons no manager can trace. Cancellation and restoration added to the explicit not-a-reset list beside bid voids.
- (decision) AD-11 AMENDED and its status changes: sequential closing is now load-bearing for a rule that did not exist when it was written. Unlimited lottery participation (FR-18) is only safe because closes commit one at a time -- a Team in several simultaneously-expiring lotteries that wins the first is removed from the remainder by the cascade BEFORE they are drawn. Under a batched fold it wins two. This is the strongest available argument against ever relaxing AD-11 for throughput, and it was recorded as such.
- (decision) AD-2 AMENDED: the fixed-per-command-type gate set gains a SECOND command type, RestoreLeadingBid, declaring {cap, slots} and nothing else. Increment, granularity, self-bid and contention were satisfied when the bid was originally placed; expiry and phase belong to the close that triggered the cancellation. Declaring the narrower set is what stops a restoration being implemented as a synthetic PlaceBid -- which would re-run the increment rule against a price that has just FALLEN and refuse every restoration that mattered.
- (constraint) No migration required. auction_events.event_type is a generic text column with only a not-blank constraint (supabase/migrations/20260821020000_auction_events.sql:63), so BidCancelled needs no schema change. Verified against the migration, not assumed. AD-26 still governs any future column.
- (decision) AD-31 TIGHTENED by the adversarial lens -- the highest-value catch of this pass. Two units obeying every AD could still build incompatibly on the cancellation write path. Three holes, one fix: (1) AD-31 said BidCancelled carries the restoration decision but not WHO appends it, so close.ts and restore.ts could each build half -- close writing a null restoration and restore writing a second event to fill it in, leaving a log window where an auction has no leader. (2) Intra-close append ORDER was unspecified: a fold reading BidCancelled before the AuctionClosed that caused it computes capacity against the wrong roster. (3) The evaluation BASIS for the restored team's re-validation was unspecified: handed the pre-close snapshot it restores a team the triggering close has just disqualified. FIXED: close.ts is the sole appender, AuctionClosed first then each BidCancelled in cascade order, and restoration is evaluated against post-close state -- the same basis AD-11 hands the next close. restore.ts decides and appends nothing.
- (event) Reviewer gate run 2026-09-08. lint_spine.py: 0 findings. Currency/version lens: nothing to flag -- this amendment names no new technology, library or version, and its one factual claim (auction_events.event_type is generic text, so no migration) was verified against the migration file rather than asserted. Adversarial lens: one hole found and closed, logged above. Lenses ran on the SESSION tier inline rather than as pinned bmad-review-lens subagents, because subagent dispatch is disabled in this session; noted so the pass can be re-run properly if wanted. The rules-correctness question -- which the customization keeps on the session tier by design -- is the one that mattered here and it ran at the intended tier.
- (event) spine finalized (fourth pass, 2026-09-08). 30 ADs grew to 31: AD-31 added. Amended in place with IDs stable: AD-2 (RestoreLeadingBid command type), AD-11 (cancellations commit inside the sequential close; sequential ordering now load-bearing for unlimited lotteries), AD-22 (cancellation resets nothing and removes nothing -- opposite of a void), AD-25 (examples 1-35, re-verify 18-25 as a block). Frontmatter binds FR-1..FR-40 and examples 1-35. Capability map gains an FR-40 row.
- (decision) AD-32 ADDED (next unused id; AD-1..AD-31 stable, none renumbered) -- The world changes by mutation plus record, and a world change refuses rather than cascades. BINDS FR-41..FR-43, CAP-22/CAP-23, team_rosters, core/projection/contracts.ts, core/rules/roster-import.ts, core/rules/bidding.ts, adapters/fantrax/. PREVENTS a roster change that reaches the cap arithmetic but not the replay, a second implementation of the two bid gates, and a second trigger for Bid Cancellation. RULE: a Roster Move or Drop mutates the reference tables AND appends a record event in ONE transaction under the AD-6 global lock. AD-4 already says the world is not event-sourced and team_rosters stays a mutable table -- but the record is not optional, and the reason is narrower than auditability: every cap and slot figure is a FUNCTION OF THE ROSTERS AT A GIVEN INSTANT, so a Move that mutates the table without appearing in the log makes AD-5's rebuild, AD-21's restore and AD-3's synthetic-clock replay fold yesterday's bids against today's rosters. The event therefore carries the WHOLE DELTA -- every Player, both Teams, and the Slot kind and Cap Hit on each side.
- (constraint) AD-31 IS DELIBERATELY UNAMENDED BY THIS PASS, and that is the load-bearing outcome rather than an omission. Its trigger rule -- cancellation is caused by an Auction Close and by nothing else, and a restoration never triggers one -- survives word for word because a Roster Move or Drop that would leave either Team failing a gate is REFUSED rather than allowed to cancel bids. The rejected alternative (reuse restore.ts to stand down surplus commitments after a trade) would have added a second cancellation trigger to a cascade AD-31 bounded on purpose, reopening the re-entrancy argument that AD's last-but-one bullet explicitly closes, and would have let an administrative act strip a Player from a Team that took no action.
- (constraint) MIGRATION REQUIRED, and verified against the file rather than assumed -- the same discipline the 2026-09-08 pass applied when it confirmed BidCancelled needed none. supabase/migrations/20260824020000_live_reference_tables.sql:61-62 carries constraint team_rosters_roster_slot_kind_check check (roster_slot_kind in ('active_bench','injury_reserve','minor_league')), enumerating exactly three kinds, so admitting a fourth ('dead_money') IS a schema change and takes a migration file applied dev-first per AD-26. Contrast BidCancelled, which needed none because auction_events.event_type is generic text. Second claim also verified at the same file, line 46: fantrax_player_id is 'text not null unique' ACROSS EVERY TEAM, which is why an Existing Contract moves by UPDATE of team_id and never delete-then-insert.
- (decision) AD-25 EXAMPLE RANGE EXTENDED to 1-43 (was 1-35). Examples 36-43 are executable specification like the rest, and two pairs are load-bearing as PAIRS rather than individually: 40/41 differ only by the 2RK designation and produce different Maximum Bids, which is the regression test proving the rookie prefix survives the Fantrax import (it does not today -- CONTRACT_END_YEAR matches NRK in a non-capturing group); 40/43 move Maximum Bid in OPPOSITE directions, which is what stops 'a Drop lowers Maximum Bid' being implemented as an unconditional rule when it is actually derived from the Cap Hit the Player was charging.
- (decision) AD-24 REAFFIRMED, NOT AMENDED, for the new Fantrax READ path. Every field name and response shape of getTeamRosters stays inside adapters/fantrax/, so a shape change between offseasons is one module -- the same confinement AD-24 already imposes on the CSV column maps. The read is SHELL: it runs outside the AD-6 write lock and outside the pure core, because the core reads no clock and no network and a divergence is not a rule. A read produces a proposal and never an event, row or projection; nothing downstream of the reader is reachable from a code path that writes.
- (event) Reviewer gate run 2026-09-10. lint_spine.py: 0 findings, before and after fixes. Currency/version lens handled INLINE, not dispatched -- this amendment names no new technology, library or version, and its two factual claims (the three-value slot-kind check constraint, and fantrax_player_id being unique across every Team) were verified against 20260824020000_live_reference_tables.sql lines 61-62 and 46 rather than asserted. Rubric walker SKIPPED, consistent with the commissioner's decision to skip the PRD gates the same day. Adversarial divergence lens RAN, on the bmad-review-lens agent per the activation step's tiering rule -- it is a recall lens walking an explicit brief, not a rules-correctness judgement. Six holes found; five closed, one rejected as out of scope. Full review at reviews/review-adversarial-2026-09-10.md.
- (decision) AD-32 TIGHTENED by the adversarial lens, five holes closed. (1) RECONSTRUCTION FOLDS, THE LIVE PATH DOES NOT -- the sharpest catch. AD-32 said the event exists 'so a fold can reproduce the world as it stood' without naming who folds, from what baseline, or where it lives, while AD-4 flatly denies team_rosters is event-sourced. A builder obeying AD-4 literally restores from the LIVE TABLE and reproduces the exact bug AD-32 claims to prevent. Closed: AD-4 governs the live write path only; AD-5 rebuild, AD-21 restore and AD-3 replay reconstruct from the last reference-data snapshot plus a forward fold of RosterMoved, DropRecorded, FR-38 eligibility and FR-32 cap events, in-memory and read-only, no migration. (2) ONLY A SETTLED CONTRACT MOVES -- a Player contested in an open Auction is neither an Existing nor an Auction Contract, so contractsReducer would have nothing to apply a transfer to; now a third refusal ground checked BEFORE the money and slots gates. This also amended PRD FR-41, which had the same gap. (3) A MOVE IS A THIRD COMMAND TYPE per AD-1/AD-2 -- 'calls the same gates' invited synthesizing a PlaceBid and force-passing the inapplicable ones, which IS the second implementation the next clause forbids, and would route two Teams' opposing deltas through a cap gate AD-7 defines as single-Team and incremental. (4) RosterSlotKind is a closed four-member union, exhaustive matches with no default -- the chargedCapHit fallthrough is sanctioned for that function alone and must not become a general pattern. (5) The Fantrax reader never receives a database client as an argument, making a write from inside it a type error rather than a convention; role separation is unavailable since importer and reader share the one service role.
- (question) OPEN, DEFERRED, NOT INTRODUCED BY THIS AMENDMENT: is the tick close sweep ONE transaction or one per close? AD-11 says each close is 'committed to the state the next close is evaluated against', which reads either way; AD-6's lock is transaction-scoped, so under the per-close reading the lock is RELEASED between closes and another writer can interleave mid-sweep. The adversarial lens raised this as a Roster Move hole. REJECTED as scoped to this change, and the reasoning matters: if the sweep commits per close, an ordinary MANAGER BID can already interleave between two closes, and bids are orders of magnitude more frequent than commissioner trades. The ambiguity is entirely pre-existing and AD-32 neither widens nor depends on it. Pinning it means deciding the tick's transaction granularity against AD-10's restart-safety requirement -- a real decision about AD-6/AD-11 that should not be smuggled in on a roster-recording amendment. REVISIT CONDITION: before Story 3.5's tick is exercised under the Story 9.7 moderator pilot, or sooner if any interleaving defect is observed.
- (event) spine finalized (fifth pass, 2026-09-10). 31 ADs grew to 32: AD-32 added, then tightened by five adversarial-lens closures before finalizing. Amended in place with IDs stable: AD-25 (examples 1-43; 36-43 added, with 40/41 and 40/43 load-bearing as PAIRS rather than individually -- a first for that AD). Frontmatter ranges moved to FR-1..FR-43, examples 1..43, CAP-1..CAP-23. Capability map gained two rows. AD-31 DELIBERATELY UNAMENDED and that is the load-bearing outcome. AD-24 reaffirmed for the new read path, not amended. lint_spine.py 0 findings.
